# EnduranceTestGUI 仕様書

GUIアプリケーションの耐久テスト（ランダムクリック・ランダム操作）を行うための
デスクトップツール。C++ / Qt5及びQt6 Widgets で実装し、CMake でビルドする。

詳細仕様はここに記す（1〜8章は現在の実装の説明、9章は変更履歴）。

## 1. 目的

- 対象アプリケーション（別プロセスのウィンドウ）に対して、ランダムなマウスクリック・
  ドラッグ・キー入力・スクロール・ショートカットキーを継続的に送り込み、長時間動作させた
  ときにクラッシュ/メモリリーク/フリーズ等が発生しないかを確認する耐久テストを行う。
- テスト対象は「特定のウィンドウ/プロセス」。画面全体や自分自身のUI部品は対象としない。

## 2. 対応OS・対応Qt

- macOS（実装済み） — Accessibility権限 + Quartz Event Services (CGEvent) で
  他プロセスへの入力送信、`CGWindowListCopyWindowInfo` でウィンドウ一覧取得、
  Accessibility API (AXUIElement) でコンテキストメニュー探索。
- Linux/X11（実装済み） — XTest拡張 (`XTestFakeMotionEvent` 等) で入力送信、
  EWMH (`_NET_CLIENT_LIST` 等) でウィンドウ一覧取得、AT-SPI（任意、ビルド時検出）で
  コンテキストメニュー探索。
- Windows は現時点で未対応（プラットフォーム抽象層はあるので追加は可能）。
- Qt5 / Qt6 のどちらでもビルド可能（`find_package(QT NAMES Qt6 Qt5 ...)` で検出）。
  ※開発機にはQt6のみが入っており、Qt5でのビルド確認は未実施。

macOS / Linux ともに `src/platform/PlatformAutomation.h` の同一インターフェースを
各OSの実装（`src/platform/macos/Automation_mac.mm`, `src/platform/linux/Automation_linux.cpp`）
が実装する形。呼び出し側（MainWindow, RandomActionEngine）はOS差を意識しない。

## 3. ディレクトリ構成

```
EnduranceTestGUI/
├── CMakeLists.txt              # メインアプリのビルド設定（テストターゲットappも含む）
├── Info.plist.in               # macOS用バンドル情報
├── SPEC.md                     # 本ファイル
├── LICENSE                     # EnduranceTestGUI自身のライセンス（MIT License、6.11節）
├── THIRD_PARTY_NOTICES.md      # 動的リンクしているサードパーティライブラリのライセンス一覧（6.11節）
├── LICENSES/                   # LGPLv3/GPLv3/LGPLv2.1/GPLv2の条文全文（参考同梱、6.11節）
├── docs/                        # GitHub Pages公開用（Settings > Pages > Deploy from a
│   │                            # branch > /docs を想定したディレクトリ構成。9節 v0.50参照）
│   ├── index.html                # ランディングページ。ブラウザの言語設定に応じてja//en/へ自動リダイレクト
│   ├── .nojekyll                  # GitHub PagesにJekyll処理をさせず静的ファイルとして配信させるための空マーカー
│   ├── ja/
│   │   ├── index.html            # エンドユーザー向けHTML操作マニュアル・日本語版（実機スクリーンショット付き）
│   │   └── images/                # 同、スクリーンショット一式
│   └── en/
│       ├── index.html            # 同・英語版（UIも英語表示に切り替えて撮影し直したスクリーンショット付き）
│       └── images/                # 同、スクリーンショット一式
├── src/
│   ├── main.cpp
│   ├── I18n.h/.cpp             # 日本語/英語のUI文字列切り替え（QSettingsで永続化、次回起動時に反映）
│   ├── MainWindow.h/.cpp       # メインGUI（対象選択・ステップ構成・操作パラメータ・ログ）
│   ├── RandomActionEngine.h/.cpp  # ランダム操作の実行ループ（QTimer駆動、ステップ順次実行）
│   ├── StepEditorDialog.h/.cpp # ステップの操作領域（対象GUIの全領域／名前付き操作領域）を選ぶダイアログ
│   ├── NamedRegionEditorDialog.h/.cpp # 名前付き操作領域（矩形＋除外矩形）を作成/編集するダイアログ
│   ├── ActionParamsEditor.h/.cpp # 1つのActionParams（ドラッグ距離等）を編集するウィジェット
│   ├── ActionKindEditor.h/.cpp # 1ステップの操作種別・重み・回数を編集するウィジェット
│   ├── DefaultActionParamsDialog.h/.cpp # デフォルトの操作種別・詳細設定を編集するダイアログ（「デフォルト」ボタン）
│   ├── RegionSelectorOverlay.h/.cpp # 画面上で矩形領域を複数選択するオーバーレイ
│   ├── RegionHighlightOverlay.h/.cpp # 編集中の操作領域を画面上に表示するだけの非対話オーバーレイ
│   ├── StopPanel.h/.cpp         # 常時最前面の緊急停止パネル
│   ├── TestConfig.h             # テスト設定（RegionStepとTestConfig。1回の実行の全パラメータ）
│   ├── TestConfigJson.h/.cpp    # NamedRegion/RegionStep/ActionParamsのJSON変換（プリセット保存/読込用）
│   ├── TestStatistics.h/.cpp    # 複数回実行分のクラッシュ統計の集計・永続化（確率的クラッシュの解析、10節）
│   ├── StatisticsDialog.h/.cpp  # 統計（試行回数・クラッシュ率・ステップ別内訳）表示ダイアログ
│   └── platform/
│       ├── PlatformAutomation.h # OS非依存の入力送信/ウィンドウ列挙/メニュー探索インターフェース
│       ├── GlobalHotkey.h       # OS非依存のグローバル緊急停止ホットキー（pimpl）
│       ├── macos/Automation_mac.mm
│       ├── macos/GlobalHotkey_mac.mm
│       ├── linux/Automation_linux.cpp
│       └── linux/GlobalHotkey_linux.cpp
└── testtarget/                  # 動作確認用の簡易ターゲットアプリ（別実行体 TestTarget）
    ├── CMakeLists.txt
    └── src/
        ├── main.cpp
        ├── TestTargetWindow.h/.cpp
        └── CounterButton.h/.cpp
```

## 4. ビルド方法

```sh
cmake -B build -S .
cmake --build build
```

- `EnduranceTestGUI` （本体）と `TestTarget`（動作確認用ターゲット）の2実行体が生成される。
- macOS: `CMAKE_PREFIX_PATH` 未指定時、CMakeLists.txtは`~/Qt/<version>/macos`
  （Qt公式インストーラのレイアウト）を優先的に探し、見つからなければ
  `/usr/local/lib/cmake`（Homebrewの旧Intelプレフィックス）にフォールバックする。
  公式インストーラのQtはuniversal binary（x86_64 + arm64）なので、Apple Siliconでは
  ネイティブarm64としてビルドされる。Homebrewのx86_64専用Qtしかない場合は
  CMakeLists.txtが自動的に`CMAKE_OSX_ARCHITECTURES=x86_64`を設定し、Rosetta 2経由の
  x86_64バイナリとしてビルドする（この場合、後述のアクセシビリティ権限が不安定になる
  既知の問題があるため、可能な限りuniversal/arm64のQtを使うことを推奨する）。
  `-DCMAKE_OSX_ARCHITECTURES=arm64`等で明示的に上書きも可能。
- macOSでは、ビルド後に`EnduranceTestGUI.app`と`TestTarget.app`を自動でad-hoc署名する
  （`codesign --force --deep --sign -`）。バンドルIDが空/未署名のままだとアクセシビリティ
  (TCC)権限が正しく認識されないため。
- Linux: `libX11-dev` `libxtst-dev`（Debian系）または `libX11-devel` `libXtst-devel`
  （RHEL系）が必須。
  加えて `libatspi2.0-dev`（Debian系）/ `at-spi2-core-devel`（Fedora系）があれば
  コンテキストメニュー選択機能が有効になる（無くてもビルド自体は警告付きで成功し、
  その機能だけがno-opになる）。

## 5. 実行時に必要な権限

- **macOS**: システム設定 > プライバシーとセキュリティ > アクセシビリティ で
  EnduranceTestGUI を許可する必要がある（他アプリへの合成入力送信・コンテキストメニュー
  探索の両方に必須）。未許可の場合、開始時にダイアログでOS標準の許可プロンプトを表示する。
  また、ウィンドウタイトル（`kCGWindowName`）の取得には画面収録権限が必要な場合があるが、
  これは自動化そのものには不要（タイトルが空でもアプリ名・PIDでの選択は可能）。
  - **【重要・確定原因】ターミナルから起動すると権限が反映されない**: `EnduranceTestGUI`を
    ターミナル（Terminal.app等）から起動すると、macOSの「responsible process（責任元
    プロセス）」の仕組みにより、アクセシビリティ権限のチェックが**起動元のターミナルアプリ
    自身**に委譲される（`open`コマンド経由でも同様）。つまりEnduranceTestGUIをアクセシビリティ
    一覧で許可していても、ターミナルアプリ自体が許可されていなければ拒否される。
    `log stream --predicate 'subsystem == "com.apple.TCC"'`でTCCのログを見ると
    `attribution={responsible={identifier=com.apple.Terminal, ...}, accessing={identifier=
    com.asobi.endurancetestgui, ...}}`のように表示され、`AUTHREQ_RESULT`の`authValue=0`
    （拒否）で確認できる。
    **対処法**: (a) `EnduranceTestGUI.app`をFinderから直接ダブルクリックで起動する
    （responsible processの委譲が起きず、アプリ自身の権限がそのまま使われる。通常の
    利用方法としてもこちらを推奨）、または (b) 使用しているターミナルアプリ自体も
    アクセシビリティ一覧に追加して許可する。
  - **Apple Silicon + Rosetta 2の注意点（副次的）**: `EnduranceTestGUI`がRosetta 2経由の
    x86_64バイナリとして動作している場合（4節参照）でも権限が認識されない事例があったため、
    universal/arm64のQtでネイティブarm64バイナリとしてビルドすることを推奨する
    （アーキテクチャを変えた場合、システム設定側の古いエントリは削除して追加し直すこと）。
    ただし上記のターミナル起動の問題とは別の要因であり、ネイティブ化だけでは解決しない
    場合がある点に注意。未署名/バンドルID未設定のアプリでも権限が正しく認識されないため、
    4節の自動ad-hoc署名も参照。
  - 権限を付与/変更した後、`EnduranceTestGUI`をフォアグラウンドに戻すと自動的に
    権限状態を再チェックして表示を更新する（`MainWindow`が`QGuiApplication::
    applicationStateChanged`を監視）。それでも反映されない場合はアプリを完全に終了して
    再起動すること。
- **Linux/X11**: 入力送信自体に追加の権限は不要（Xサーバへのアクセスがあれば動作する）。
  コンテキストメニュー選択機能を使うには対象アプリのツールキットがAT-SPIブリッジを
  有効にしている必要がある（GTK/Qtアプリは通常デフォルトで有効）。Waylandには未対応
  （XTestはX11プロトコル前提）。XWayland経由でX11アプリを操作する用途は動作しうるが、
  ネイティブWaylandアプリは対象にできない。
  - **管理者権限（root）がないLinuxユーザーでの動作（v0.47で実機検証）**: 通常の
    デスクトップ利用（自分のアカウントでXセッションにログインし、対象アプリも同じ
    アカウントで起動する）を想定する限り、本ツールの全機能は非rootユーザーのままで
    問題なく動作することを、実際に非特権ユーザー（`sudo`権限なし）を作成し、Xvfb+
    openboxのXセッションからTestTarget・EnduranceTestGUI双方をそのユーザーとして
    起動して確認した。入力送信・グローバルホットキー登録・ログ/スクリーンショット/
    画面録画の`~/Documents`・`~/Pictures`への保存・`kill -9`によるクラッシュ検知と
    異常時自動収集一式（プリセットJSON・操作領域画像・録画フレーム）まで、rootでの
    実行時と同じ結果になることを確認済み。
  - **唯一の例外（検知はできるが内容の閲覧はできない場合がある）**: 6.7節の
    クラッシュダンプ・診断ログ収集機能が探索する`/var/lib/systemd/coredump/`は、
    ディレクトリ自体は一般ユーザーでも一覧参照できる（`755`）ことが多いが、中の
    individual coredumpファイルは大抵`root`所有・`600`または`640`権限で保存されており、
    対象アプリを起動した本人であっても、システムの設定（sudoまたは`coredumpctl`が
    使えるか等）次第では中身を直接読めないことがある。これを実機で
    （`chmod 600 root:root`の疑似coredumpファイルを配置して）再現・確認した上で、
    v0.47で対策を追加した: 見つかったパスに対して`QFileInfo::isReadable()`を確認し、
    現在のユーザー権限では読めない場合はその旨をログに追記する
    （「このファイルは現在のユーザー権限では読み取れません（root権限、または
    coredumpctl等の専用ツールが必要な場合があります）。パスの記録のみ行いました。」）。
    パス自体の検出・記録（`anomaly_<timestamp>_crashreport_location.txt`への書き出し）
    は権限に関わらず行われるため、開発者へ「このパスをroot権限で確認してほしい」と
    伝える材料としては引き続き機能する。
  - `isProcessRunning`（`kill(pid, 0)`ベースのクラッシュ検知）は、対象プロセスが
    別ユーザー所有で`EPERM`が返る場合でも「まだ生きている」と正しく解釈する実装に
    元々なっている（`kill() == 0 || errno == EPERM`）ため、対象アプリを別ユーザーで
    実行しているケースでも誤ってクラッシュと判定することはない。

- 権限が許可されている場合には「権限設定を開く」のボタンは無効化する。

## 6. 機能仕様

### 6.1 対象選択 (Target)
- 起動中の他プロセスの通常ウィンドウを列挙し、コンボボックスで選択する。
- 「更新」ボタンで再列挙。
- 権限状態（許可済み/未許可）を表示し、「権限設定を開く」でOS設定画面を開く
  （macOSはシステム設定のアクセシビリティ画面、Linuxは操作不要のため no-op）。

実装状況（クラッシュ後の対象アプリ再選択、v0.49）: 「クラッシュしても、対象アプリを
もう一度起動すればすぐ同じ条件でテストできるようにしてほしい」という要望を受けて実装
した。そもそも②ステップ構成・①操作領域・タイミング設定は特定のpidに紐付いておらず、
クラッシュ（6.7節）やテスト終了で自動的に消えることはなかった（`m_steps`/
`m_namedRegions`は対象アプリの生死と無関係にアプリ内メモリ上で保持され続ける）ため、
対象アプリを選び直しさえすれば元々同じ設定のまま再実行できていたが、**再選択の手間**
自体が残っていた（一覧再列挙後、コンボボックスの選択が失われて毎回手動で選び直す
必要があった）。
- `MainWindow::onRefreshTargets()`は一覧を再列挙して`QComboBox::clear()`する前に、
  その時点で選択されていたウィンドウのアプリ名を`m_lastTargetAppName`に記憶するように
  した。再列挙後、新しい一覧の中から同じアプリ名を持つウィンドウを自動的に探して
  再選択する（`tryReselectLastTarget()`）。対象アプリが再起動されて新しいpidを持つ
  新しいウィンドウとして出現しても、アプリ名が同じなら自動的に選び直される。
- `MainWindow::onEngineFinished()`（停止・クラッシュ検知どちらでも呼ばれる）で
  `onRefreshTargets()`を呼ぶようにし、実行が終わるたびに一覧を即座に更新するように
  した。既存の「アプリがフォアグラウンドに戻ったら再列挙する」仕組み（`applicationStateChanged`
  監視）と組み合わさり、典型的な流れは: クラッシュ検知→通知ダイアログ（6.7節）→
  ユーザーが対象アプリを再起動→（別ウィンドウ等から）EnduranceTestGUIへ切り替えると
  一覧が自動更新され新しいプロセスが自動的に選び直される→「開始」を押すだけで同じ
  ステップ構成のまま再実行できる、となる。
- `MainWindow::onLoadPreset()`（プリセット読込）にも同じ仕組みを適用し、従来は
  「①で対象ウィンドウを選び直してください」と促すだけだった`targetAppNameHint`を、
  実際にマッチするウィンドウが開いていれば自動選択するようにした（一致しなければ
  従来通り手動選択を促すメッセージにフォールバックする）。
- 実機（Xvfb + openbox）で、ステップを設定し実行中に対象アプリを`kill -9`で強制終了→
  クラッシュ通知ダイアログを閉じる→対象アプリ（同名の新しいプロセス）を再起動→
  「更新」を押すとコンボボックスが新しいpidのエントリへ自動的に選び直されること、
  ステップ・操作領域・タイミング設定が変更前のまま保持されていること、そのまま
  「開始」を押すだけで同じ設定のテストを再開できることを確認した。
  -Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。

実装状況（対象アプリの自動起動コマンド、v0.51）: 「対象アプリの自動起動コマンド（任意）」
欄・「参照...」・「今すぐ起動」ボタンをこのパネルに追加した。①の連続自動実行
（バッチ実行）がクラッシュ検知後に対象アプリを自動再起動するために使う（設定は任意、
プリセットJSONにも保存される）。詳細・実機確認内容は6.7節末尾のv0.51を参照。

連続実行ボタンを押してツールがすぐに立ち上がらないとエラーができます。テスト対象の
ツールは場合によっては起動に時間がかかる可能性もあるので、起動まで一定時間待つ
パラメータを設定できるようにしてください。

実装状況（対象ツールの起動待ち時間パラメータ、v0.68）: 「対象アプリの自動起動
コマンド」の下に「対象ツールの起動検知後、さらに待つ時間: 0 秒」というスピン
ボックスを追加した（0〜300秒、デフォルト0=従来通り即座に開始）。

- 対象ウィンドウが出現したことを検知してから実際に`beginRun()`（安全確認・
  ②起動時セットアップ・③ステップ構成の実行開始）を呼ぶまでの間に、ここで
  指定した秒数だけ追加で待つ。対象ウィンドウがマップされた直後はまだ自身の
  UIを描画し終えていない/入力を受け付けられない状態のツールに対して、
  `beginRun()`内の安全確認（`windowPidAtPoint`によるフォアグラウンド確認）が
  時々失敗してしまうケースに対応する。
- 連続実行（⑤）のキル→再起動サイクル、①バッチ実行の自動/手動再起動待ち、
  ▶開始の「起動してから開始する」オプションの3経路すべてに共通で適用される
  よう、対象検知後の分岐を`MainWindow::startRunAfterLaunchWait()`という
  1つの関数にまとめた。0秒（デフォルト）の場合は従来と全く同じタイミングで
  即座に`beginRun()`を呼ぶため、既存の動作に影響はない。待ち時間中は
  専用の`QTimer`（`m_launchWaitTimer`、シングルショット）で管理し、
  「■停止」を押すとこの待ち時間中でも即座に中断できる（`onStop()`に
  `m_batchWaitTimer`と対称なガードを追加）。
- プリセットJSON（`targetLaunchCommand`と同じ場所）にも保存され、次回
  読み込み時に復元される。
- 実機（Xvfb + openbox）で、3秒に設定した状態で連続実行を押すと「対象ツールの
  起動を検知しました。安定するまでさらに3秒待ちます...」というログが表示され、
  ちょうど3秒後（ログのタイムスタンプで確認）に次の処理（`beginRun()`の
  呼び出し）が実際に始まること、待ち時間中に「■停止」を押すと「連続実行を
  中断しました（起動待ち時間の経過待ち中でした）」と表示されてすぐに中断・
  ①②③が再度有効化されることを確認した。-Wall -Wextra -Wpedantic付きの
  クリーンビルドで警告0件を維持。

テスト対象ツールの全体のウィンドウサイズを保存する機能と、保存したウィンドウサイズに
合わせてテスト対象ツールのウィンドウサイズを変更する機能をつけてください。

実装状況（対象ツールのウィンドウサイズ保存・復元、v0.72）: 「対象ツールの起動検知後、
さらに待つ時間」欄の下に、保存済みサイズを表示するラベルと「現在のウィンドウサイズを
保存」「保存したサイズに変更」の2ボタンを追加した。

- 保存は1件のみ（複数のサイズを切り替えて使うような要望ではなく、単純に「保存/復元」
  という要望だったため）。「現在のウィンドウサイズを保存」を押すと、①で選択中の対象
  ウィンドウの現在の大きさ（`PlatformAutomation::queryWindowBounds`）を
  `m_savedWindowSize`に記録し、ラベルを「保存したウィンドウサイズ: 幅W 高さH」に
  更新する。保存前は「(未保存)」と表示する。
- 「保存したサイズに変更」を押すと、既存の「ウィンドウ操作」種別（6.5節）のリサイズが
  内部で使っているのと同じ`PlatformAutomation::resizeWindow(pid, windowId, size)`を
  呼び、①で選択中の対象ウィンドウを保存済みサイズへ変更する（保存前に押すとエラー
  ダイアログを出す）。対象を切り替えてから押せば、直前に保存したサイズを別のウィンドウに
  適用することもできる（同じ対象アプリを再起動した後などを想定）。
- プリセットJSON（`hasSavedWindowSize`/`savedWindowWidth`/`savedWindowHeight`）にも
  保存/復元され、アプリを再起動しても保存済みサイズが失われない。
- 実機（Xvfb + openbox）で、`TestTarget`のウィンドウサイズを保存 →
  （`xdotool windowsize`で）大きく変更 → 「保存したサイズに変更」で、`TestTarget`
  自身が自己申告するウィンドウサイズ（画面下部の「ウインドウ位置・サイズ」表示）が
  保存時とピクセル単位で一致することを確認した。なお、対象ウィンドウ自体の内部レイアウト
  上の最小幅より小さいサイズへの変更を試みた場合、Qtのレイアウト制約によって要求より
  大きい値に丸められることがある（`xdotool windowsize`で直接同じサイズを指定しても
  同様に丸められることを確認済みであり、今回追加した`resizeWindow`呼び出し自体の精度
  問題ではなく対象アプリ自身のウィンドウサイズ制約による、既存の「ウィンドウ操作」
  リサイズ種別とも共通の挙動）。-Wall -Wextra -Wpedantic付きのクリーンビルドで
  警告0件を維持。

テスト対象ツールの現在のウインドウ位置を保存する機能と、保存したウィンドウ位置に
合わせてテスト対象ツールのウィンドウ位置を変更する機能をつけてください。

実装状況（対象ツールのウィンドウ位置保存・復元、v0.73）: 上のウィンドウサイズ保存・
復元（v0.72）と全く同じ設計で、「保存したウィンドウサイズ」欄の下に「保存したウィンドウ
位置: (未保存)」ラベルと「現在のウィンドウ位置を保存」「保存した位置に変更」の2ボタンを
追加した。

- 保存は1件のみ。「現在のウィンドウ位置を保存」で`PlatformAutomation::
  queryWindowBounds`の左上座標を`m_savedWindowPos`に記録し、「保存した位置に変更」で
  `PlatformAutomation::moveWindow(pid, windowId, pos)`（既存の「ウィンドウ操作」種別の
  移動が内部で使っているのと同じ関数）を呼んで復元する。未保存で押すとエラーダイアログ。
- プリセットJSON（`hasSavedWindowPos`/`savedWindowPosX`/`savedWindowPosY`）にも
  保存/復元される。
- 実機（Xvfb + openbox）で、`TestTarget`を`xdotool windowmove`で位置(500,620)へ
  移動した直後に「現在のウィンドウ位置を保存」（アプリ内部では`queryWindowBounds`
  基準で(499,600)として記録される — `xdotool`とアプリ内部の座標取得とで約20pxの
  ズレがあるのは、ウィンドウマネージャの装飾枠（タイトルバー）を含むかどうかの違いで
  あり、既知のフレーム関連の表示差である）→ `xdotool windowmove`で全く別の位置(51,50)へ
  移動 → 「保存した位置に変更」を押すと、`xdotool getwindowgeometry`で確認できる座標が
  移動前の(500,620)へぴったり戻ることを確認した（アプリ内部の座標系・`xdotool`の座標系
  それぞれの中で一貫しており、保存・復元のペアが常に同じ値に往復することを確認済み）。
  -Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。

連続実行を実行する時に、テスト対象ツールのウィンドウ位置やウィンドウサイズを保存した
ものに合わせて変更するオプションをつけてください。

実装状況（連続実行時に保存済みジオメトリを自動適用、v0.74）: 「保存したウィンドウ位置」
欄の下に「連続実行時に保存済みのウィンドウ位置・サイズへ変更する」チェックボックス
（デフォルトOFF）を追加した。

- 有効にすると、対象アプリが（再）起動して検知されるたびに、上のv0.72/v0.73で
  保存済みのウィンドウサイズ・位置があればそれぞれ独立に適用してから実行を始める
  （`MainWindow::applySavedWindowGeometryIfEnabled()`）。サイズのみ保存済みならサイズ
  だけ、位置のみ保存済みなら位置だけを適用し、どちらも未保存の場合は何もしない。
- 適用のタイミングは、6.1節v0.68で「対象ツールの起動検知後、さらに待つ時間」が
  共通化したのと同じ`startRunAfterLaunchWait()`の先頭。そのため、この関数を呼ぶ
  3経路すべて——「⟳ 連続実行」のキル→再起動サイクル、①「連続実行回数」（バッチ実行）
  の自動/手動再起動待ち、「▶ 開始」の「起動してから開始する」オプション——に同じ
  ように適用される（要望の文言は「連続実行を実行する時に」だが、v0.68の起動待ち時間と
  同じ理由で3経路とも対象にした方が一貫性があるため）。
- プリセットJSON（`applySavedGeometryOnRelaunch`）にも保存/復元される。
- 実機（Xvfb + openbox）で、サイズ(1250x950)・位置(300,200)を保存済みにした状態で
  ヘッドレスCLI実行（`--run`、6.14節。`--run`は常に対象を一度キルしてから新規起動する
  ため、v0.68までで検証済みの「⟳ 連続実行」相当の経路を自動的に踏む）を行い、
  新しいインスタンスが検知された直後に「保存したウィンドウサイズに変更しました:
  幅1250 高さ950」「保存したウィンドウ位置に変更しました: x300 y200」のログが
  この順で出力され、その後テストが正常に完走（異常なし、終了コード0）することを
  確認した。-Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。

### 6.2 ステップ構成 (Steps) — 領域ごとに操作種別・回数を指定し、順番に繰り返し実行
テストは1つ以上の「ステップ」の順序付きリスト（`TestConfig::steps`、各要素が`RegionStep`）
として構成する。手順は次のとおり（②ステップ構成の「追加」ボタンは領域選択のみを行い、
それ以外は③操作パラメータパネルで設定する）。

1. ②ステップ構成パネルの「追加」ボタンを押すと`StepEditorDialog`（領域選択のみの
   小さなダイアログ）が開く。「対象GUIの全領域を使用（自動追従）」、または①対象選択
   パネルの「操作領域」リストに登録済みの名前付き操作領域から1つを選ぶ（詳細は6.3節）。
   前者は毎イテレーションで対象ウィンドウの最新bounds（位置・サイズ）を再取得するため、
   ウィンドウの移動/リサイズに追従する。OKを押すと、既定値（左クリック有効・回数50）で
   ステップがリストに追加され、選択状態になる。
2. ②のリストでステップを選択すると、③操作パラメータパネルにそのステップの設定が
   表示される。そこで、有効化する操作種別（クリック/ドラッグ/キー入力/スクロール（上/下）/
   ショートカットキー/ウィンドウ操作、複数可）と発生頻度（重み）・そのステップで実行する
   操作回数、および操作の詳細パラメータ（6.4節）を設定する。
3. ②の「追加」を繰り返して次のステップを作り、③で設定…という流れで全ステップを作成し、
   `MainWindow` のステップ一覧（追加/編集/削除/上へ/下へ/すべて削除）で全体の順序を
   管理する。「編集」ボタンは既存ステップの領域（対象GUIの全領域↔名前付き操作領域）だけを
   変更するためのもので、操作種別・重み・回数・詳細パラメータは変更しない（③で編集する）。

- ステップ構成の中に待ち時間を挿入できるようにしてください。待ち時間をパラメータとして設定できるようにしてください。

実装状況（待機ステップ、v0.34）: `RegionStep`に`isWaitStep`/`waitDurationMs`（デフォルト
1000ms）を追加した。`isWaitStep`が真のステップは領域・操作種別・`ActionParams`のフィールドを
一切使わず、`waitDurationMs`だけ待ってから次のステップへ進む、それ専用のステップとして扱う。
②ステップ構成パネルに「待機を追加...」ボタンを新設し（`MainWindow::onAddWaitStep`）、
`QInputDialog::getInt`で待機時間（ms、1〜600000）を入力するだけでリストに追加できる。
「編集...」ボタンも選択中のステップが待機ステップなら同様の入力ダイアログを開く
（`onEditSelectedStep`が分岐）。一覧では「ステップN: 待機（X ms）」と表示し
（`describeStep`）、③操作パラメータパネルは待機ステップ選択時は全体を無効化する
（操作種別・詳細設定のいずれも待機ステップには存在しないため）。
`RandomActionEngine::performRandomAction`は現在のステップが待機ステップの場合、
対象への操作を一切行わずログに待機時間を出力して次のステップへ進み、通常の
（`minIntervalMs`〜`maxIntervalMs`からランダムに選ぶ）待ち時間の代わりに
`waitDurationMs`そのものをタイマーにセットする。待機は「実行回数」（イテレーション数）には
含めない。この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュ
なしの起動確認のみ）。

`RandomActionEngine` はステップを先頭から順番に実行し、各ステップでは指定回数の
ランダム操作を行ってから次のステップへ進む。最後のステップまで行ったら先頭のステップへ
戻り、これを「シーケンス」として何度も繰り返す（繰り返し回数の上限は6.6節参照）。

固定のスクリーン座標として保存されるため、対象ウィンドウが大きく移動した場合は
再選択が必要（既知の制約。将来的にウィンドウ相対座標へ変更する余地あり）。

操作の最大実行回数や繰り返し回数で無制限は選択できないようにしてください。必ず上限を設定してください。

実装状況（②のラベル更新・実行中表示）: ③で操作種別のチェックボックス・重みスピン
ボックス・操作回数スピンボックスのいずれかを変更すると、そのたびに②のステップ一覧の
該当行のラベル（`MainWindow::describeStep`が返す「ステップN: 領域 | 操作: ... | 回数: ...」
という文字列）が即座に更新される（`ActionKindEditor::changed`シグナルを
`MainWindow::flushActionParamsEditor`に接続し、値を読み取ってステップへ反映した直後に
その行のテキストを再設定する）。値を確定するために他のステップへ移動したり保存操作を
行ったりする必要はない。また、テスト実行中は現在実行中のステップの行の先頭に
「▶ 実行中」というマーカーが付く（`RandomActionEngine::currentStepChanged(int)`
シグナルを開始時・次のステップへ進むたびに発行し、`MainWindow::onCurrentStepChanged`が
旧・新の該当行だけを再描画してマーカーを付け替える。停止時にはマーカーを消す）。

ステップ構成で複数のステップをまとめる機能をつけてください。まとめたステップは決められた
回数ランダムにそれぞれのステップを呼び出せるようにしてください。

実装状況（ステップのグループ化、v0.42）: `RegionStep`に`isGroup`/`groupMembers`
（`QList<RegionStep>`、自己参照だがネストは許可しない運用）/`groupTotalCallCount`
（デフォルト50）/`groupWeight`（グループ自身がさらに別のグループのメンバーになることは
ないため、これはメンバー側でのみ使う）を追加した。②ステップ構成リストで通常ステップ
（待機・グループを除く）を2つ以上選択すると「グループ化」ボタンが有効になり、押すと
選択した各ステップが丸ごと新しいグループステップ1つに置き換わる（一覧上の位置は選択
範囲の先頭、他のステップは詰めて繰り上がる）。グループステップの「編集...」を押すと
`StepGroupEditorDialog`（②③パネルを縮小コピーしたようなマスター・ディテールUI）が
開き、グループ全体の合計呼び出し回数と、メンバーごとの領域・操作種別・重み・操作パラメータ
（`ActionKindEditor`の操作回数行は`setActionCountRowVisible(false)`で非表示にし、
メンバー自身の回数は無意味であることを明示）を編集する。グループを1つ選択すると
「グループ解除」ボタンが有効になり、押すとメンバーステップがその場に展開されて元の
（グループ化前の）並びに戻る。

実行時の意味（`RandomActionEngine::performGroupAction`）: グループの1回の「呼び出し」は
「メンバーの中から`groupWeight`に応じて1つだけランダムに選び、そのメンバーの操作を
1回だけ実行する」ことであり、次の呼び出しでは改めてランダムに選び直す（同じメンバーが
何度も連続で選ばれることもある）。これを`groupTotalCallCount`回繰り返した時点で
（メンバー自身の`actionCount`は一切参照しない）次のステップへ進む。通常ステップと
グループステップの実行ロジックの大部分（領域解決・安全確認・操作種別選択・実際の
操作実行のswitch文）は共通のため、`runOneAction()`＋`ActionOutcome`列挙型として
くくり出し、通常ステップ用の`performRandomAction()`とグループのメンバー用の
`performGroupAction()`の両方から呼び出す形にリファクタリングした。

名前付き操作領域の削除保護・リネーム時の参照追従（`stepsReferencing`/
`renameRegionReferences`）と、実行開始前の設定検証（`buildConfigFromUi`内の
`validateStepActionConfig`）は、いずれもグループのメンバーステップまで再帰的に
チェックするように対応した。JSONプリセットの保存/読み込み（`TestConfigJson`）も
`isGroup`/`groupMembers`/`groupTotalCallCount`/`groupWeight`をそのまま
シリアライズ/デシリアライズする形で対応済み。

実機確認（Linux, Xvfb + openbox）: 3つの通常ステップを追加し2つを複数選択して
グループ化→②に「ステップ1: グループ（2個のステップ、合計呼び出しN回）」と表示される
ことを確認。編集ダイアログでメンバーごとの重み変更が一覧表示に即座に反映されること、
グループの合計呼び出し回数を5、直後の通常ステップの回数を50、全体の最大実行回数を12に
設定して実行したところ、ログ上「クリック」操作がちょうど5回続いた直後に「ステップ 2 へ
移行します」が出力され、その後合計12回に達して停止することを確認（グループが自分の
合計呼び出し回数ぶんだけ実行してから正しく次のステップへ進むことの検証）。グループ解除で
メンバーが独立した通常ステップとして復元されることも確認。グループを含む設定をJSON
プリセットとして保存し、②をすべて削除した状態から読み込み直したところ、合計呼び出し回数・
メンバーごとの重みを含め全設定が完全に復元されることを確認。
なお`StepGroupEditorDialog`は当初、操作パラメータ詳細（`ActionParamsEditor`）を
スクロール領域に入れずに配置しており、1080px高の画面に収まらずOK/Cancelボタンを含む
下半分が画面外に出て操作不能になる不具合があった（他の同種ダイアログ
`DefaultActionParamsDialog`と同じ`QScrollArea`によるラップで解消。実機確認で発見・修正）。
ビルドは`-Wall -Wextra -Wpedantic`付きで警告0件を維持している。

再検証（実機確認）: 実装後、改めてビルドし直した上でXvfb + openbox環境で一通り
再実行し、以下を確認した。
- グループ化/グループ解除/②一覧のラベル表示（メンバー数・合計呼び出し回数）は
  クリーンビルド後も同様に動作する。
- クリック/ダブルクリックという判別しやすい操作種別を2メンバーにそれぞれ割り当てて
  実行し、③下部の「操作種別ごとの回数」サマリーで実際の呼び出し内訳を検証した。
  重み1:3（クリック側1、ダブルクリック側3）・合計呼び出し200回の設定で実行したところ、
  クリック50回・ダブルクリック150回とちょうど理論値（25%/75%）どおりの結果になった。
  重み1:1で同様に実行したところ、1回目はクリック78回・ダブルクリック122回と理論値
  （100/100）からやや外れた結果になったが、これは以下の統計的検証によりランダムな
  ばらつきの範囲内であることを確認した（実装のバグではない）。
- `pickWeightedGroupMemberIndex`と同じ重み付き抽選ロジックを切り出した検証用
  スタンドアロンプログラムで、重み1:3・1:1のそれぞれについて20万回抽選した結果は
  1:3→24.81%/75.19%、1:1→49.88%/50.12%と、いずれも理論値にほぼ一致した
  （アルゴリズム自体に偏りがないことの確認）。さらに「1:1の重みで1回のテスト実行
  （合計呼び出し回数200）を模した試行」を2000回繰り返しシミュレーションしたところ、
  片方のメンバーが選ばれる回数は78〜125回の範囲でばらつくことを確認した——すなわち
  実機での「78回」という観測値は、200回という比較的小さい試行数では普通に起こりうる
  統計的なばらつき（二項分布の分散）であり、重み付き抽選の実装が偏っていることを示す
  ものではない。合計呼び出し回数を大きくするほど実際の比率は指定した重みの比率に
  収束する（大数の法則）。

ステップ構成にタスクを登録できるようにしてください。タスクはいくつかの操作フローを一つに
まとめた操作で、タスク全体を一つの操作としてください。（タスク内で操作はランダムに前後
などはしない）

実装状況（ステップ構成への「タスク」追加、v0.61）: グループ（メンバーからランダムに
1つだけ選んで実行する）とは対照的に、登録した順番で全メンバーを1回ずつ必ず実行し、
その一巡全体で「実行回数」1回分としてカウントする専用のコンテナとして実装した。
`RegionStep`に`isTask`/`taskMembers`（`QList<RegionStep>`、自己参照だがグループ同様
ネスト・待機ステップの混在は許可しない運用）を追加した。②ステップ構成リストで通常
ステップを2つ以上選択すると「タスク化」ボタンが有効になり、押すと選択した各ステップが
丸ごと新しいタスクステップ1つに置き換わる（グループ化と同じ位置・詰め方の挙動。
待機ステップ・グループ・タスク自体はタスク化の対象外で、選んだ場合は警告ダイアログで
拒否する）。タスクステップの「編集...」を押すと`TaskEditorDialog`（`StepGroupEditorDialog`
からグループ固有の合計呼び出し回数・重みスピンボックスを除いたマスター・ディテールUI）が
開き、メンバーの領域・操作種別・操作パラメータと並び順（上へ/下へ）を編集する
（メンバーの実行回数・重みは意味を持たないため非表示）。タスクを1つ選択すると
「タスク解除」ボタンが有効になり、押すとメンバーステップがその場に展開されて元の並びに
戻る（グループ解除と同じ挙動）。

実行時の意味（`RandomActionEngine::performTaskAction`）: タスクの実行は
`m_currentTaskMemberIndex`（`start()`・`advanceToNextStep()`の両方で0にリセット）で
現在何番目のメンバーの番かを管理し、1回の「呼び出し」で`taskMembers[m_currentTaskMemberIndex]`
の操作を1回だけ実行してからインデックスを1つ進める、という形で1メンバーずつ確実に
順番通り進む（ランダムな選択は行わない）。全メンバーの実行が一巡し終えた時点で初めて
`m_iterationCount`を1つ増やし（メンバーごとには増やさない）、次のステップへ進む。
選んだメンバーが一時的に実行不可（`ActionOutcome::SkippedNoCount`）だった場合は
インデックスを進めずに次のtickで同じメンバーを再試行し、タスクの固定順序が崩れない
ようにしている。通常ステップ・グループステップと共通の`runOneAction()`をそのまま
再利用しており、領域解決・安全確認・操作実行のロジックに変更はない。

名前付き操作領域の削除保護・リネーム時の参照追従、実行開始前の設定検証
（`validateStepActionConfig`）、JSONプリセットの保存/読み込み（`isTask`/`taskMembers`の
再帰的なシリアライズ/デシリアライズ）は、いずれもグループと同様にタスクのメンバー
ステップまで対応済み。

実機確認（Linux, Xvfb + openbox、日本語・英語UIの両方）: クリック操作の2つの通常
ステップを選択してタスク化し、②に「ステップ1: タスク（2個の操作を順番に実行）」と
表示されることを確認。実行ログでは同一シーケンス内で2つのメンバーのクリックが必ず
この順番で連続し、2つ実行し終えるごとに「シーケンス N 回目の実行を開始します」（この
タスクが唯一のステップである設定の場合）が出力され、実行回数がメンバー2個で1回分
としてカウントされることを確認（グループのような偏りのある抽選ではなく、常に固定
順序であることの検証）。タスク解除でメンバーが独立した通常ステップとして復元される
ことも確認。`TaskEditorDialog`は日本語・英語いずれのUIでもタイトル・説明文・メンバー
一覧・詳細編集パネルのラベルが正しく翻訳されることを確認。ビルドは
`-Wall -Wextra -Wpedantic`付きで警告0件を維持している。

タスクの実装に伴って、テスト対象のアプリの操作中に出現したダイアログの操作をできるように
してください。例えば、右クリックでメニューを選択した後ダイアログが出てきたときに、その
ダイアログも操作対象として操作します。ここは操作領域の設定と矛盾する可能性があるので
必要があれば仕様を提案してください。

実装状況（タスク内での新規ダイアログ操作、v0.62）: 上の要望を受け、タスクのメンバー
専用のオプションとして実装した（グループ・通常ステップには追加していない -- 後述の
「なぜタスク限定か」を参照）。`RegionStep`に`targetsPopupDialog`を追加し、真の場合は
そのメンバー自身の`useWholeWindow`/`regionName`（除外領域を含む）を一切使わず、
「対象プロセスが現在開いているウィンドウのうち、対象アプリのメインウィンドウ
（`targetWindowId`）以外の1つ」を操作領域（そのウィンドウの現在の全体bounds）として
実行時に解決する（`RandomActionEngine::resolveStepRegion`）。`TaskEditorDialog`の
「追加...」「編集...」から開く領域選択ダイアログ（`StepEditorDialog`）に、タスクの
メンバーを編集している場合に限り3つ目の選択肢「新しく出現したウィンドウ（ダイアログ等）
を対象にする（自動検出）」を追加した（`allowPopupDialogTarget`引数、既定値false -- 通常
ステップ・グループのメンバー編集では従来通り2択のまま）。

**操作領域の設定との整合性（提案されていた懸念点への回答）**: 矛盾は生じない設計にした。
このオプションを有効にしたメンバーは、対象アプリの「メインウィンドウ以外の新しい
ウィンドウ」という、既存の操作領域（名前付き領域・対象GUI全領域）とは別次元の第三の
領域源を明示的に選んだことになるため、既存の操作領域の定義や解決ロジックには一切手を
入れていない。

**なぜタスク限定か**: 「直前の操作がこのダイアログを開いたはず」という前提が意味を持つのは、
メンバーの実行順序が固定されているタスクだけである。グループはメンバーを毎回重み付き
ランダムに選び直すため「直前の操作」という概念自体が無く、通常ステップにも「このステップの
直前」という関係はない。よってこのオプションはタスクのメンバーにのみ意味があり、UIも
それ以外では表示されないようにした。

**既存の「予期しないウィンドウ自動解消」との関係**: 6.7節の`handleUnexpectedWindows()`は
毎ティック、対象アプリのメインウィンドウ以外のウィンドウを検出すると安全のため
Escapeで閉じにかかる（クラッシュ検知等の安全機構）。これは今回の機能と一見競合するが、
`RandomActionEngine::currentActionTargetsPopupDialog()`で「今まさに実行しようとしている
アクションが`targetsPopupDialog`なメンバーかどうか」を判定し、そうである時だけその
ティックに限って`handleUnexpectedWindows()`をスキップするようにした。タスクが
`targetsPopupDialog`のメンバーを離れた後（次のシーケンスで先頭の通常メンバーへ戻った後等）
にまだ余分なウィンドウが残っていれば、既存の自動解消がそのまま働いて後始末する（実機
確認で実際にこの経路が働くことを確認済み、後述）。

**ダイアログがまだ現れていない場合**: 直前の操作がまだダイアログを開き終えていない
可能性があるため、対象ウィンドウ以外のウィンドウが見つからない場合はエラーにせず、
同じメンバーを次のティックで再試行する（`m_popupDialogWaitStrikes`、上限
`kMaxPopupDialogWaitStrikes`＝50回)。上限に達しても現れない場合は
`handleUnexpectedWindows()`の「閉じられない場合」と対称的に、対象アプリのバグの
可能性がある事象として異常停止として報告する（`ステップN（タスク内操作M/M）が対象と
する新しいウィンドウ（ダイアログ等）が現れないため、安全のためテストを停止しました`）。

**ウィンドウ操作種別との関係**: 「ウィンドウ操作」（移動/リサイズ/最小化/最大化）は
常に対象アプリのメインウィンドウ（`targetWindowId`）を操作する種別であり、
`targetsPopupDialog`なメンバーの領域（新しく出現した別ウィンドウ）とは無関係のため、
このオプションが有効なメンバーでは操作種別の抽選対象から自動的に除外する
（`RandomActionEngine::pickWeightedActionKind`/`RegionStep::hasAnyActionEnabled`）。

実機確認（Linux, Xvfb + openbox）: `TestTarget`の「終了（クリック厳禁）」ボタン
（クリックすると`QMessageBox::warning`の終了確認ダイアログ、既定ボタンは「いいえ」を
開く）を使い、①メンバー1（名前付き操作領域でこのボタンをクリック）→②メンバー2
（`targetsPopupDialog`）の2メンバーからなるタスクで検証した。メンバー1のクリックで
実際に終了確認ダイアログが開き、メンバー2がそのダイアログのウィンドウ全体を操作領域
として正しく解決してクリックすることを確認（ログ上、メンバー2実行中は「予期しない
ウィンドウ」警告が出ないことも確認 -- 抑制が機能している証拠）。1パス分のクリックで
ダイアログが閉じきらなかった場合、次のシーケンスでメンバー1に戻った直後に既存の
`handleUnexpectedWindows()`が「予期しないウィンドウ」警告を出してEscapeで自動的に
閉じ、その後メンバー1のクリックが正常に成立することも確認した（既存の安全機構との
共存が設計どおり機能している）。合計4回（メンバー2個で1回換算）実行して回数制限で
正常停止し、`TestTarget`自体はクラッシュせず起動し続けたままであることを確認。
ビルドは`-Wall -Wextra -Wpedantic`付きで警告0件を維持している。

ステップ構成のタスク内では操作途中で出てくるダイアログも操作できるようにしてください。特に
出てきたダイアログのボタンを押したり特定の操作をできるようにしたいです。

実装状況（タスク内ダイアログの「ボタンを押す」操作、v0.70）: 上のv0.62で追加した
`targetsPopupDialog`（新しく出現したダイアログ等を操作領域として自動解決する仕組み）を
土台に、そのダイアログに対して「名前を指定してボタンを押す」という具体的な操作を追加した。
既存の右クリックメニュー項目選択（`enableContextMenuSelection`/`contextMenuItemNames`、
6.9節）と全く同じ設計 -- 「候補となるボタン名を複数登録しておき、実際にその時点で存在する
ものをOSのアクセシビリティAPIから探して押す」という方針を踏襲した。

- `RegionStep`に`enableDialogButtonPress`/`dialogButtonPressWeight`を追加し、
  `targetsPopupDialog`が真のタスクメンバーでのみ意味を持つ操作種別として
  `ActionKindEditor`に「ダイアログのボタンを押す」チェックボックス・重みを追加した
  （`hasAnyActionEnabled()`もこの組み合わせのみ有効と判定するよう変更）。
- `ActionParams`に`dialogButtonNames`（候補ボタン名のリスト）を追加し、
  `ActionParamsEditor`にコンテキストメニュー項目名リストと同じUI（一覧・追加・削除）を
  新設した。
- 新しい`ActionKind::DialogButtonPress`を`RandomActionEngine`に追加。実行時は
  `dialogButtonNames`をランダムな順序で1件ずつ試し、`PlatformAutomation::clickButtonByName`
  （新設、macOS Accessibility API / Linux AT-SPIの両バックエンドに実装 -- 既存の
  `clickContextMenuItem`系と同じ「ベストエフォート・未対応環境では失敗として扱う」設計）
  が最初に成功した名前を採用する。候補が1つも見つからない場合はクラッシュ扱いにはせず、
  その回をスキップして次のティックで同じメンバーを再試行する（`SkippedNoCount`、タスクの
  固定順序を保つ既存の挙動どおり）。安全確認（対象アプリが最前面かどうか）も既存の
  他操作種別と同じタイミング・同じ停止条件で行う。
- `TestConfigJson`のシリアライズ/デシリアライズ、`MainWindow`/`StepGroupEditorDialog`/
  `TaskEditorDialog`のステップ内容表示（`describeStep`相当）にも対応した。

実機確認（Linux, Xvfb + openbox）: この環境は`DBUS_SESSION_BUS_ADDRESS`が未設定のため
AT-SPIが常に無効（`atspiUsable()`が偽を返す）であり、v0.59でも同じ理由により確認できな
かった「アクセシビリティAPIでの検索・クリックが実際に成功する経路」はこの環境では検証
できない（8章「既知の制約」参照）。そのため、それ以外のパイプライン全体
（ウィンドウ位置追従込みの操作領域解決 → ダイアログ出現待ち → タスクメンバーとしての
`DialogButtonPress`発火 → 候補名を1件ずつ試す → 見つからずスキップして次ティックへ
安全に再試行）を、手作業で組み立てたプリセットJSONをCLIヘッドレス実行
（`--run`/`--repeat`、6.14節）に通して確認した。`TestTarget`の終了確認ダイアログを
実際に開かせ、`dialogButtonNames=["No"]`を設定したメンバーが対象ウィンドウ相対座標
（追従補正込み）でダイアログ領域を正しく解決し、AT-SPI無効環境での「見つからず」の
フェイルソフト経路を繰り返し安全に踏むこと、`maxDurationSec`の時間制限で正常停止し
サマリーが正しく出力されることを確認した。`TestTarget`自体はクラッシュせず動作し
続けた。ビルドは`-Wall -Wextra -Wpedantic`付きのクリーンビルドで警告0件を維持している。

### 6.3 操作領域（名前付き）と除外領域指定
対象アプリの操作領域を矩形領域と矩形領域内での矩形マスク領域(複数指定可能)を用いて指定する。操作領域には自動でラベルをつける。指定された操作領域内での操作をランダムに行う。
設定された操作領域をリストとして表示する。リスト内の操作領域を選択して、除外領域の追加、編集、削除を行う。除外領域は操作領域内での矩形マスク領域として指定する。除外領域は複数指定可能であり、指定された除外領域内では操作を行わない。また選択した操作領域を削除することができる。

実装状況: 操作領域は`TestConfig::namedRegions`（`NamedRegion`、`name`+`regions`+
`excludeRegions`）として①対象選択パネルの「操作領域」グループで一元管理する
プール（追加/編集/削除。`NamedRegionEditorDialog`で名前・矩形（複数可）・除外
（マスク）矩形（複数可）を設定）で、②ステップ構成の各ステップはこのプールから
名前で1つを参照する（`RegionStep::useWholeWindow`が偽の場合は`regionName`で指定）。
「操作領域には自動でラベルをつける」という当初の仕様に合わせ、「追加...」を押した時点で
`MainWindow::generateDefaultRegionName`が既存の操作領域と重複しない「操作領域1」
「操作領域2」…という名前を自動生成し、`NamedRegionEditorDialog`の名前欄にあらかじめ
入力した状態で開く（`MainWindow::onAddNamedRegion`）。ユーザーは何も入力せずにOKを
押すだけで登録でき、必要であれば自動生成された名前をそのまま編集して分かりやすい名前
（例:「メインメニュー」）に変更することもできる。`NamedRegionEditorDialog`内で描画した
個々の矩形は、この操作領域自体の名前とは別の採番（「矩形1」「矩形2」…、除外矩形は
「除外1」「除外2」…）でリスト表示する。当初は個々の矩形にも「領域N」というラベルを
使っていたため、操作領域自体の自動生成名（当時は「領域N」）と紛らわしかった
（v0.31で「操作領域N」/「矩形N」に採番を分けて解消。詳細はv0.31の変更履歴を参照）。
同じ操作領域を複数のステップから参照できる。除外領域は操作領域ごとに保持し、
`RandomActionEngine`はステップの参照先の操作領域が持つ`excludeRegions`のみを見て
除外判定を行う。「対象GUIの全領域」（`useWholeWindow`）を選んだステップには除外領域を
設定できない（除外を使いたい場合は固定矩形の操作領域を登録してそれを参照する）。
操作領域の削除は、それを参照しているステップが1つでもあれば拒否し、該当ステップ番号を
表示する（先にステップ側の参照を変更するか、ステップ自体を削除する必要がある）。
操作領域の名前を編集で変更した場合は、参照している全ステップの`regionName`を自動的に
新しい名前へ追従させる。

「操作領域」リストの項目を選択しただけでは画面上に何も表示しない。その代わり、
「追加...」「編集...」で`NamedRegionEditorDialog`を開いている間、編集中の操作領域の
矩形（含める領域・除外領域）を画面上に半透明でハイライト表示し続ける
（`NamedRegionEditorDialog::updateHighlight`が矩形の追加/削除のたびに呼ばれ、
`RegionHighlightOverlay`を更新する。ダイアログが閉じる際（保存・キャンセルいずれも
`QDialog::finished`シグナルで検出）にハイライトも消える）。含める領域は緑、除外領域は
赤の枠・塗りで、`RegionSelectorOverlay`（描画用オーバーレイ）と同じ配色を使う。
「矩形を描画...」「除外矩形を描画...」で`RegionSelectorOverlay`（描画用の別の
オーバーレイ、こちらは既存矩形を描画しつつ新しい矩形をマウスで選択させる対話的な
オーバーレイ）を開く間は、2つの常に最前面のオーバーレイが視覚的にぶつからないよう、
一旦ハイライトを隠し、描画セッションが終わったら再表示する。

（v0.37でこの方式に変更。以前は①の「操作領域」リストで項目を選択するたびに
`MainWindow`がハイライトを表示していたが、選択のたびに複数モニタ分のOSレベルの
ウィンドウを作り直す実装になっていた時期があり、「操作領域を選択すると固まる」不具合の
原因になっていた（v0.33で導入、v0.36で「作り直す」を「使い回す」に変更して改善は
したものの、そもそも一覧の選択ごとに毎回オーバーレイを出し入れする設計自体が
不具合を生みやすかったため、編集中だけ表示する今の方式に変更した。詳細は変更履歴
v0.30/v0.33/v0.36/v0.37を参照）。

解像度・スケールが異なる複数モニタをまたいでも正しく表示できるよう、仮想デスクトップ
全体を覆う単一のウィンドウではなく、**`QScreen`（モニタ）ごとに1つ、そのモニタの
`geometry()`に一致するウィンドウ**を作る設計になっている（v0.33）。モニタごとの
ウィンドウは`NamedRegionEditorDialog`が開いている間使い回し、`hide()`も破棄せず
非表示にするだけにしている（v0.36。モニタの台数が変わった時だけ作り直す）。この
オーバーレイはマウス入力を一切受け取らない（`Qt::WA_TransparentForMouseEvents`）ため、
他の操作を妨げない。

実装状況（「対象ウィンドウの移動に追従させる」オプションの不具合修正・デフォルト
有効化、v0.57）: 「操作領域で追加ボタンあるいは編集ボタンを押したときに出てくる
操作領域をアプリの移動に合わせて追従するオプションが効いてません」という報告を受け、
実機で原因を再現・特定して修正した。

- **【発見・修正】既存の追従設定を編集し直すと追従が壊れる不具合**:
  `NamedRegionEditorDialog::result()`は、「対象ウィンドウの移動に追従させる」が
  オンのとき、矩形を1つも描き直さずに（例えば名前を変えただけで）OKを押した
  場合でも、`anchorTopLeft`（追従の基準点）を常にダイアログを開いた時点の
  対象ウィンドウの現在位置へ無条件に上書きしていた。矩形自体（`regions`）は
  古い基準点のまま変わらないため、基準点だけが新しい位置にずれてしまい、
  実行時に`RandomActionEngine::resolveStepRegion`が誤った平行移動量で矩形を
  ずらす結果になっていた。ずれ幅が大きい場合はクリック先が対象アプリの
  ウィンドウの外に出てしまい、「対象アプリ以外のウィンドウを操作しそうに
  なったため停止」という安全装置が働いてテスト自体が異常停止することもある
  （このクラスの不具合は`NamedRegionEditorDialog.h`のコメントに「元の基準点は
  維持されるはず」という設計意図が既に書かれていたが、実装がそれに従っていない
  という形で存在していた）。矩形が実際にこのセッション中に描画/削除された
  かどうかを`m_regionsChanged`で追跡し、追従が既に有効だった操作領域を矩形の
  変更なしに保存し直す場合は既存の基準点をそのまま維持するよう修正した
  （矩形を新たに描画した場合や、このセッションで追従を新たに有効にした場合は
  従来どおり現在の対象ウィンドウ位置を新しい基準点にする）。
- **【変更】新規作成時のデフォルトを有効に変更**: `NamedRegion::
  followsTargetWindow`のデフォルト値を`false`から`true`に変更した。これは
  「追加...」で新規作成する操作領域（`MainWindow::onAddNamedRegion`が
  デフォルト構築する`NamedRegion`）にのみ影響し、`NamedRegionEditorDialog`の
  チェックボックスが初期状態でオンになる。保存済みプリセットの読込は
  `TestConfigJson::namedRegionFromJson`がJSON中の値を明示的なfalseフォール
  バック付きで読むため、この既定値変更の影響を受けない（古いプリセットの
  挙動は変わらない）。
- 実機（Xvfb + openbox）で、対象ウィンドウのボタンを覆う追従有効な操作領域を
  作成し、(a) 対象ウィンドウを実行中に移動させてもクリックが正しくボタンに
  命中し続けること、(b) 一旦ウィンドウを大きく移動させてから当該操作領域を
  矩形を描き直さずに「編集...」→「OK」で保存し直しても（修正前はここで
  安全装置が働いて異常停止していたのを確認済み）、その後の実行でクリックが
  引き続き正しくボタンに命中することを確認した。-Wall -Wextra -Wpedantic付きの
  クリーンビルドで警告0件を維持。

少なくともLinux環境では、操作領域の追加で矩形を描画を押すと左上に含める領域を選択中と出て
選択できるようになるのですが、それで指定した位置と実画面での位置が少しズレています。
おそらく矩形選択の画面と実画面がズレているためダイアログが二つ出ているように見えているの
かと思います(下のissueと関係している)。矩形を描画するモードで選択した位置と実際に操作
する操作領域がずれないように修正してください。

実装状況（矩形描画オーバーレイのHiDPI座標ズレ、v0.63）: `RegionSelectorOverlay`/
`PointPickerOverlay`が背景として使う仮想デスクトップ全体の合成スクリーンショット
（`OverlayGeometry::grabVirtualDesktopSnapshot()`）に、コードレビューで実際のバグを
発見した。合成先の`QPixmap`をデフォルトのdevicePixelRatio（1.0）のまま作成していたため、
HiDPI（拡大率>1）なLinux/macOS環境では、この合成画像をオーバーレイ描画時に
`QPainter::drawPixmap(0, 0, ...)`でウィジェットへ貼り付ける際、ウィジェット自身の
論理座標系（マウスイベントの`globalPosition()`が返す座標系と同じ）に対して画像が
実際の拡大率倍だけ大きく描画されてしまう不具合があった。結果として、画面上に見えている
背景画像の位置と、実際にドラッグで記録される矩形の座標（＝最終的に操作領域として使われる
座標）が徐々にズレていく（拡大率が大きいほど、また画面端に近いほどズレが大きくなる）ため、
「矩形を描画するモードで選択した位置」と「実際に操作する操作領域」が一致しなくなる。
修正として、合成先`QPixmap`のサイズをプライマリスクリーンのdevicePixelRatio倍のピクセル
数で確保し、`setDevicePixelRatio()`で明示的にその比率を設定するようにした
（`OverlayGeometry::grabVirtualDesktopSnapshot()`）。各スクリーンの`grabWindow(0)`は
元々正しい比率を自己記述したピクスマップを返すため、合成先の比率さえ揃えれば
`drawPixmap`はそれぞれ正しく縮尺・配置される。この関数は`RegionSelectorOverlay`と
`PointPickerOverlay`の両方が共有しているため、1箇所の修正で両方に適用される
（`RegionHighlightOverlay`は元々1スクリーン単位で`grabWindow(0)`の戻り値をそのまま
使っており、この問題の対象外）。

実機確認（Linux, Xvfb + openbox。この環境のXサーバはdevicePixelRatio常に1.0のため、
今回のHiDPI起因のズレそのものは再現できなかった）: `TestTarget`のボタン1の実際の画面
上の位置をピクセル単位で特定した上で、その位置ちょうどに「矩形を描画...」でドラッグして
矩形を確定し、記録された座標（例:「矩形1: (80, 95) 326 x 46」）がドラッグした座標と
完全に一致すること、続けて表示される`RegionHighlightOverlay`のハイライト枠がボタン1の
実際の輪郭とぴったり重なることを、修正前後で比較して確認した（修正前後で結果は同一 --
dpr=1環境では元々ズレが無いため、退行が無いことの確認に留まる）。HiDPI環境での実際の
改善効果そのものは実機（該当環境）での確認ができていない。ユーザー報告にあった
「ダイアログが2つ出る」不具合（#51、下記参照）との直接の因果関係も未確認 -- 引き続き
再現手順の提供をお待ちしています。-Wall -Wextra -Wpedantic付きのクリーンビルドで
警告0件を維持。

### 6.4 操作パラメータ（③パネル。操作種別の選択と詳細設定の2分類）

③操作パラメータパネルは、②ステップ構成パネルで選択中のステップに連動するマスター・
ディテールUIで、**分類が異なる2つの設定**を上下に並べて編集する。この2分類の違いを
理解することが本パネルを使う上で重要なので、まずその対比を示す。

|                | 6.4.1 操作種別の選択 | 6.4.2 操作の詳細設定 |
|----------------|----------------------|----------------------|
| 保持する構造体 | `RegionStep`本体のenable*/weight*/actionCount | `ActionParams`（`RegionStep::customActionParams`または`TestConfig::defaultActionParams`） |
| 内容           | どの操作種別を有効にするか・発生頻度（重み）・回数 | 有効にした操作種別が具体的にどう動くか（距離、範囲、文字種、候補リストなど） |
| 「デフォルト」の意味 | **一度限りのひな型**（②で新規ステップを追加した瞬間にコピーされるだけ。既存ステップには影響しない） | **ライブな共有値**（`useDefaultActionParams`が真の間、すべての対象ステップに常に反映される） |
| ②で未選択のときの③の表示 | グループ全体が無効化（編集不可） | `defaultActionParams`を直接編集できる |
| 編集ウィジェット | `ActionKindEditor` | `ActionParamsEditor` |
| 個別トグル | 存在しない（切替の概念自体がない） | ステップごとに「デフォルトを使う」/「このステップ専用の設定を使う」ラジオボタンで切替 |

#### 6.4.1 操作種別の選択（`RegionStep::enableClick`等/`clickWeight`等/`actionCount`）

そのステップで**どの操作種別を発生させるか**・**互いの発生頻度（重み。数字が大きいほど
多く発生）**・**そのステップで実行する操作の総回数**を決める。対象となる9種別は6.5節で
1つずつ説明する（クリック/ダブルクリック/ドラッグ/キー入力/スクロール(上/下/横)/
ショートカットキー/ウィンドウ操作）。クリックのみ「左」「右」の内訳サブチェックボックスを
持つ（両方有効なら発生ごとにランダムに選択）。

この分類はステップごとに必ず具体的な値を持つ（`RegionStep`のメンバとして常に存在する）ため、
`ActionParams`のような「デフォルトを使う/このステップ専用」という切替概念そのものが存在
しない。そのため②で1つもステップが選択されていない間、③の「このステップの操作種別・
重み・回数」グループ全体は無効化（グレーアウト）される — 編集対象となる具体的なステップが
存在しないため。

一方、右上の「デフォルト」ボタン（`DefaultActionParamsDialog`）には、この操作種別選択の
**ひな型**（`MainWindow::m_defaultActionKinds`、UIとしては同じ`ActionKindEditor`を再利用）
を編集する欄もある。ここでの設定は**②で新しくステップを追加した瞬間の初期値としてのみ
使われる一度限りのコピー元**であり、既存のステップの値を書き換えたり、以後もその値を
参照し続けたりするような「ライブな」関係ではない（6.4.2の`defaultActionParams`とは
この点で意味が異なるので注意）。

#### 6.4.2 操作の詳細設定（`ActionParams`）

6.4.1で有効にした各操作種別が**具体的にどう動くか**（距離、範囲、使用文字、候補リストなど）
を決める。こちらは`TestConfig::defaultActionParams`（ライブな共有デフォルト、①対象選択
パネルおよび「デフォルト」ダイアログの設定に相当）と、各ステップの
`RegionStep::customActionParams`（そのステップ専用の値）の2種類があり、ステップごとに
`useDefaultActionParams`でどちらを使うか一括で切り替える（操作種別ごとの個別切替はできない）。

- ②でステップが選択されていない場合: `defaultActionParams`（デフォルト値）を直接編集する。
- ②でステップが選択されている場合: そのステップの「デフォルトを使う」/「このステップ専用の
  設定を使う」をラジオボタンで切替でき、後者を選ぶとそのステップの`customActionParams`を
  編集する（前者を選んだ状態で編集した場合は`defaultActionParams`を編集することになり、
  他の「デフォルトを使う」ステップにも影響する — ここが6.4.1のひな型との最大の違い）。
- ステップの選択を切り替えると、直前まで編集していた内容は自動的に対応する場所
  （デフォルト、またはそのステップの`customActionParams`）へ保存される
  （`MainWindow::flushActionParamsEditor`）。

具体的に指定できる項目は以下（`ActionParamsEditor`ウィジェットで編集し、デフォルト・
カスタムいずれでも同じUIを使い回す）。

- **左クリック**: ボタンやメニュー項目をクリックするためのパラメータ（位置、ボタンの種類など）。
- **右クリック時のメニュー項目選択（実験的機能）**: 有効化した場合、`ContextMenuSelectionMode`
  で選び方を「項目名で指定」「上から何番目かで指定」のいずれかに切り替えられる。
  - 項目名で指定: 候補項目名のリストを保持し、右クリックで開いたメニューに候補のいずれかが
    実際に存在すればランダムに1つ選んで実行する。メニューの項目数・並びが状況によって
    変わっても、同じ意味の項目を選び続けられるため安全性が高い。
  - 上から何番目かで指定: 候補の番号（1始まり、複数指定可）を保持し、開いたメニューの
    項目数に収まる候補の中からランダムに1つ選んで実行する。項目名を調べる必要がなく
    手軽だが、メニューの項目数・並びが状況によって変わる場合は意図しない項目
    （最悪の場合「削除」「終了」等）を選んでしまう可能性があるため、通常は項目名指定を
    推奨する。
  - いずれのモードでも、候補が1つも該当しなければEscapeでメニューを閉じる（詳細は6.5節）。
  - **この設定を無効化していても**、右クリックでメニューが開いた場合はEscapeで即座に
    閉じる（v0.27で修正。詳細は6.5節・8章「既知の制約」参照）。
- **ドラッグ**: 距離レンジ（最小/最大px）と、方向（ランダム／上／下／左／右のいずれか
  1つを`DragDirectionMode`で指定）。
- **キー入力**: 使用文字集合（デフォルト: 英数字小文字）に加え、名前付きキー
  （Tab/Return/Escape/Backspace/Delete/矢印キー）を候補に含められる。詳細は6.10節。
- **ホイール操作(上)・ホイール操作(下)・ホイール操作(横)**: スクロールの量のレンジ
  （最小/最大）を上/下/横それぞれ個別に持つ（有効化・発生頻度＝重みは6.4.1側で
  `RegionStep::enableScrollUp/scrollUpWeight`,`enableScrollDown/scrollDownWeight`,
  `enableScrollHorizontal/scrollHorizontalWeight`として個別指定。例: 下方向スクロール
  だけ多めに発生させる、といった調整が可能）。横方向は発生のたびに左右どちらかをランダム
  に選ぶ。ダブルクリックと同様、以前は上下の量を確率的に流用する
  `ActionParams::scrollHorizontal`フラグだったが、v0.21で独立した操作種別（6.4.1側）に
  昇格した（6.5節）。
- **ショートカットキー**: `QKeySequence`形式の文字列のリスト（例 `"Ctrl+C"`,
  `"Ctrl+Shift+Z"`）。UIから追加・削除可能。
- **ウィンドウ操作**: 移動/リサイズ/最小化/最大化のうち許可する種類を選択する。詳細は6.10節。

#### 6.4.3 「デフォルト」ボタン・ダイアログ

③右上の「デフォルト」ボタンを押すと`DefaultActionParamsDialog`が開き、②のステップ選択を
変えずに6.4.1のひな型（`m_defaultActionKinds`）と6.4.2のライブな共有デフォルト
（`m_defaultActionParams`）を1つの画面でまとめて編集できる。ダイアログ内では「デフォルトの
操作種別・重み・回数」（6.4.1のひな型。既存ステップへの影響なし、と明記したラベル付き）と
「デフォルトの操作の詳細設定」（6.4.2のライブな共有デフォルト。「デフォルトを使う」ステップ
全てに影響する、と明記したラベル付き）が別グループとして並ぶ。OKを押すと両方を更新し、
③パネルが現在デフォルト値を表示中であればその場に反映する。②で何も選択していない状態でも
③パネルは直接`defaultActionParams`を編集できるが（6.4.2の従来経路）、「デフォルト」ボタンは
現在のステップ選択を維持したまま手早く両方のデフォルト値を確認・編集したい場合のショートカット。

### 6.5 操作種別 (Actions) — ステップごとに複数有効化可能
- **ランダムクリック**: 左クリック/右クリックを個別に有効・無効化可能（ステップ内で
  両方有効なら発生ごとにランダムに選択）。右クリックでメニュー項目選択機能が有効な場合、
  クリック直後にメニューの探索・選択（または閉じる）を行う。
- **ダブルクリック**: 「クリック」とは別の、独立した操作種別（`RegionStep::
  enableDoubleClick/doubleClickWeight`）。常に左ボタンで80ms間隔の2回クリックを送信する
  （右ダブルクリックはOS上の意味が不定なため対象外）。コンテキストメニュー選択ロジックは
  実行しない。v0.20までは`ActionParams::doubleClickPercent`（0〜100%）による、通常の
  クリックが確率的にダブルクリックへ変わる仕組みだったが、v0.21で独立した操作種別に
  昇格し、③操作パラメータパネルの「このステップの操作種別・重み・回数」から直接
  有効化・重み付けできるようにした。
- **ランダムドラッグ**: 開始点から、6.4節の距離レンジ・方向設定に従って終点を計算し
  ドラッグする（終点はそのステップの含める領域のバウンディングボックスにクランプ。
  除外領域チェックは開始点のみで終点には未適用 — 既知の制約）。
- **ランダムキー入力**: 許可文字集合からランダムに1文字選び、Unicode文字として直接
  キーイベントを生成する（キーボード配列に依存しない実装）。修飾キー付きの破壊的な
  ショートカット（Cmd+Q等）は生成しない。
- **ランダムスクロール（上/下/横）**: 「スクロール(上)」「スクロール(下)」「スクロール(横)」
  はステップ内で個別に有効化・重み付けできる別々の操作種別。それぞれ6.4節の自分専用の
  レンジから量をランダムに決定する（横は発生のたびに左右どちらかをランダムに選ぶ）。
  横スクロールも、v0.20までは上下と共通の確率フラグ（`scrollHorizontal`）だったが、
  v0.21で独立した操作種別に昇格した。
- **ショートカットキー**: 6.4節のリストからランダムに1つ選び、対応する修飾キー＋
  キーのイベントを送信する（`PlatformAutomation::keyShortcut`）。macOSは仮想キーコード
  ＋`CGEventFlags`、LinuxはCtrl/Shift/Alt/Super相当のキーを押下→本体キー→離す、の順で
  送信。QtのポータブルなCtrl表記はmacOSではCmdとして送信される（Qt標準の挙動）。
- **メニュー項目選択の実装詳細（実験的機能。実機での動作検証は未実施）**:
  - macOS: 右クリック直後の最前面ウィンドウ（＝開いたメニューである想定）に対し
    `AXUIElementCopyElementAtPosition`でAccessibility要素を取得し、
    `kAXParentAttribute`を辿って`AXMenu`の祖先を探索する。項目名指定は
    `kAXChildrenAttribute`の各`AXMenuItem`の`kAXTitleAttribute`を列挙して一致するものを
    `AXUIElementPerformAction(kAXPressAction)`で選択し（`clickContextMenuItem`）、
    番号指定は`kAXChildrenAttribute`のN番目の要素に直接`kAXPressAction`を送る
    （`clickContextMenuItemAt`）。
  - Linux: AT-SPI（`atspi-2`、CMakeで`pkg_check_modules`により検出、無ければ
    no-opにフォールバック）でデスクトップ全体のアクセシビリティツリーを探索し、
    `ATSPI_ROLE_MENU`かつ表示中のノードを探す（深さ15・探索ノード数4000の上限あり）。
    項目名指定は見つかったメニューの子（`ATSPI_ROLE_MENU_ITEM`）の名前を列挙して一致する
    ものを`atspi_action_do_action`で選択し（`clickContextMenuItem`）、番号指定はN番目の子に
    直接`atspi_action_do_action`を送る（`clickContextMenuItemAt`）。対象アプリのツール
    キットがAT-SPIブリッジを有効にしている必要がある。
  - 項目名指定・番号指定のいずれも、期待通りに動かない場合ログには「選択できなかった」旨は
    出ない（現状は静かにEscapeで閉じるだけ）。項目名指定ではまず候補項目名（メニューに
    表示される文言と完全一致させる必要がある）を疑うこと。
  - **`enableContextMenuSelection`が無効（デフォルト）でも、右クリックのたびに150ms待って
    Escapeでメニューを閉じる**（v0.27で修正）。以前はこのブロック全体が
    `enableContextMenuSelection`有効時のみ実行されていたため、右クリックが有効なのに
    このメニュー項目選択機能自体は無効、というよくある設定（`enableRightClick`は有効・
    `enableContextMenuSelection`は無効のデフォルト）だと、対象アプリのウィジェットが
    実際にネイティブなコンテキストメニューを開いた場合にそれを閉じる処理が一切
    実行されず、開いたメニューが画面上に残り続けていた。すると以降のランダム操作の
    クリックやキー入力がそのメニュー（同じプロセスが所有するウィンドウなので
    `windowPidAtPoint`による安全チェックは通過してしまう）に吸われたり奇妙な形で
    干渉したりし、テスト全体が「たまに右クリックのあとで固まる」ように見える不具合が
    あった。修正後は`enableContextMenuSelection`の有効・無効にかかわらず、開いた
    可能性のあるメニューを毎回即座に解決（選択、または該当なしならEscapeで閉じる）する。

### 6.6 タイミング・制限
- 操作間隔: 最小/最大ms（都度この範囲でランダムな間隔をとり次の操作をスケジュール）。
  デフォルト値は20ms〜100ms。「ms指定」「回数/秒指定」の2つの入力方法を切り替えられる
  （実装状況は本節末尾を参照）。
- 最大実行回数（全ステップ合計）: **必須・無制限は選択不可**（1以上の値を必ず指定する。
  UI上も0を選べないようスピンボックスの下限を1にしている）。デフォルト値は1000。
- 最大実行時間（秒単位）: **必須・無制限は選択不可**（1以上の値を必ず指定する）。
  デフォルト値は120秒。
- ステップ全体（シーケンス）の繰り返し回数: **必須・無制限は選択不可**（1以上の値を必ず
  指定する）。デフォルト値は10。ステップが先頭に戻るたびにカウントし、上限に達したら
  自動停止する（最大実行回数/最大実行時間とは独立したカウンタ）。
- 「対象アプリを常に最前面に保つ」: 各イテレーション前に対象プロセスをアクティブ化
  （キー入力・ショートカットが対象に届くようにするため）。

実装状況（操作間隔の2つの指定方法）: ①対象選択パネルの「操作間隔:」欄に「ms指定」/
「回数/秒指定」のラジオボタンを設け、`QStackedWidget`（`MainWindow::m_intervalStack`）で
該当する入力欄を切り替えて表示する。
- 「ms指定」: 従来通り`m_minIntervalSpin`/`m_maxIntervalSpin`（最小/最大ミリ秒）。
- 「回数/秒指定」: `m_minRateSpin`/`m_maxRateSpin`（`QDoubleSpinBox`、0.01〜100.00回/秒、
  小数点2桁）。デフォルト値は10.00〜50.00回/秒（ms指定のデフォルト20ms〜100msと等価）。
  回数/秒は間隔(ms)の逆数の関係にあるため、**最小回数/秒（最もゆっくり）が最大ms間隔に、
  最大回数/秒（最も速く）が最小ms間隔に対応する**（例: 10回/秒〜50回/秒 ⇔ 20ms〜100ms）。

どちらのモードで入力した値も保持され続けるため、モードを行き来しても値が失われることは
ない。`MainWindow::buildConfigFromUi`は、テスト開始時点で選択されているモードに応じて
`TestConfig::minIntervalMs`/`maxIntervalMs`を計算する（回数/秒指定の場合は
`interval = 1000 / rate`で変換）。`RandomActionEngine`側はこれまで通りms単位の
`minIntervalMs`/`maxIntervalMs`のみを扱い、回数/秒という概念自体を知らない（UI側だけの
入力補助）。

ランダムテスト実行時にテスト対象のGUIの画面のスクショと操作領域を示した画像ファイルを
内部で撮っておいて、実行後に必要があれば操作領域の保存ボタンで画像をファイルで保存
できるようにしてください。

実装状況（操作領域スクリーンショットの内部保持と保存、v0.44）: ①対象選択パネルの
「タイミング・制限」に「操作領域スクリーンショットの撮影タイミング:」欄を新設し、次の
3つから選べるようにした（`TestConfig::screenshotCaptureMode`、デフォルトは
「ステップが変わるたび」）。
- 「実行前に一度だけ」: そのテスト実行で最初の操作を行う直前に1回だけ撮影する。
- 「ステップが変わるたび」: 現在のステップ（グループの場合は選ばれたメンバーではなく
  グループ自体）の番号が変わるたびに撮影する（実行開始時の最初のステップも含む）。
- 「一定間隔ごと」: 指定した操作回数（`screenshotCaptureIntervalActions`、デフォルト50、
  スピンボックスで変更可）ごとに撮影する。

いずれのタイミングでも、`RandomActionEngine`が内部に保持する画像は常に**直近の1枚のみ**
（新しい撮影のたびに上書き）で、実行中にメモリを消費し続けることはない。この1枚は
実行をまたいで保持される（`RunSummary`の実行結果サマリーと同様、次の撮影が起きるまでは
前回実行時の画像が保存可能なまま残る）ため、②ログ欄の「実行結果サマリーを保存...」の
隣に新設した「操作領域画像を保存...」ボタンは、一度でも撮影されていれば実行中かどうかに
関わらず押せる。

撮影処理（`RandomActionEngine::renderRegionScreenshot`）は、実際に操作対象となる領域を
解決した直後（`runOneAction`内、通常ステップ・グループメンバーいずれの操作でも共通）に
対象ウィンドウの現在のbounds分だけを`QScreen::grabWindow`で切り出し、その上に
`QPainter`で操作領域（含める領域、6.3節）を緑の実線、除外領域を赤の破線で描き込む。
つまり保存される画像は「そのステップ（またはグループメンバー）が実際にランダム操作を
行っていた領域が画面上のどこだったか」をそのまま視覚的に確認できるようになっている。

「操作領域画像を保存...」ボタンを押すとフォルダ選択ダイアログ（`QFileDialog::
getExistingDirectory`）が開き、選んだフォルダに`操作領域_<ステップ表示名を除去した
ラベル>_<操作領域の名前>_<タイムスタンプ>.png`という名前でPNGとして書き出す
（`<ステップ表示名>`はグループのメンバーから撮影された場合「ステップ1（グループ内
メンバー2）」のように6.2節の`stepLabel`表記がそのまま使われる。`<操作領域の名前>`は
v0.47で追加、次の実装状況を参照）。撮影タイミングの設定はJSONプリセット（10節）にも
`timing.screenshotCaptureMode`/`timing.screenshotCaptureIntervalActions`として保存/
復元される。

実装状況（保存するPNGに操作領域の名前を付記、v0.47）: 「不具合発生時にどの操作領域で
何が起きたのかを一目で分かるようにする」観点で、保存される画像そのものに操作領域の
名前を追記するようにした。
- `RandomActionEngine::renderRegionScreenshot`に`regionName`引数を追加し、緑の実線で
  描く含める領域それぞれの左上に、その領域の名前（名前付き操作領域なら`NamedRegion::
  name`、対象GUIの全領域を使うステップなら「対象GUIの全領域」）を、視認性のため半透明の
  背景付きラベルとして描き込むようにした（`drawRegionLabel`。1つの操作領域が複数の矩形
  から成る場合は同じ名前を各矩形に付ける）。赤い破線で描く除外領域にも同様に「除外」の
  ラベルを付けた。呼び出し元は`runOneAction`で、`step.useWholeWindow`かどうかに応じて
  `regionName`を都度組み立てて`maybeCaptureRegionScreenshot`へ渡している。
- ファイル名にも操作領域の名前を追加した。「操作領域画像を保存...」の保存先ファイル名は
  上記の通り`<ステップ表示名>_<操作領域の名前>`の2つを含む形にし、異常停止時に自動保存
  される方（6.7節）も`anomaly_<タイムスタンプ>_region_<操作領域の名前>.png`という
  ファイル名にした（従来は`anomaly_<タイムスタンプ>_region.png`で領域名を含んでいな
  かった）。名前に含まれる空白・スラッシュはファイル名として使えるよう除去している。
- 実機（Xvfb + openbox）で、「対象GUIの全領域」を使うステップで異常停止を発生させ、
  保存されたPNGファイル名が`anomaly_<timestamp>_region_対象GUIの全領域.png`になる
  ことと、画像自体にも緑枠の左上に「対象GUIの全領域」という緑色ラベルが実際に描画
  されていることを画像を開いて直接確認した。名前付き操作領域（矩形が対象GUIの全体で
  はない一部分のみのケース）についても、`resolveStepRegion`が返す矩形と`regionName`の
  対応関係はコードレビューで確認済みだが、この開発機では領域選択オーバーレイ自体が
  非対応環境のため、そのケースの対話的な実機確認はできていない（6.6節冒頭の既存の注記
  と同様の制約）。

なお、この機能は6.7節の「異常検知時のスクリーンショット」（対象プロセスのクラッシュ等で
異常停止した際に全画面をPicturesフォルダへ自動保存する既存機能）とは目的も実装も別物
（後者は毎回即座にディスクへ書き出す/前者は1枚だけメモリに保持し手動保存）だが、どちらも
`QScreen::grabWindow`を使っている点は共通。実機（Xvfb + openbox）で3つの撮影タイミングを
それぞれ実行して確認し、いずれも撮影後に「操作領域画像を保存...」が有効になり、指定
フォルダへ緑枠付きのPNGが正しく保存されることを確認した（対象領域が「対象GUIの全領域」
だったため緑枠は画像の縁全体を囲う形になる。名前付き操作領域の一部だけを囲むケースは、
この開発機では領域選択オーバーレイ自体が非対応環境のため対話的に確認できていないが、
`resolveStepRegion`が返す矩形をそのまま`runOneAction`から渡しているだけなので、実際の
操作対象決定ロジックと同じ座標系で描画されることはコードレビューで確認済み）。

実装状況（画面録画・クラッシュダンプ収集のオプション化、v0.46）: 「タイミング・制限」に
チェックボックスを2つ追加した。
- 「異常停止時に直近の画面を録画として保存する（追加負荷あり）」
  （`TestConfig::enableScreenRecording`、デフォルトOFF）。
- 「異常停止時にOSのクラッシュダンプ・診断ログを収集する」
  （`TestConfig::enableCrashDumpCollection`、デフォルトON）。

いずれもJSONプリセットに保存/復元される。詳細と実装は6.7節を参照。

### 6.7 緊急停止・異常検知
- テスト実行中は常時最前面のフローティングパネル（StopPanel）を表示し、実行回数表示と
  「■ 停止」ボタンを常に押せる状態にする（対象アプリにフォーカスが移っていてもテストを
  止められるようにするための安全策）。
- メインウィンドウ側の「■ 停止」ボタンでも同様に停止可能。
- **クラッシュ検知**: 毎イテレーション実行前に対象プロセスの死活を確認
  （`PlatformAutomation::isProcessRunning`、`kill(pid, 0)`ベース）し、対象プロセスが
  消えていたら「対象アプリケーションの異常終了（クラッシュ）を検知したため停止しました」と
  ログを出して自動停止する。v0.48で、検知と同時に**クラッシュを知らせるダイアログ
  （`QMessageBox::critical`）をポップアップ表示**し、どのステップの実行中に検知したか・
  実行回数・乱数シードを一目で分かるようにした（実装の詳細・実機確認内容は本節末尾の
  v0.48を参照）。
- 対象ウィンドウ自体が見つからなくなった場合（プロセスは生きているがウィンドウが
  閉じられた等）も、その旨をログに出して自動停止する。
- **対象アプリ以外を誤操作しそうになった場合の安全停止**: 各操作を実際に送信する直前に、
  それが本当に対象アプリに当たるかを確認し、当たらない可能性がある場合は即座にテスト全体を
  停止する（スキップして継続はしない）。
  - クリック・ドラッグ・スクロール（座標を指定する操作）: `PlatformAutomation::
    windowPidAtPoint(pt)`でその座標にある最前面のウィンドウの所有プロセスIDを取得し、
    対象のPIDと一致するかを確認する。対象ウィンドウが移動した、他のウィンドウに覆われた、
    最小化された等の理由で座標上に対象アプリがない場合は不一致となり停止する。ドラッグは
    開始点・終了点の両方をチェックする。
  - キー入力・ショートカット（座標を指定しない操作）: `PlatformAutomation::
    activeProcessPid()`で現在アクティブ（最前面）なアプリケーションのPIDを取得し、
    対象のPIDと一致するかを確認する。対象アプリがアクティブでない場合、キー入力が別の
    アプリに送られてしまう可能性があるため停止する。
  - いずれの確認も判定不能（-1が返る等）な場合は「安全とは確認できない」とみなし停止する
    （fail-closed）。

実装状況（異常時自動収集の強化・画面録画、v0.46）: 「不具合発生時にどのような操作で
不具合が起きたのかを開発者に伝える」観点で、異常停止時（`RunSummary::anomaly`）の
自動保存内容を拡張した。保存先はいずれも既存の異常時スクリーンショット（6.10節）と
同じ`RandomActionEngine::anomalyArtifactsDirectory()`
（＝`~/Pictures/EnduranceTestGUI_Screenshots/`）。
- **実際に使われたテスト設定のプリセットJSON保存**: `MainWindow::buildPresetJson()`を
  `onSavePreset()`から共通処理として切り出し、異常時にも同じ内容を
  `anomaly_<タイムスタンプ>_preset.json`として保存する。ただし乱数シードは、UI上の
  設定値（「0=毎回ランダム」のこともある）ではなく、実行開始時に実際に使われた値
  （`RunSummary::rngSeedUsed`）で上書きする。これにより、異常発生時の操作列を
  このプリセットから理論上再現できる（再現性の限界は6.10節「乱数シード」を参照）。
- **クラッシュダンプ・診断ログの探索**（`enableCrashDumpCollection`、デフォルトON）:
  `PlatformAutomation::findRecentCrashReport(pid, appName)`で対象アプリの直近の
  クラッシュ記録をベストエフォートで探し、見つかった場合はそのパスを
  `anomaly_<タイムスタンプ>_crashreport_location.txt`に書き出す。
  - Linux: `/var/lib/systemd/coredump`と`/var/crash`を探索し、ファイル名にpidまたは
    対象アプリ名を含み、かつ直近300秒以内に更新されたものだけを対象とする（無関係な
    古いファイルを誤って対象アプリのものと報告しないための時間窓）。
  - macOS: `~/Library/Logs/DiagnosticReports`内の`.ips`/`.crash`ファイルのうち、
    ファイル名に対象アプリ名（大文字小文字区別なし）を含み直近300秒以内に更新された
    ものを対象とする（**この開発機はmacOSの対話的動作確認ができないため未検証**、
    8章参照）。
  - コアダンプ基盤自体が無い/無効な環境（今回のテスト用コンテナ等）では、単に
    「見つからなかった」旨がログに出るだけであり、これは正常な挙動（異常ではない）。
  - （v0.47）見つかったファイルが現在のユーザー権限で実際に読み取れるかどうかを
    `QFileInfo::isReadable()`で確認し、読み取れない場合は続けて「このファイルは
    現在のユーザー権限では読み取れません（root権限、またはcoredumpctl等の専用ツール
    が必要な場合があります）。パスの記録のみ行いました。」とログに追記するようにした。
    Linuxの`/var/lib/systemd/coredump/`はディレクトリ自体が一般ユーザーでも一覧参照
    できることが多い一方、個々のcoredumpファイルは大抵root所有・厳しめのパーミッション
    （`600`〜`640`）で保存されており、非管理者ユーザーで対象アプリを動かしている場合、
    「パスは見つかったが中身は読めない」状態になりうることを実機で（該当パーミッションの
    疑似coredumpファイルを配置して）確認した上での対策（5節の「非rootユーザーでの動作」
    も参照）。パスの記録自体は読み取り可否に関わらず行うため、「このパスを別途root権限
    で確認してほしい」と開発者に伝える材料としては引き続き機能する。
- **画面録画**（`enableScreenRecording`、デフォルトOFF、追加負荷があるため明示的な
  オプトインとした）: 有効にして実行すると、`RandomActionEngine`が実行中0.5秒間隔
  （`kRecordingFrameIntervalMs`）で対象ウィンドウのスクリーンショットをリングバッファ
  （`QTimer`+`QList<QPixmap>`、上限`kMaxRecordingFrames`=20枚＝直近約10秒分、上限
  超過時は最古のフレームを破棄）へ保持し続ける。一時停止中は録画も停止する。異常停止時
  にはこのバッファの内容を`anomaly_<タイムスタンプ>_recording/frame_0000.png`〜
  `frame_NNNN.png`として連番で書き出し、不具合発生直前の画面推移を後から追える。
  正常終了時（回数/時間上限到達・ユーザー停止）はバッファを破棄するだけで書き出しは
  行わない。
- 実機（Xvfb + openbox）で、`kill -9`による対象プロセスの強制終了で異常停止を誘発し、
  プリセットJSON・実際に使われたシード値・録画フレーム（17枚保存を確認、うち1枚を
  画像として開き有効な内容であることを確認）がいずれも正しく保存されることを確認した。
  クラッシュダンプ探索は、このコンテナにコアダンプ基盤が無いため「見つからなかった」
  という正常系の結果になることを確認した。

実装状況（クラッシュ通知ダイアログ、v0.48）: 「対象アプリがクラッシュして落ちた場合に
検知してダイアログで知らせ、同時にテストを終了してどのステップで落ちたか分かるように
してほしい」という要望を受けて実装した。
- `RunSummary`に`targetCrashed`フラグを追加し、`isProcessRunning`ベースのクラッシュ検知
  （本節冒頭）でのみ`true`にする（`doStop(reason, isAnomaly, targetCrashed)`の第3引数）。
  対象ウィンドウ消失・安全停止・ハング検知など他の異常停止とは区別しており、
  `MainWindow::onRunSummaryReady`は`summary.targetCrashed`を見て、他の異常時と共通の
  ログ/自動保存処理に加えて`QMessageBox::critical`でクラッシュ通知ダイアログを表示する。
  （翻訳後の`reason`文字列そのものを見て判定するのではなく専用のboolを使っているのは、
  6.12節のI18n機構により`reason`が英語になっている場合でも判定を言語非依存にするため。）
- クラッシュ検知時のログメッセージ自体にも、どのステップの実行中に検知したかを追記した
  （「ステップ 1の実行中に対象アプリケーションの異常終了（クラッシュ）を検知したため
  停止しました」）。ダイアログの本文はこの`stopReason`をそのまま使い、加えて実行回数・
  乱数シード・異常停止時の記録が自動保存済みである旨を表示する。
- **【重要な発見】`isProcessRunning`のゾンビプロセス誤判定を修正**: この機能を実装し
  実機（`kill -9`でTestTargetを強制終了）で検証したところ、クラッシュ通知ダイアログが
  出ず、代わりに（本来は想定通りだが今回の目的には具体性が足りない）「ステップ 1の
  対象領域が見つからないため停止しました」という別の異常停止経路が先に働いてしまう
  現象を発見した。原因を調査したところ、`kill(pid, 0) == 0`は**ゾンビプロセス
  （終了済みだが親プロセスがまだ`wait()`で回収していないプロセス）に対しても真を返す**
  ため、`isProcessRunning`が「クラッシュ直後、親プロセスに回収されるまでの短い間」
  対象プロセスを誤って「まだ生きている」と判定してしまい、クラッシュ検知が１テンポ
  遅れて別の異常停止経路（ウィンドウ消失起因）に先を越されることがあると判明した
  （本節冒頭の「クラッシュ検知・安全停止・異常時スクリーンショット」というv0.40の
  実機確認記録は、まさにこの現象を「どちらでも安全に停止するので実害なし」として
  記録していたが、クラッシュ専用ダイアログを出す今回の目的にはこの取りこぼしが
  直接影響することが分かった）。
  - **対策**: `isProcessRunning`に、プロセスの状態がゾンビでないことの確認を追加した。
    Linuxは`/proc/<pid>/stat`の状態フィールド（`queryProcessStats`と同じ括弧探索方式で
    パース）が`Z`かどうかを確認する。macOSは`proc_pidinfo(pid, PROC_PIDTBSDINFO, ...)`で
    取得した`pbi_status`が`SZOMB`かどうかを確認する（`queryProcessStats`と同じ
    `libproc`ベースの手法。**この開発機はmacOSの対話的動作確認ができないため未検証**、
    8章参照）。いずれもゾンビと判定した場合は「実行中ではない」として扱う。
  - 実機（Xvfb + openbox）で、修正前は`kill -9`直後に「対象領域が見つからない」経路で
    停止しダイアログが出ないことを確認した上で、この修正を適用した後は同じ手順で
    確実に「対象アプリのクラッシュを検知しました」ダイアログが表示され、
    「ステップ 1の実行中に対象アプリケーションの異常終了（クラッシュ）を検知したため
    停止しました」「実行回数: 107」「乱数シード: 1557827985」が正しく表示されること、
    ダイアログを閉じるとテストが同時に停止済みの状態（「▶ 開始」有効・「■ 停止」無効）
    になっていることをスクリーンショットで確認した。
-Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。

実装状況（確率的クラッシュの解析機能、v0.51）: 「テスト対象ツールによってはある操作の
後に確率的にツールが落ちるようなバグを解析する。その際には何回テストして何回ツールが
落ちたのかをまとめる必要がある」という要望を受けて、10節で提案した5件（③①②④⑤）を
実装した。
- **③複数回実行の集計（`TestStatistics`クラス、新設）**: `RunSummary`（クラッシュ有無・
  クラッシュ時ステップ`crashStepIndex`・実行回数・経過時間・乱数シード等）を1回ごとに
  `MainWindow::onRunSummaryReady()`から`TestStatistics::addRun()`へ渡し、
  `~/Documents/EnduranceTestGUI_Logs/crash_stats.jsonl`へ追記形式で永続化する（アプリを
  再起動しても、対象アプリを手動で再起動して同じテストを繰り返しても、同じファイルに
  蓄積され続ける）。「同じテスト」であることの判定には、①②③の設定内容（名前付き領域・
  ステップ・デフォルト操作パラメータ・タイミング/制限の主要項目）から算出したSHA-1
  フィンガープリント（`TestStatistics::computeFingerprint()`、乱数シードや録画/
  クラッシュダンプ収集等の診断オプションは対象外）を使う。①対象選択下・ログ欄下の
  「統計...」ボタンから`StatisticsDialog`を開くと、このフィンガープリントについての
  「試行回数」「クラッシュ回数／率」「クラッシュまでの平均実行回数・経過時間」
  「ステップ別クラッシュ内訳（表）」を確認でき、CSVエクスポート（全フィンガープリント分、
  1行1実行）・このテスト設定の統計のみリセット、もできる。ログ欄下には簡易サマリー
  （「このテスト設定: N回中M回クラッシュ（P%）」）を常時表示する。
- **①連続自動実行モード（バッチ実行）**: 「連続実行回数」スピンボックス（デフォルト1
  =従来通り）をN>1にすると、`MainWindow::onStart()`が`m_batchModeActive`を立て、以後
  `onEngineFinished()`→`continueBatchIfNeeded()`が1回終わるたびに次を自動的に開始する。
  対象アプリがまだ生きていれば（`tryReselectLastTarget()`が成功すれば）即座に次を開始し、
  落ちていれば（②の自動起動コマンドが設定されていればそれを使って自動起動した上で）
  `m_batchWaitTimer`（1秒間隔）で対象アプリの再出現をポーリングし、検知次第自動的に
  次を開始する——つまり②を使わず対象アプリを手動で再起動する運用でもバッチは機能する
  （ユーザーが再起動さえすれば自動的に検知して続きが進む）。「■ 停止」はバッチ全体を
  キャンセルする（実行中はそのランが終わり次第、再起動待ち中は即座に）。
- **②対象アプリの自動起動コマンド設定**: ①対象選択に「対象アプリの自動起動コマンド
  （任意）」欄（実行ファイルを選ぶ「参照...」ボタン、手動で今すぐ試せる「今すぐ起動」
  ボタン付き）を追加した。`QProcess::splitCommand()`でプログラム/引数に分割した上で
  `QProcess::startDetached(program, arguments)`を使う（Qt6で削除された単一QString版の
  `startDetached`は使わず、Qt5/Qt6両対応の形にした）。プリセットJSONにも
  `targetLaunchCommand`として保存/読込される。空のままでも①は機能する（SPEC.md 10 ①
  参照）。
- **⑥「開始時にこのコマンドで対象ツールを起動してから開始する」オプション（v0.59）**:
  ①対象選択に新設したチェックボックス（`m_launchBeforeStartCheck`）。オフ（デフォルト）
  では従来通り、「▶ 開始」は今①で選択中の対象へそのままステップ構成の実行を始める。
  オンにすると、「▶ 開始」を押した瞬間に対象アプリを起動せず、まず②の自動起動
  コマンドで対象アプリを起動し（`launchTargetAppFromConfiguredCommand()`）、
  `m_batchWaitTimer`で出現をポーリングして検知してから、通常どおり起動時セットアップ
  →ステップ構成の実行を開始する（`m_launchBeforeStartPending`フラグで、この直後の
  `beginRun()`が①バッチ実行の継続ではなく単発の対話的開始であることを
  `onBatchWaitTick()`に伝える）。オンなのに②の自動起動コマンドが未設定の場合は
  「▶ 開始」を押した時点で設定エラーを表示し、何も開始しない。
- **⑦独立した「⟳ 連続実行」ボタン（v0.59）**: 「▶ 開始」＋「連続実行回数」N>1
  （上記①）は対象アプリが生きていればそのまま使い回す一方、こちらは押すたびに
  必ず対象アプリを一度終了させてから新しいインスタンスを起動し直す、より厳密な
  「毎回クリーンな状態から」の繰り返し実行。`PlatformAutomation::terminateProcess(pid)`
  （Linux/macOSともSIGTERM、非同期・ベストエフォート）を新設し、押すたびに
  `killTargetThenRelaunchForContinuousRun()`が①の対象一覧に現在残っている対象アプリ
  （見当たらなければ直前の実行で使ったpid、`m_lastRunTargetPid`）へ`terminateProcess()`
  を送ってから、`isProcessRunning()`で実際にプロセスが終了するまで
  `m_batchWaitTimer`でポーリングし（`m_continuousWaitPhase == WaitingForExit`）、
  終了を確認したら②の自動起動コマンドで新しいインスタンスを起動して出現を待ち
  （`WaitingForAppear`）、検知したら起動時セットアップ→ステップ構成を実行する。
  1回終わるたびにこのキル→再起動サイクルを「連続実行回数」の指定回数だけ繰り返す
  （`m_continuousRunMode`フラグで、内部的には①と同じ`m_batchModeActive`ベースの
  継続処理を共有しつつ、`waitForTargetThenContinueBatch()`/`onBatchWaitTick()`の
  分岐先を切り替える形で実装した）。②の自動起動コマンドが未設定のときは押した時点で
  設定エラーを表示する（このボタンは対象アプリが生きているかどうかに関わらず必ず
  起動し直す仕様のため、①と異なり自動起動コマンドは必須）。
- **④ステップ別クラッシュ率のハイライト**: `MainWindow::describeStep()`が、
  `updateStatisticsDisplay()`が③の集計から作るキャッシュ（`m_stepCrashCounts`/
  `m_statsTotalRuns`）を見て、そのステップの直後にクラッシュしたことが一度でもあれば
  「⚠ クラッシュ N/M回」を行末に追記する（0件のステップには何も表示しない）。
  `updateStatisticsDisplay()`はプリセット読込後・実行開始直前・実行終了後に呼ばれる。
- **⑤直前操作ログの自動添付強化**: `RandomActionEngine`に直近
  `kRecentActionHistorySize`（15）件の実行済み操作の説明文を保持するリングバッファ
  （`m_recentActionDescriptions`）を追加し、`RunSummary::recentActions`として毎回の
  サマリーに含めた（`formatSummaryText`/`summaryToJson`双方に反映、クラッシュ時に限らず
  常時）。乱数シードだけでは再現しない非決定的なクラッシュでも、直前の実操作
  （クリック座標等）から当たりをつけやすくなる。
- **【重要な発見・修正】クラッシュ通知ダイアログのモーダル化が①のバッチ処理を停止
  させてしまう不具合**: v0.48で追加したクラッシュ通知ダイアログ（本節既出）は
  `QMessageBox::critical`（内部で`exec()`し、ユーザーがOKを押すまでイベントループごと
  ブロックする）で実装されていたが、①のバッチ実行と組み合わせると、無人運用中に
  クラッシュが起きるたびにダイアログがアプリ全体を停止させてしまい、無人での連続
  再現試行という目的そのものを阻害することが実機テストで判明した。`QMessageBox`を
  `Qt::WA_DeleteOnClose`付きで`show()`する非モーダル表示に変更し、ダイアログを閉じずに
  放置してもバッチ処理が裏で進み続けるようにした（クラッシュの記録自体はこの時点で
  すでにログ・統計・異常時アーティファクトとして保存済みのため、ダイアログを見るか
  どうかはユーザーの任意になる）。
- **【副次的な発見・修正】実行終了後も②ステップ一覧に「▶ 実行中」表示が残る既存の
  不具合**: 本機能の実機テスト中に、`MainWindow::onEngineFinished()`が
  `m_currentRunningStepIndex`をリセットする**前**に`describeStep()`を呼んでいたため、
  その呼び出しの引数（`index`）と比較対象（`m_currentRunningStepIndex`）が常に同じ値に
  なり、「実行中」表示がテスト終了後も（次にそのステップの表示が別の理由で更新される
  まで）残り続けるバグ（v0.51より前から存在）を発見した。リセットしてから
  `describeStep()`を呼ぶ順序に修正した。
- 実機（Xvfb + openbox）で、①③④⑤についてはバッチ実行（自動起動コマンド設定あり）で
  `kill -9`によるクラッシュと`TestTarget`自身の「クリック厳禁」終了ボタンへの偶発的な
  クリックによる自己終了の両方を経由して複数回連続実行が完走すること、統計ダイアログの
  数値・ステップ別内訳・CSVエクスポート・リセットが正しいこと、を確認した。さらに②を
  使わない完全手動運用（自動起動コマンド欄を空にし、`kill -9`後に手動で`TestTarget`を
  再起動してから改めて「開始」を押す）でも、試行回数・クラッシュ率・ステップ別
  バッジが同じように正しく積算されることを確認し、ユーザーからの「自動再起動の機能を
  使わずに手動でテスト対象ツールを立ち上げて同じテストを実行する時にも③④⑤の機能は
  使えるように」という明示的な要件を満たしていることを確かめた。
-Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。

テスト対象ツールの操作応答が遅い時に自動でテスト操作を減速するオプションをつけてください。

実装状況（応答が遅い時の操作間隔自動延長、v0.71）: 上のハング検知が使っている
`PlatformAutomation::checkWindowResponsive`（`_NET_WM_PING`応答確認、8秒間隔で
ポーリング）をそのまま流用した。`TestConfig::autoSlowdownEnabled`（デフォルトOFF、
①タイミング・制限パネルの「対象アプリの応答が遅い時は操作間隔を自動的に延ばす」
チェックボックス）を有効にすると、`RandomActionEngine::checkTargetResponsiveness()`が
WM_PINGへの応答を1回だけ逃した時点（＝2回連続で失敗してハングと確定する前の段階）で
`m_slowdownActive`を立て、以後`scheduleNext()`が算出する操作間隔を
`kAutoSlowdownIntervalMultiplier`（5倍）だけ引き伸ばす。応答が戻れば（次のWM_PING
成功時）即座に元の間隔へ戻す。ハング確定（2回連続失敗）時の停止条件はこのオプションの
有無に関わらず変更していない——このオプションはあくまで「本当にハングする前に負荷を
下げて回復のチャンスを与える」ための予防措置であり、実際にハングした場合は従来どおり
異常停止する。

実機確認（Linux, Xvfb + openbox）: `TestTarget`に9秒間イベントループを止める
デバッグ用ボタンを一時的に追加し、`autoSlowdownEnabled: true`のプリセットJSONを
CLIヘッドレス実行（`--run`、6.14節）でそのボタンをクリックさせた上で、`-Wall -Wextra
-Wpedantic`付きのクリーンビルドが警告0件のまま、オプション有効時も異常なく通常どおり
（回数制限で正常停止）完走することを確認した。ただし、この`TestTarget`実行環境
（Xvfb + openbox上のQt/xcbビルド）は`_NET_WM_PING`に対応を表明していながら実際には
一度も応答を返さないという、8章・9章v0.41で既知の制約として文書化済みの制約を持つため
（実機でも確認済みとあり、この開発環境固有の問題ではない）、`m_everRespondedToPing`が
一度も真にならず「一度も応答しないためハング検知自体を無効化する」既存の安全側
フォールバック（本節既出）が先に働いてしまい、本オプションが実際に狙う「1回だけ応答が
遅れて間隔を延ばし、その後回復して元に戻る」という経路そのものは、この環境では
（既存のハング検知の成功経路自体と同様に）検証できなかった。デバッグ用ボタンは
検証後に削除し、通常ビルドに戻した。

### 6.8 ログ
- タイムスタンプ付きで実行した操作（種類・座標・選択したメニュー項目等）を表示。
- 「ログをクリア」「ログを保存...」（テキストファイル書き出し）に対応。

実装状況（ログの欠落防止・可読性向上、v0.46）:
- **ログの欠落防止**: 画面表示用のログ欄（`m_logView`）は`QPlainTextEdit::
  setMaximumBlockCount(5000)`により古い行を自動的に捨てる仕様であり、長時間の実行では
  不具合発生前の操作履歴が失われてしまっていた。`MainWindow`に`m_fullLogFile`
  （`QFile`）を追加し、実行開始時（`onStart`）に`~/Documents/EnduranceTestGUI_Logs/
  run_<タイムスタンプ>.log`を開き、`appendLog()`が画面表示に1行追加するたびに同じ内容を
  このファイルへも書き込んで都度flushするようにした（画面上は従来通り直近5000行のみ
  表示だが、ファイルには実行開始から終了までの全行が残る）。実行終了時
  （`onEngineFinished`）にクローズする。
- **座標・操作対象の可読性向上**: 座標を伴う操作（クリック・ドラッグ・スクロール等）の
  ログ行に、対象ウィンドウ左上を原点とした相対座標`[対象ウィンドウ相対: (x, y)]`を
  追記するようにした（`PlatformAutomation::queryWindowBounds`で取得したウィンドウ位置
  との差分）。さらに、OSのアクセシビリティAPIで実際にクリックされた地点のUI部品名を
  `PlatformAutomation::accessibleNameAtPoint(pt)`でベストエフォート取得し、取得できた
  場合はログ行に`[部品名]`として追記する（取得できない場合は追記しない）。
  - macOS: `AXUIElementCopyElementAtPosition` + `kAXTitleAttribute`→
    `kAXDescriptionAttribute`→`kAXRoleAttribute`（ロール名を括弧書き）の順に
    フォールバックして取得（**未検証**、8章参照）。
  - Linux: AT-SPI（`atspi_get_desktop`→`get_component_iface`→
    `get_accessible_at_point`）で取得。**【重要な発見】** この実装中に、AT-SPIの
    呼び出し（`atspi_init()`等）はアクセシビリティ用のD-Busバスに接続できない環境では
    エラーを返すのではなく、GLibの`G_LOG_LEVEL_ERROR`（dbindが内部で「Couldn't
    connect to accessibility bus」を報告する際に使用）経由でプロセスを`abort()`
    させることを発見した。この`G_LOG_LEVEL_ERROR`はGLibの設計上常にfatalであり、
    `g_log_set_fatal_mask()`・`g_log_set_always_fatal()`のいずれでもマスクできない
    ことを、独立した再現プログラムと`gdb`のバックトレース
    （`SIGTRAP`→`g_log_structured_array`→`_atspi_bus`）で確認済み。実際、この機能を
    実装当初のまま組み込んだところ、アクセシビリティ用D-Busバスが存在しないXvfb
    コンテナ上でクリック操作のたびにGUI本体がクラッシュする重大な不具合が実機テストで
    発覚した。
    - **対策**: AT-SPI呼び出しの前に`qEnvironmentVariableIsSet
      ("DBUS_SESSION_BUS_ADDRESS")`を確認する`atspiUsable()`ヘルパーを新設し、
      環境変数が未設定の場合はAT-SPI自体を一切呼ばずに空文字列（＝部品名なし）を
      返すようにした。これは新設の`accessibleNameAtPoint`だけでなく、6.5節の
      右クリックメニュー項目選択で使われている既存の`findOpenMenu()`（実装当初から
      同じ脆弱性を潜在的に持っていたが、これまで露見していなかった）にも同じガードを
      追加した。残存リスクは8章を参照。
  - 実機（Xvfb + openbox）で、保存された全ログファイルの内容を直接確認し、相対座標・
    部品名の両方が正しく付記されていること、および修正後は`kill -9`による強制終了を
    挟みながらクリックを伴うテストを長時間（実行回数300回超）実行してもクラッシュしない
    ことを確認した。

### 6.9 GUIデザイン
- 画面を縦に３分割します。
- 一段目には対象選択の項目を置きます。
- ２段めは横に２分割し、左から「ステップ構成」「操作パラメータ」を配置
- ３段目にログ出力をする
- 「対象選択」では領域の追加、編集、削除とそのほか全体的な設定を行う。ステップの全体的な繰り返し回数や、最大実行回数/最大実行時間、対象アプリを常に最前面に保つかどうか、
  操作間隔のレンジなどを設定する。各領域の操作のデフォルトを設定することも可能とする。
- 「ステップ構成」には操作の順番を示すリストを表示し、ステップの追加/編集/削除/順序変更を行う。「追加」は「対象選択」で決めた領域の選択のみを行い、その中での操作種別（クリック/ドラッグ/キー入力/スクロール/ショートカットキー）と回数は「操作パラメータ」パネルで指定する。ステップの順番はリスト内で上下移動させることで変更可能とする。
- 「操作パラメータ」では、②で選択中のステップの操作種別・重み・回数と、各ステップで使用する操作の詳細な設定を行う。具体的には、ドラッグ操作の距離や方向、キー入力の許可文字、スクロール量などを指定できる。また、「対象選択」で指定した操作のデフォルト値を用いる選択も可能とする。

実装状況（v0.35で3カラム横並びから縦3段組みに変更。旧レイアウトの詳細は変更履歴v0.35参照）:
`MainWindow`は`QSplitter(Qt::Vertical)`を外枠として縦に3段（1段目:
①`buildTargetColumn`、2段目: ②`buildStepsColumn`/③`buildActionParamsColumn`を
`QSplitter(Qt::Horizontal)`で左右に並べたもの、3段目: 実行制御+ログ）に分割している。
いずれの境界もドラッグして自由に幅・高さを変更できる。①には対象選択（上段）の下に、
**操作領域**（名前付き、追加/編集/削除ボタンと一覧。詳細は6.3節）と**タイミング/制限**
（シーケンス繰り返し回数・最大実行回数/時間・常に最前面・操作間隔・乱数シードを含む）を
横並びに配置している（v0.38。①の中はスクロール領域なのでどちらのグループも幅が
足りなければ内容が折り返される。6.9節に明記されていないため、横幅を有効に使うための
実装上の判断として横並びにした）。②の
ステップ一覧の「追加」ボタンは`StepEditorDialog`（領域選択のみ）を開き、①で登録した
操作領域（または「対象GUIの全領域」）から1つを選んでステップを追加する（6.2節）。
「編集」も同ダイアログで領域だけを変更する。③は②で選択中のステップに連動するマスター・
ディテールUIで、上段に**このステップの操作種別・重み・回数**（ステップ未選択時は無効化。
クリック/ダブルクリック/ドラッグ/キー入力/スクロール(上/下/横)/ショートカット/ウィンドウ
操作の9種別）、下段に操作の詳細設定（`ActionParams`。デフォルト/カスタムの切替を含む）を
配置する。この2分類（操作種別の選択と操作の詳細設定）の違い、および右上の「デフォルト」
ボタン・`DefaultActionParamsDialog`の挙動は6.4節（特に6.4.1〜6.4.3）で詳述する。
実行制御（Start/Stop/一時停止ボタン・状態表示・リソース使用量/経過時間/実行回数表示）と
ログは3段目にまとめて配置している（①②③のいずれにも属さない横断的な要素のため）。

実装状況（狭い幅・高さでの操作性）: `QSplitter`は各段・各カラムの境界をドラッグして自由に
幅・高さを変えられるため、ウインドウ全体を小さくしたり、いずれかの段/カラムを狭めたり
した場合でも各コントロールが操作できるよう、以下の対応を行った（v0.28。この時点ではまだ
3カラム横並びレイアウトだったが、対応内容自体はv0.35の縦3段組みレイアウトでも引き続き
有効）。
- 長い説明文の`QLabel`には`setWordWrap(true)`を設定し、幅に応じて自動的に折り返す
  ようにした（③の各種説明ラベル、`NamedRegionEditorDialog`・`StepEditorDialog`・
  `DefaultActionParamsDialog`の説明ラベル、権限状態ラベルなど）。`QCheckBox`は
  `setWordWrap`を持たないため、特に長いチェックボックス文言（③のウィンドウ操作、
  操作の詳細設定パネルの右クリックメニュー選択）はテキストに明示的な改行を入れて
  2行表示にした。
- 1行に多数のコントロールを並べていた箇所を、複数行に分割した。具体的には: ③の操作の
  詳細設定パネルのドラッグ距離/方向（2行に分割）・名前付きキーの6個のチェックボックス
  （ラベルを独立した行にし、3個ずつ2行に分割）・ウィンドウ操作の4個のチェックボックス
  （ラベルを独立した行に分割）、①の権限状態ラベルとボタン（ラベル→ボタンの縦積みに変更）、
  下部の実行制御行（開始/停止/一時停止/状態表示の行と、メモリ・CPU使用率/経過時間/実行回数
  表示の行の2段に分割）。
- ①のタイミング・制限フォーム（`QFormLayout`）には`setRowWrapPolicy(QFormLayout::
  WrapLongRows)`を設定し、ラベルが長い行（「ステップ全体（シーケンス）の繰り返し回数
  （必須）:」等）は幅が足りない場合に入力欄をラベルの下へ自動的に折り返すようにした。

### 6.10 耐久テストのバリエーション拡張機能

8章のレビューで提案した8機能を実装した。いずれも`RandomActionEngine`
（`ActionKind::WindowOp`追加、`pickWeightedActionKind`、一時停止/再開、乱数シード、
リソースサンプリング、異常時スクリーンショット）と`ActionParams`/`RegionStep`
（`TestConfig.h`）に集約されている。

- **ウィンドウ操作アクション（`ActionKind::WindowOp`）**: ステップで有効化すると、
  対象ウィンドウ自体の移動・リサイズ・最小化・最大化のいずれかをランダムに実行する
  （`RegionStep::enableWindowOp` + `ActionParams::windowOpMove/Resize/Minimize/Maximize`
  で許可する種類を選べる）。安全確認は座標ではなく`queryWindowBounds(windowId, pid)`が
  成功するか（＝そのウィンドウが今も対象プロセスのものか）で行う。移動・リサイズは
  ウィンドウが乗っている画面（`QScreen`）の利用可能領域内に収まるよう乱数の範囲を
  決定する。最大化は各OSのネイティブな「最大化」状態ではなく、画面の利用可能領域に
  移動+リサイズすることで統一的に実現している（`PlatformAutomation::moveWindow`/
  `resizeWindow`/`minimizeWindow`/`maximizeWindow`）。macOSはAccessibility API
  （`AXUIElement`の`kAXPositionAttribute`/`kAXSizeAttribute`/`kAXMinimizedAttribute`。
  pid+windowIdから対応する`AXUIElement`ウィンドウを現在のboundsで突き合わせて特定する
  `copyAXWindow`を新設）、LinuxはXlib（`XMoveWindow`/`XResizeWindow`/`XIconifyWindow`/
  `XMoveResizeWindow`、いずれも`_NET_WM_PID`で対象pidの所有ウィンドウであることを確認
  してから実行）で実装。`activateProcess`は最小化されたウィンドウを復帰させる処理も
  行うよう拡張した（macOS: 全ウィンドウの`kAXMinimizedAttribute`をfalseに設定、
  Linux: `XMapWindow`。Linux側は従来`collectWindows()`が可視ウィンドウのみを対象と
  していたため最小化中のウィンドウを見つけられない問題があり、`_NET_CLIENT_LIST`を
  直接見る`findAnyWindowForPid`に変更して対応した）。
- **名前付きキー**: `ActionParams::keyIncludeTab/Return/Escape/Backspace/Delete/
  ArrowKeys`のチェックボックスで、印字可能文字（`allowedKeyChars`）に加えてTab・
  Return・Escape・Backspace・Delete・矢印キー（上下左右）をキー入力の候補に含められる。
  `PlatformAutomation::keyTapNamed(Qt::Key)`を新設（`keyShortcut`と同じキーコード
  変換を修飾キーなしで使う）。Linux側はこれまで`keyShortcut`がASCII文字にしか対応
  できていなかった（`KeySym(key)`への直接キャストが名前付きキーの値と一致しないため）
  ため、`qtKeyToX11Keysym`という共通変換関数を新設し、`keyShortcut`・`keyTapNamed`
  両方で使うように修正した（副次的な既存バグ修正）。
- **ダブルクリック**: 実装当初は`ActionParams::doubleClickPercent`（0〜100%）で、左
  クリックが発生した際にその割合でダブルクリック（80ms間隔で2回クリック）にする方式
  だったが、v0.21で独立した操作種別（`RegionStep::enableDoubleClick/doubleClickWeight`）
  に置き換えた。詳細は6.5節。
- **操作種別ごとの重み**: `RegionStep::clickWeight/doubleClickWeight/dragWeight/keyWeight/
  scrollUpWeight/scrollDownWeight/scrollHorizontalWeight/shortcutWeight/windowOpWeight`
  （デフォルト1）。`RandomActionEngine::pickWeightedActionKind`が有効化された種別の重みの
  合計から加重ランダム選択する（全て1のときは従来の一様ランダムと同じ）。v0.20以降、
  各操作チェックボックス+重み用スピンボックスは`StepEditorDialog`ではなく③操作パラメータ
  パネルの「このステップの操作種別・重み・回数」グループに配置されている（6.2/6.4節）。
- **乱数シード**: `TestConfig::rngSeed`（0=毎回ランダム）。`RandomActionEngine`は
  内部の`QRandomGenerator`を専用インスタンス化し（従来は`QRandomGenerator::global()`
  への生ポインタで、明示的なシード指定ができなかった）、開始時に指定シード（0の場合は
  自動生成した値）で`seed()`し、その値をログに記録する。クラッシュ等で停止した場合、
  ログに記録されたシードを「対象選択」パネルの乱数シード欄に入力して再実行すれば、
  理論上同じ乱数列（＝同じ操作列）を再現できる。ただし対象アプリ自身の内部状態や
  タイミング依存の挙動までは再現しない点に注意。
- **リソース使用量の記録**: `PlatformAutomation::queryProcessStats(pid)`
  （`ProcessStats{residentMemoryMB, cpuTimeSeconds}`）を新設。macOSは`proc_pidinfo`
  （`PROC_PIDTASKINFO`）、Linuxは`/proc/<pid>/status`（VmRSS）と`/proc/<pid>/stat`
  （utime+stime、`_SC_CLK_TCK`で秒に変換）を読む。`RandomActionEngine`は5秒おとに
  サンプリングし、直前サンプルとの差分からCPU使用率（%、1コア相当）を計算して
  `resourceUsageUpdated`シグナルで通知、ログにも記録する。`MainWindow`はコントロール行に
  現在値を表示する。一時停止中はサンプリングも止まる。
- **一時停止/再開**: `RandomActionEngine::pause()`/`resume()`を新設。一時停止中は
  アクションのタイマーとリソースサンプリングを止め、経過時間の計測も一時停止する
  （`m_pausedElapsedMs`に停止までの経過を積算し、再開時に`QElapsedTimer`を再スタート
  することで、最大実行時間の制限に一時停止中の時間を含めないようにしている）。
  `MainWindow`のコントロール行に「‖ 一時停止」/「▶ 再開」ボタンを追加。
- **異常検知時のスクリーンショット保存**: `RandomActionEngine::doStop`に`isAnomaly`
  引数を追加し、クラッシュ検知・対象ウィンドウ消失・安全停止（対象アプリ以外を操作
  しそうになった等）で`true`を渡すようにした（ユーザーによる停止や回数/時間制限到達は
  `false`のまま）。`isAnomaly`のときは全スクリーン（`QGuiApplication::screens()`の
  各`QScreen::grabWindow(0)`）を`~/Pictures/EnduranceTestGUI_Screenshots/`
  （`QStandardPaths::PicturesLocation`）にPNGで保存し、保存先をログに出力する。
  macOSでは画面収録の権限が無いと空/失敗する可能性がある点に注意（8章参照）。

### 6.11 ライセンス表示

本アプリはQt（Widgetsモジュール）を使って構築されている。Qtのフリーソフトウェア版は
GNU LGPL バージョン3（一部モジュールはGPLバージョン2/3）の下で配布されており、動的リンク
であれば利用側アプリのソースコード開示までは要求されないが、エンドユーザーがQt自身の
ライセンス条文を確認できる手段を提供する必要がある。

実装状況: メニューバーに「ヘルプ」メニューを新設（`MainWindow::buildMenuBar`）し、次の
2項目を配置した。
- **「EnduranceTestGUIについて...」**（`MainWindow::onAboutApp`）: `QMessageBox::about`で
  アプリ名・概要・使用しているQtのバージョン（`QT_VERSION_STR`）・QtがLGPLv3
  （一部GPL）で配布されている旨・条文は「Qtについて...」から確認できる旨を表示する。
- **「Qtについて...」**（`MainWindow::onAboutQt`）: Qt標準の`QMessageBox::aboutQt(this)`を
  そのまま呼ぶ。このダイアログはQt自身が提供するもので、実際に使用しているQtが
  どのライセンスで配布されているかを明示し、「Show License」ボタンでその条文全体を
  表示できる。そのため、ライセンス条文をアプリ側で別途同梱・複製する必要がない。

macOS上ではQtがこの種のメニュー項目を自動的にアプリケーションメニュー（メニューバー
左端、アプリ名のメニュー）へ統合する場合がある（Qtの標準的な挙動）。いずれの場所に
表示されても、ダイアログの内容自体は変わらない。

実装状況（リポジトリをpublicにするにあたってのライセンス整備、v0.49）: 上記のアプリ
内表示に加えて、リポジトリ直下に以下のファイルを新設した。
- **`LICENSE`**: EnduranceTestGUI自身のソースコードのライセンス（MIT License）。
- **`THIRD_PARTY_NOTICES.md`**: 動的リンクしているサードパーティライブラリ
  （Qt、GLib/GObject、AT-SPI、X11クライアントライブラリ、macOSのApple純正
  フレームワーク）それぞれのライセンスと、動的リンクのみで無改変である旨、Qt自体の
  ソースコード入手先（<https://download.qt.io/>）をまとめた一覧。
- **`LICENSES/`ディレクトリ**: LGPLv3・GPLv3・LGPLv2.1・GPLv2の条文全文
  （Debianプロジェクトが配布する公式の写し、`/usr/share/common-licenses/`由来）を
  参考として同梱。これらはQt自体（LGPLv3、一部モジュールGPLv2/v3）およびGLib/AT-SPI
  （LGPLv2.1+）が採用しているライセンスの条文で、動的リンクであるため厳密には同梱
  必須ではないが、リポジトリを閲覧する開発者が実際の条文をすぐ確認できるよう用意した。
  ネットワークアクセスが制限された開発環境で作業したため、GNU公式サイトからではなく
  Debianパッケージが同梱する公式配布物の写しを使用している（内容はFSF発行の正文と
  同一）。
- Qt自体のライセンス条文は、本節冒頭で述べた通り実行中のアプリの「ヘルプ」→
  「Qtについて...」からも確認できる（こちらが一次情報源）。`LICENSES/`と
  `THIRD_PARTY_NOTICES.md`はリポジトリ単体を見ただけでもライセンス関係が分かるように
  するための補足という位置付け。

### 6.12 多言語対応（日本語/英語、v0.47）

UI表示言語を日本語・英語から選べるようにした。

- **設計方針**: `I18n`（`src/I18n.h/.cpp`）という薄いレイヤーを新設した。Qt標準の
  `tr()`/`.ts`/`.qm`（Qt Linguist）方式は採らず、既存コード全体に元々ベタ書きされて
  いる日本語の`QStringLiteral`テキストそのものを翻訳テーブルの**キー**として使う
  独自方式にした（`I18n::t(QStringLiteral("日本語テキスト"))`という形でラップする）。
  理由は、このコードベースがv0.1から一貫して`tr()`を一切使わずUI文字列を直接
  日本語で書いてきており（今回調査するまで約260件相当）、今さら全箇所に意味のある
  `tr()`メッセージIDを新設するより、既存の日本語テキストをそのままキーとして
  再利用する方が変更が機械的かつレビューしやすく、翻訳テーブル（`I18n.cpp`内の
  `QHash<QString, QString>`）と実際のUI文字列が常に一致していることも保証しやすい
  ため。
- **言語切り替えのタイミング**: `MainWindow`のメニューバーに「言語 / Language」
  メニューを新設し、「日本語」「English」のチェック可能な排他選択項目
  （`QActionGroup`）を配置した。選択すると`I18n::setLanguage()`が`QSettings`
  （組織名`asobi`、アプリ名`EnduranceTestGUI`。`main.cpp`のexecutableName設定と
  同じスコープ）へ即座に永続化されるが、**画面表示への反映は次回起動時**になる
  （選択直後に「言語設定を保存しました。変更を反映するにはアプリを再起動して
  ください。/ Language preference saved. Please restart the app for the change
  to take effect.」という日英併記のダイアログを表示する）。これは、ウィンドウ
  タイトル・ボタンラベル・グループボックスのタイトルなど大半のUI文字列が
  ウィジェット構築時に一度だけ設定される作りになっており、ライブ再翻訳
  （`QEvent::LanguageChange`のハンドリングと全ウィジェットの文言再設定）を
  実装するコストに見合わないと判断したための設計上の割り切り。ログ・ステータス
  メッセージなど実行時に都度生成される文字列は、生成されるたびに`I18n::t()`を
  呼ぶ作りになっているため、次回起動後は特別な対応なしに正しい言語で表示される。
- **実装**: `I18n::currentLanguage()`は初回呼び出し時に`QSettings`から設定を
  遅延読み込みする（デフォルトは日本語）。`I18n::t(japaneseText)`は現在の言語が
  英語の場合のみ翻訳テーブルを引き、見つからない場合は日本語テキストそのものを
  返す（訳漏れが「空白」ではなく「日本語のまま表示される」という安全側の劣化に
  なるようにしている）。`%1`/`%2`等の`QString::arg()`プレースホルダは、翻訳後の
  英語テキスト側でも同じ個数・同じ意味の位置に保つ必要があり（`arg()`呼び出し
  自体は言語を意識しないため）、これは翻訳テーブル作成時に一件ずつ確認した。
  C++の隣接文字列リテラル結合（`QStringLiteral("A" "B")`は`QStringLiteral("AB")`と
  等価）を使って複数行に分けて書かれている長いメッセージ（確認ダイアログの説明文、
  「EnduranceTestGUIについて」の本文など）も、結合後の文字列全体を1つのキーとして
  正しく扱うようにしている。
- **対象範囲**: `MainWindow`・`RandomActionEngine`・`StepGroupEditorDialog`・
  `ActionParamsEditor`・`NamedRegionEditorDialog`・`ActionKindEditor`・
  `StepEditorDialog`・`DefaultActionParamsDialog`・`StopPanel`・
  `RegionSelectorOverlay`の計10ファイルにある、ユーザーに表示される日本語文字列
  （メニュー・ボタン・ラベル・ツールチップ・ダイアログ・ログ/ステータスメッセージ
  ・エラーメッセージ・保存ファイル名の一部など）約290件すべてを`I18n::t()`で
  ラップした。ヘッダファイル（`.h`）中の日本語はすべてソースコード上のドキュメント
  コメントであり実行時文字列ではないため対象外。「言語 / Language」メニュー項目
  自体の文言と、「日本語」「English」という選択肢名、および上記の再起動を促す
  ダイアログは、現在の表示言語に関わらず常に両言語併記のままにしている
  （言語設定そのものを見失わないようにするため）。
- **実機確認**（Xvfb + openbox）: 「言語 / Language」→「English」を選択→
  アプリを再起動し、メニューバー・①②③パネルの全ラベル・ボタン・ログ・
  「EnduranceTestGUIについて」ダイアログ（`Qt %1`のバージョン埋め込みや
  「"About Qt..."」のような二重引用符を含む長文）が正しく英語表示されることを
  スクリーンショットで確認した。この過程で1件、実装上の不具合を発見・修正した:
  「タイミング・制限」の英訳を当初「Timing & Limits」としていたところ、Qtの
  ウィジェットは既定でラベル文字列中の`&`を次の1文字を下線付きのキーボード
  ニーモニックとして解釈する仕様のため、画面上「Timing _Limits」のように文字化けした
  ように見える形で表示されてしまう不具合を実機テストで発見した。「Timing / Limits」
  に変更して解決した（`&`を含む訳文が今後増える場合は、`&&`でエスケープするか、
  この例のように`&`自体を使わない訳文にする必要がある）。

実装状況（Qt標準ダイアログ部品の未翻訳を修正、v0.52）: アプリ全体の実機回帰テスト
（9章v0.52参照）で、`QDialogButtonBox`の標準ボタン（OK/Cancel/Close）・
`QInputDialog::getInt()`（「待機ステップを追加/編集」）・`QMessageBox`の標準ボタン
（はい/いいえ等）・`QFileDialog`（「参照...」「保存」「読み込む」等で使われる、
「アドレス:」「ファイル名:」「ファイルの種類:」等を含むファイル選択ダイアログ）が、
日本語モードでも常に英語表示のままになる不具合を発見した。原因は、これらの文字列は
本節冒頭で説明した独自`I18n::t()`レイヤーの対象外（Qt自身が内部的に持つ標準UI文字列
であり、アプリ側のコード上に日本語のリテラルとして存在しない）であるため、これまで
一度も翻訳テーブルでカバーされたことがなかったこと。Qt自体はこれらの標準文字列の
日本語訳（`qtbase_ja.qm`）を独自に保有しているが、アプリ側が対応する`QTranslator`を
インストールしない限り適用されない仕様のため、素のままでは常にQtの元言語（英語）で
表示され続ける。
- **対策**: `main.cpp`で、`I18n::currentLanguage()`が日本語の場合にのみ、
  `QLibraryInfo::path(QLibraryInfo::TranslationsPath)`（Qt6。Qt5は
  `QLibraryInfo::location()`）が指すQt自身の翻訳ファイル格納ディレクトリから
  `qtbase_ja.qm`を`QTranslator`で読み込み、`QApplication::installTranslator()`で
  登録するようにした。該当ファイルがシステム上に見つからない場合は`load()`が
  `false`を返すだけで`installTranslator()`自体を呼ばず、これまでどおり英語表示に
  静かにフォールバックする（クラッシュや例外にはならない）。アプリ自身の`I18n`
  レイヤーとは独立した仕組みのため、既存の約290件の翻訳には一切手を加えていない。
- この対策により、`QDialogButtonBox`のOK/Cancel/Close、`QMessageBox`のはい/いいえ、
  `QInputDialog`のOK/Cancel、`QFileDialog`の「アドレス:」「ファイル名:」
  「ファイルの種類:」「開く」「保存」「キャンセル」等、日本語モードで従来英語の
  ままだった標準UI文字列がすべて日本語で表示されるようになった。英語モードでは
  この`QTranslator`自体をインストールしないため、影響は一切ない。
- 実機（Xvfb + openbox）で、日本語モードにおいて「待機ステップを追加」ダイアログ
  （`QInputDialog`）・「統計」ダイアログの「閉じる」ボタン（`QDialogButtonBox::Close`）
  ・テスト設定読み込み時の上書き確認（`QMessageBox`の「はい」「いいえ」）・
  「テスト設定を読み込む」ファイル選択ダイアログ（`QFileDialog`）が、修正前は
  該当ボタン/ラベルのみ英語のまま表示されていたのに対し、修正後はすべて正しく
  日本語表示されることを確認した。英語モードに戻した場合に元の英語表示のまま
  変化がないことも確認した。
  -Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。

### 6.13 起動時セットアップ（デターミニスティックなセットアップマクロ、v0.53）

検証するテストツールによっては、起動直後にログイン・初期設定など決まった手順を
踏んでから初めてランダム操作による耐久テストを始めたい場合がある（例:
「ユーザー名欄をクリック→アカウント名を入力→Tab→パスワードを入力→Return」）。
従来の②ステップ構成は「同じ領域内で*ランダムに選んだ*操作を繰り返す」設計のため、
こうした*毎回同じ順序で同じ操作を1回だけ*行う用途には向かなかった。この節の機能は、
②のランダムループが始まる**前**に、①「対象選択」パネルで組んだ固定シーケンスを
一度だけ実行する仕組みを追加するもの。

- **データモデル**（`TestConfig.h`）: `enum class SetupActionType { Click,
  DoubleClick, RightClick, Drag, TypeText, KeyPress, Wait }`と、1件分の操作を表す
  `struct SetupAction`（`type`、`point`/`dragToPoint`（対象ウィンドウ左上を基準とした
  相対座標）、`text`（TypeText用）、`keySequence`（KeyPress用、`QKeySequence`形式）、
  `waitMs`（Wait用）、`label`（任意の説明文））を新設。`TestConfig::setupActions`
  （`QList<SetupAction>`）に順序付きで保持する。座標は`NamedRegion::followsTargetWindow`
  のような追従有無のトグルを設けず、**常に**対象ウィンドウの現在位置を基準に解決する
  設計にした（セットアップマクロは起動のたびに再実行するのが通常の使い方であり、
  「追従しない」を選ぶ意味がある場面がほぼ無いため）。
- **座標の指定方法（`PointPickerOverlay`）**: `src/PointPickerOverlay.h/.cpp`を新設。
  `RegionSelectorOverlay`（6.3節、矩形をドラッグで複数指定）を単純化し、画面全体を
  覆う半透明オーバーレイ上で1回左クリックした地点を返すだけの部品にした
  （`Esc`でキャンセル）。ドラッグ追跡用の状態を持たない分`RegionSelectorOverlay`より
  シンプルで、`SetupActionEditorDialog`がクリック/ダブルクリック/右クリック/ドラッグの
  座標欄から呼び出す。
- **1件を編集するダイアログ（`SetupActionEditorDialog`）**: `src/
  SetupActionEditorDialog.h/.cpp`を新設。「種類」コンボボックスで7種類から選び、
  `QStackedWidget`で種類ごとの入力欄（点1つ/ドラッグの2点/文字列/キー/待機ミリ秒）を
  切り替える。点は「位置を選択...」ボタンから`PointPickerOverlay`で拾った絶対座標を、
  ダイアログを開いた時点の対象ウィンドウ左上との差分（相対座標）に変換して保持する
  （`NamedRegionEditorDialog`が対象ウィンドウ位置を`MainWindow::currentTargetTopLeft()`
  から受け取る手法と同じ）。対象ウィンドウが選択されていない状態では座標選択を
  無効化し、警告ダイアログを出す。
- **①パネルへのUI統合**: 「対象」グループの下に「起動時セットアップ（対象アプリ
  起動直後に一度だけ実行。ログイン等）」グループボックスを新設した。中身は
  ②ステップ一覧と同様の一覧+ボタン構成（追加.../編集.../削除/↑上へ/↓下へ）に加えて、
  「連続自動実行（①バッチ）では初回のみ実行する」チェックボックスを配置（下記参照）。
  一覧の各行は`MainWindow::describeSetupAction()`が「1: クリック (195, 67)
  [Button1Click]」のような1行要約を生成する。実行中はこのグループも他の①②③パネル
  同様に無効化される（`setControlsEnabled()`）。
- **実行ロジック（`RandomActionEngine`）**: `start()`で`m_config.setupActions`が
  空でなければ`m_inSetupPhase = true`にし、`performRandomAction()`（`m_timer`の
  スロット）の先頭付近、既存の時間/回数制限チェックや対象クラッシュ検知・予期しない
  ウィンドウ処理より後・②のステップ処理より前で`performSetupAction()`に分岐させる
  ことで、②の乱数ループと全く同じタイマー駆動・単一スレッドの仕組みを再利用しつつ
  「セットアップフェーズ中は別の処理をする」を実現した。`trySetupAction()`が
  1件を実際にディスパッチする: Click/DoubleClick/RightClick/Dragは
  `queryWindowBounds()`で対象ウィンドウの*現在の*左上を取得し相対座標を絶対座標に
  変換したうえで、6.7節と全く同じ「`windowPidAtPoint(pt) == targetPid`でなければ
  安全のため停止」というfail-closedの安全確認を行ってから`PlatformAutomation::
  mouseClick`/`mouseDrag`を呼ぶ。TypeText/KeyPressは同様に`activeProcessPid() ==
  targetPid`を確認してから`keyTap`/`keyShortcut`（`parseShortcut()`を`ActionKind::
  Shortcut`と共用）を呼ぶ。右クリックが開いたコンテキストメニューは6.7節の
  `handlePossibleContextMenu()`を再利用して必ず閉じるが、セットアップは
  「毎回同じ操作をする」ものなのでメニュー項目のランダム選択（`ActionParams::
  enableContextMenuSelection`）はここでは使わず、常にEscapeで閉じるだけにしている。
- **安全確認失敗時の再試行（提案④相当）**: 起動直後は対象ウィンドウがまだ
  最終的な位置・サイズに落ち着いていない、あるいはまだマップされていないことが
  ありうるため、上記の安全確認（`windowPidAtPoint`/`activeProcessPid`/
  `queryWindowBounds`自体の失敗）に対しては即座に`doStop()`せず、
  `retrySetupOrFail()`が`m_timer`を短い間隔（300ms）で再スケジュールして同じ
  `SetupAction`を再試行する（ブロッキングsleepではなくタイマー再スケジュールなので
  再試行中でも■停止は即座に効く）。最大20回（`kMaxSetupSafetyRetries`）まで再試行し、
  それでも解決しなければ`doStop(..., isAnomaly=true)`で異常停止として扱う
  （6.7節のスクリーンショット/クラッシュダンプ収集が自動的に働く）。1件成功する
  たびに再試行カウンタは0に戻る。
- **ログ・サマリーへの記録**: 各セットアップ操作は「起動時セットアップ 1/2:
  クリック at (452, 124) (説明文)」のようにログへ出力され、②の乱数操作と同じ
  `recordRecentAction()`で`RunSummary::recentActions`にも含まれる（クラッシュ直前の
  操作列を追うときにセットアップ分も見えるようにするため）。ただし
  `RunSummary::actionKindCounts`（操作種別ごとの回数）や`iterationCount`
  （実行回数）には含めない設計にした。これらは②の乱数操作の統計を表す指標であり、
  「毎回同じ回数だけ必ず実行される」セットアップの回数を混ぜると、たとえば
  「クリック: 5回」が乱数操作5回なのかセットアップ込みの数なのか紛らわしくなる
  ため。
- **①バッチ（連続自動実行）との連携**: `TestConfig::setupActions`自体は「常に今回
  実行する」という意味に統一し（`TestConfig.h`のコメント参照）、「バッチ2回目以降は
  スキップする」という判断は`RandomActionEngine`ではなく`MainWindow::beginRun()`が
  行う。①の「連続自動実行（①バッチ）では初回のみ実行する」チェックボックスが
  オンかつ`m_batchRunsCompleted > 0`（＝このバッチの2回目以降の実行）のときだけ、
  `buildConfigFromUi()`が返した`TestConfig`の`setupActions`を`beginRun()`が
  その場で空にしてから`m_engine->start()`に渡す。初回だけ出るライセンス同意
  ダイアログのようなケースを想定した機能。
- **プリセット保存/読込**: `TestConfigJson.h/.cpp`に`setupActionToJson`/
  `setupActionFromJson`を追加し、`MainWindow::buildPresetJson()`/`onLoadPreset()`が
  `setupActions`配列と`setupActionsFirstRunOnly`真偽値をプリセットJSONの一部として
  保存/復元する（他の①②③設定と同じ扱い）。
- **翻訳**: 新設した約45件の文字列をすべて`I18n::t()`でラップし、`I18n.cpp`の
  翻訳テーブルに英訳を追加した。実機テストで発見した1件の訳漏れ（種類コンボの
  「文字入力」単体キーが、ログ用の「文字入力 '%1'」とは別キーだったため未登録
  だった）を修正済み。
- **既知の制約（v0.55で解消）**: `PointPickerOverlay`は`RegionSelectorOverlay`と同じ
  `Qt::WA_TranslucentBackground`を使っていたため、コンポジットマネージャの無い
  Xvfb+openbox環境では半透明部分が不透明な黒として描画されていた（8章の既存の制約と
  同一原因）。クリック自体は正しく機能していた（実機テストでボタン中央をクリックして
  座標が正しく拾えることを確認済み）。この制約はv0.55でスクリーンショット背景方式に
  変更したことで解消済み（6.13節のv0.55を参照）。
- **実機確認**（Xvfb + openbox、日英両モード）: ①パネルに「起動時セットアップ」
  グループが正しく配置されること、「追加...」→種類コンボの全7種類が正しく翻訳
  されること（英語モードで発見した「文字入力」未訳を上記の通り修正）、Wait種別で
  待機時間分だけ②開始が遅れログに「起動時セットアップが完了しました。ランダム
  操作を開始します」と出ること、Click種別で`PointPickerOverlay`により拾った座標が
  対象ウィンドウ（TestTarget）の実際のボタンを正しくクリックし
  （TestTargetの左クリックカウンタが1増え、TestTarget自身のログにも記録される
  ことで確認）、その後②の乱数操作へ正しく引き継がれることを確認した。
  プリセット保存→読込の往復でセットアップ一覧・「初回のみ実行」チェックが
  正しく復元されることも確認した。「初回のみ実行」チェックを付けて連続実行回数2で
  バッチ実行し、1回目のログには起動時セットアップの実行が含まれる一方、2回目の
  ログ（「連続実行: 2/2回目を開始します」の直後）には起動時セットアップの行が
  一切現れず②の乱数操作からいきなり始まることを確認した。
  -Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。

実装状況（コードレビューで発見した不具合の修正、v0.54）: v0.53実装分についてコード
レビュー（差分全体の精査）を行い、以下4件の不具合を修正した。

- **【発見・修正】座標を選択しないまま「OK」を押せてしまう不具合**:
  `SetupActionEditorDialog`でクリック/ダブルクリック/右クリック/ドラッグを選び、
  「位置を選択...」を一度も押さずに「OK」を押すと、`SetupAction::point`が既定値の
  `QPoint(0, 0)`のまま保存されてしまっていた。この座標は対象ウィンドウの左上
  ぎりぎり（通常はウィンドウ枠/装飾の領域で、対象プロセス自身のウィンドウとは
  判定されない）を指すため、実行時に`trySetupAction()`の安全確認
  （`windowPidAtPoint(pt) == targetPid`）が必ず失敗し、最大20回・約6秒の再試行の
  末に「対象アプリは正常なのに」異常停止として扱われてしまう、という分かりにくい
  失敗を招いていた。`SetupActionEditorDialog`に`m_pointPicked`/`m_dragToPicked`
  （実際に「位置を選択...」が押されたか、または編集対象が既に原点以外の座標を
  持っていたか）を追加し、点が必要な種別で位置が未選択のまま「OK」を押すと
  「入力エラー: 「位置を選択...」から位置を指定してください。」で弾くようにした。
  併せて、ほぼ同一実装が3箇所（`onPickPoint`/`onPickDragFromPoint`/
  `onPickDragToPoint`）に重複していたのを`pickPointInto()`という共通ヘルパーに
  統合した。
- **【発見・修正】文字入力（パスワード等）の内容が平文でログに残る不具合**:
  本節冒頭で挙げた「ログイン」というユースケース自体が、`SetupActionType::
  TypeText`でパスワードを入力する使い方を想定しているにもかかわらず、
  `RandomActionEngine::trySetupAction()`が生成する説明文（「文字入力 '実際の
  文字列'」）がそのまま画面のログ・実行のたびに書き出される完全ログファイル・
  `RunSummary::recentActions`（異常停止時に6.7節の仕組みで診断一式として保存
  される）に記録されてしまっていた。入力した文字列そのものではなく文字数のみを
  記録する（「文字入力（13文字、内容はログに記録しません）」）よう変更し、
  `SetupActionEditorDialog`の文字入力ページにも「パスワード等も入力できます。
  実行ログにはこの内容自体は記録されません。」という案内文を追加した。なお、
  ①パネルのセットアップ一覧が表示する1行要約（`MainWindow::
  describeSetupAction()`）は編集時に内容を確認できるよう引き続き実際の文字列を
  表示する（この一覧はファイルに保存されるログやバグ報告用の診断一式には含まれ
  ないため）。
- **【リファクタリング】`PointPickerOverlay`/`RegionSelectorOverlay`間の重複コード**:
  仮想デスクトップ全体のジオメトリを求める`virtualDesktopGeometry()`と、
  Qt5/Qt6両対応で`QMouseEvent`のグローバル座標を取り出す`globalPosOf()`が、
  両オーバーレイの`.cpp`に一字一句同じ実装で重複していた（片方だけ修正して
  もう片方に反映し忘れるリスク）。`src/OverlayGeometry.h`（ヘッダオンリー、
  `OverlayGeometry`名前空間）に切り出し、両方から共有するようにした。
- **【発見・修正】プリセット読込の上書き確認ダイアログの文言漏れ**: 起動時
  セットアップだけが設定されていて②③が空の状態で「テスト設定を読み込む」を
  押すと、確認ダイアログ自体は表示される（`!m_setupActions.isEmpty()`も表示
  条件に含めたv0.53の対応どおり）ものの、文言が「現在の操作領域・ステップ構成は
  読み込んだ内容で上書きされます」のままで起動時セットアップへの言及が無く、
  ユーザーがそれと気付かずに上書きしてしまう恐れがあった。「現在の起動時
  セットアップ・操作領域・ステップ構成は読み込んだ内容で上書きされます」に
  修正した。
- 上記4件はいずれも実機（Xvfb + openbox、日本語モード）で再現・修正確認した:
  座標未選択のまま「OK」を押すと入力エラーダイアログが出て保存されないこと、
  既存エントリの再編集（座標変更なしで「OK」）ではエラーが出ないこと、
  「hunter2secret」（13文字）を入力するセットアップを実行し、保存された完全
  ログファイルに文字数のみが記録され元の文字列が一切含まれないこと、プリセット
  読込の確認ダイアログの文言が更新されていることを確認した。
  -Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。

実装状況（コンポジット非対応環境での「画面が真っ暗になる」不具合の修正、v0.55）:
「Linux上の実行で操作領域の追加をしようとすると画面が真っ暗になって操作できません。
画面を透かしにして操作領域の選択をしやすいようにコードを変えてください」という
報告を受け、8章に既知の制約として記載していた黒塗り問題（コンポジットマネージャの
無い環境で`Qt::WA_TranslucentBackground`が効かない）を、実際に操作不能になるという
実害があることを踏まえて解消した。

- **【修正】スクリーンショット背景方式への変更**: `RegionSelectorOverlay`・
  `PointPickerOverlay`・`RegionHighlightOverlay`から`Qt::WA_TranslucentBackground`
  を削除し、代わりにオーバーレイを表示する直前（構築時、または非表示から再表示に
  切り替わる直前）に`QScreen::grabWindow(0)`で画面のスクリーンショットを取得して
  自前で不透明な背景として描画し、その上に矩形・ラベル・ヒントテキスト等を重ねて
  描くように変更した（`src/OverlayGeometry.h`に共通ヘルパー
  `grabVirtualDesktopSnapshot()`を追加）。コンポジットの有無に関わらず同じ見た目に
  なるため、`openbox`単体のような環境でも実際の背景が透けて見える。
- **【発見・修正】`RegionHighlightOverlay`が画面全体を覆っていた不具合**: 当初は
  `RegionSelectorOverlay`/`PointPickerOverlay`のみ修正すれば十分と判断したが、
  実機テストで「操作領域の設定」ダイアログを開いた瞬間に画面全体が真っ暗になる
  ことを確認した。原因は、このダイアログが開くと同時に常時表示される
  `RegionHighlightOverlay`（画面全体を覆う常に最前面のウィンドウ）も同じ理由で
  黒塗りになっていたため。同じスクリーンショット背景方式を適用し、非表示から
  再表示に切り替わるたび（`RegionSelectorOverlay`表示中は隠しているため）に
  新しいスクリーンショットを撮り直すようにして修正した。
- **【発見・修正】ダイアログがハイライトの下に隠れてZ順位が固定できない不具合**:
  上記の修正後も実機テストで「操作領域の設定」ダイアログ自体が全く見えない
  現象が続いた。`xdotool`でウィンドウを直接調査したところ、`RegionHighlightOverlay`
  の常時最前面ウィンドウ（`Qt::WindowStaysOnTopHint`）が、`raise()`/
  `activateWindow()`を呼んでも実際のスタッキング順序でこのダイアログの上に
  居座り続けることが分かった（`openbox`がQtからの明示的な積み重ね操作より
  「常に最前面」ヒントを優先していた）。`NamedRegionEditorDialog`自身にも同じ
  `Qt::WindowStaysOnTopHint`を付与し、両者を同じ「常に最前面」レイヤーに
  揃えることで解決した。
- **【発見・修正】確認済みの矩形にセレクタのヒントバーが焼き付く不具合**: 上記2件の
  修正後、矩形をドラッグしてEnterで確定した直後の画面に、既に閉じたはずの
  `RegionSelectorOverlay`のヒントテキストバー（「含める領域を選択中 ― ...」）が
  ハイライト表示と同時に残って見える現象を確認した。原因は、ウィンドウの
  unmap（非表示化）がX11では非同期に処理されるため、`RegionSelectorOverlay`を
  閉じた直後に`RegionHighlightOverlay`が`screen->grabWindow(0)`で次の背景を
  撮影すると、まだ画面に残っている閉じかけのヒントバーごと撮影してしまい、
  それがそのままハイライトの背景として固定されてしまうこと。
  `OverlayGeometry::settleDesktopBeforeSnapshot()`を追加し、生のスクリーン
  グラブを行う直前に必ず数回`processEvents()`と短い待機を挟むことで、直前に
  閉じた別ウィンドウの後始末が画面に反映されるのを待ってから撮影するようにした。
- 実機（Xvfb + openbox、コンポジットマネージャなし）で、「操作領域の設定」
  ダイアログを開いた瞬間に画面が真っ暗にならないこと、「矩形を描画...」で
  背景（対象アプリ・ダイアログ）が透けて見えたままドラッグで矩形を追加できる
  こと、Enterで確定した直後の画面にヒントバーの焼き付きが無くハイライト
  表示（緑の矩形とラベル）のみが正しく残ること、確定した矩形が①パネルの
  操作領域一覧に正しく追加されることを確認した。
  -Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。

実装状況（起動時セットアップの位置選択結果を画面上に表示、v0.58）:
「起動時セットアップで追加ボタンでセットアップを追加するとき、選択された位置や
開始位置、終了位置をダイアログ表示中だけ表示をしてください」という要望を受け、
`NamedRegionEditorDialog`が`RegionHighlightOverlay`で編集中の操作領域を画面上に
表示し続けるのと同じ仕組みを、`SetupActionEditorDialog`にも追加した。

- 新設した`PointHighlightOverlay`（`RegionHighlightOverlay`と同じ、`QScreen`ごとに
  1つのクリックスルー・常に最前面ウィンドウという設計。複数モニタ/異なるスケール
  ファクタ環境での正しい表示のため）が、選択済みの点を画面上に小さな十字マーカーと
  ラベル（Click/DoubleClick/RightClickなら「位置」、Dragなら「開始」「終了」を
  破線で結んで表示）として描画する。`SetupActionEditorDialog::updateHighlight()`が
  種類コンボの変更時・位置picked時・ダイアログを開いた直後に呼ばれ、ダイアログが
  閉じる際（`QDialog::finished`）に消える（`NamedRegionEditorDialog::
  updateHighlight`と同一パターン）。背景は`RegionHighlightOverlay`と同じく
  スクリーンショットを自前で描画する方式（v0.55で確立した、コンポジット非対応
  環境での黒塗り回避策）を採用している。
- **【発見・修正】ダイアログ自体がこの新しい常に最前面のオーバーレイの下に隠れる
  不具合**: 実装直後の実機テストで、`NamedRegionEditorDialog`がv0.55より前に
  抱えていたのと全く同じ原因（`RegionHighlightOverlay`同様、`Qt::
  WindowStaysOnTopHint`を持つ常に最前面のオーバーレイウィンドウを、それより
  下位レイヤーのダイアログの上から表示すると、一部のウィンドウマネージャが
  ダイアログを完全に隠してしまう）で`SetupActionEditorDialog`自体が見えなくなる
  不具合を発見した。`NamedRegionEditorDialog`と同じ対処（ダイアログ自身にも
  `Qt::WindowStaysOnTopHint`を付与し、`updateHighlight()`内で`raise()`/
  `activateWindow()`を呼ぶ）で修正した。
- 実機（Xvfb + openbox）で、Click系の種別で位置を選択すると画面上に「位置」
  ラベル付きマーカーが表示されること、Drag種別で開始・終了の両方を選択すると
  2つのマーカーが破線で結ばれて表示されること、種類コンボをDragに切り替えると
  ダイアログ自体が隠れずに正しく表示され続けること、ダイアログをOKで確定/
  キャンセルするとマーカーが消えること、既存エントリを「編集...」で開き直すと
  保存済みの点にマーカーが再表示されることを確認した。-Wall -Wextra -Wpedantic
  付きのクリーンビルドで警告0件を維持。

実装状況（起動時セットアップの「記録」機能、v0.60）:
「起動時セットアップに記録ボタンをつけて、記録ボタンを押すと次にエスケープ
ボタンが押されるまでのユーザー操作を記録して起動時セットアップに記録される
ようにしてください。このとき、操作中に出てくるダイアログも操作対象となります
ので、ダイアログも含めた操作ができるように実装してください」という要望を受けて
実装した。v0.53で「真のシステム全体パッシブ入力記録」をスコープ外とした理由
（プラットフォームごとの低レベルフック実装が必要、macOS側の検証リスクが大きい）
は変わっていないが、今回は要望が録画そのものを明示的に求めているため、その
前提で実装した。

- 新設した`InputRecorder`（`src/platform/InputRecorder.h`）が、マウスの
  クリック・ドラッグ・キーボード入力を**システム全体で**（＝どのウィンドウが
  フォーカスされているかに関わらず、対象アプリが開いたダイアログを操作した
  場合も含めて）観測し、`SetupAction`相当の情報（種別・座標・文字列）を
  `actionRecorded`シグナルで通知する。`MainWindow::onRecordSetupActions()`
  （①の新設「● 記録...」ボタン）が`InputRecorder::start()`を呼び、
  `onSetupActionRecorded()`が届いた座標を①で選択中の対象ウィンドウの
  現在位置基準に変換して`m_setupActions`に逐次追加する（既存の「追加...」と
  同じ`SetupAction`の型にそのまま入るので、記録後も通常通り「編集...」で
  内容を調整できる）。Escキーが押されると自動的に記録を終了する
  （`onRecordingFinished()`）。記録中は①②③の他の操作を`setControlsEnabled
  (false)`でロックし、`RecordingIndicatorPanel`という新設の常に最前面の
  小さいパネル（「■ 記録終了」ボタン・記録件数表示）を表示して、対象アプリ側に
  フォーカスが移っていてもいつでも終了できるようにした（`StopPanel`と同じ
  設計）。
- **「システム全体で観測する」の実装方法（重要）**: 対象アプリ・ダイアログへの
  入力を実際に record 中も普通に届かせる必要がある（さもないと、ダイアログの
  ボタンを押す操作そのものが記録できても、ダイアログ自体が反応しなくなって
  しまい本末転倒）。そのため、入力を横取りする「グラブ」ではなく、素通しで
  observe できる仕組みを使った。
  - Linux: XInput2の**raw**イベント（`XI_RawButtonPress/Release`,
    `XI_RawKeyPress/Release`）を、ルートウィンドウに対して
    `XIAllMasterDevices`で購読する（`InputRecorder_linux.cpp`）。rawイベントは
    通常のウィンドウ階層・グラブの状態に関わらず購読している全クライアントに
    配送される一方、実際のイベント配送（対象ウィンドウ/ダイアログへの入力）を
    一切妨げないため、要件を満たす。
  - macOS: `CGEventTapCreate`を`kCGEventTapOptionListenOnly`（読み取り専用）
    で設置する、Linuxの raw イベントに相当する仕組み（`InputRecorder_mac.mm`）。
    実機での検証はできていない（SPEC.md 8章参照）。
  - クリック/ドラッグの判定、テキスト入力のバッファリング（1文字ずつではなく
    「まとめて入力した文字列」として`TypeText`にする）、Ctrl/Alt/Meta等の
    修飾キー追跡、Escキー検知はプラットフォーム非依存の共通ロジック
    （`InputRecorder.cpp`）にまとめ、Linux/macOSそれぞれのバックエンドは
    「生のボタン/キーイベントをQt::MouseButton/Qt::Keyに変換して渡す」薄い層に
    留めた。
  - スコープ: 記録されたクリックは常に単発`Click`として記録する（意図的な
    ダブルクリックか偶然の素早い2連クリックかをタイミングだけで確実に区別する
    のは信頼性に欠けるため）。非ASCII文字・デッドキー入力は対象外（この
    アプリの既存の文字入力機能自体がASCII前提のため、スコープを広げていない）。
- **【発見・回避】少なくとも1つの仮想Xサーバー環境でXTestで合成したクリックの
  `XI_RawButtonRelease`が配送されない問題**: 実機（Xvfb + openbox）での動作
  確認中、`xdotool click`等で合成したクリックについて、押下（RawButtonPress）は
  正しく届く一方で離す方（RawButtonRelease）が一度も届かないことを、専用の
  最小テストプログラムで切り分けて確認した（キー入力の押下/解放は両方とも
  正しく届いており、ボタンの解放に限られた挙動）。実際のウィンドウへの通常の
  ButtonPress/ButtonRelease配送自体は正しく行われている（このアプリの他の
  自動操作機能が問題なく動いていることからも分かる）ため、Xvfbのraw
  イベント配送パイプライン側の制約と見られる。原因が実機の物理入力でも
  同様に起きるのか（起きないと想定しているが未確認）を問わず頑健にするため、
  ボタン押下を検知した際に`XQueryPointer`でボタン状態を40ms間隔でポーリングし、
  ビットが落ちた時点で自前で解放を検出するフォールバックを追加した
  （raw解放イベントが正常に届く環境ではそちらが先に処理し、フォールバックは
  何もしない二重処理防止つき）。
  - **（v0.66で追加確認）能動グラブ保持中はraw押下イベント自体も配送されない**:
    上記の「合成クリックのRawButtonReleaseが来ない」不具合よりさらに広い版として、
    本物の`QMenu`（ネイティブなX11の能動グラブを保持するポップアップ）が開いている
    間にその上で合成クリックを行うと、対象アプリ自身はクリックを正しく受け取って
    処理する（対象アプリ側のログで確認済み）にもかかわらず、このXvfbでは
    `XI_RawButtonPress`/`Release`のどちらも一切配送されないことを実機テストで
    確認した。このテスト環境固有のXvfbの制約であり、実際の人間の操作（本物の
    マウスハードウェア由来のイベント）では発生しないと考えられる（詳細は6.13節の
    v0.66を参照）。
- 実機（Xvfb + openbox）で、①対象選択で対象アプリを選択した状態から「●
  記録...」を押すと記録中パネルが表示されること、対象アプリ上でのクリック・
  ドラッグ・右クリックがそれぞれ正しい種別・座標（対象ウィンドウ基準）で
  ①の一覧に追加されること、続けて文字列を入力すると1つの`TypeText`として、
  Tab/Returnキーがそれぞれ`KeyPress`として記録されること、Escキーで記録が
  終了し「記録を終了しました（Escキー）。記録件数: 6」のようにログへ出力
  されることを確認した。-Wall -Wextra -Wpedantic付きのクリーンビルドで
  警告0件を維持。

起動時セットアップに全消去のボタンを追加してください。

実装状況（「全消去」ボタン、v0.64）: ②ステップ構成の「すべて削除」
（`m_clearStepsButton`/`onClearSteps()`）と全く同じ方式で、①起動時セットアップの
「↑ 上へ」「↓ 下へ」の並びに「全消去」ボタンを追加した（`m_clearSetupActionsButton`/
`onClearSetupActions()`）。確認ダイアログは出さず、押すと即座に`m_setupActions`を
空にして一覧を再描画する（「すべて削除」と挙動を揃えた）。実機（Xvfb + openbox）で、
待機アクションを1件追加した状態で「全消去」を押すと一覧が即座に空になることを確認した。
-Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。

起動時セットアップの操作にホイール操作や特定のメニューの選択を操作として追加してください。

実装状況（起動時セットアップに「ホイールスクロール」「メニュー項目を選択」を追加、
v0.65）: `SetupActionType`に`Scroll`/`MenuSelect`の2種類を追加した。

- **ホイールスクロール**（`Scroll`）: 対象ウィンドウ基準の位置（既存のクリック等と
  同じ`PointPickerOverlay`で指定）・方向（上/下/左/右）・量（ホイールの「目盛り」数）を
  指定する。実行時は`SetupAction::scrollDx`/`scrollDy`として`RandomActionEngine::
  trySetupAction`から`PlatformAutomation::scroll()`をそのまま呼ぶ（符号の意味は
  `RegionStep`のScrollUp/ScrollDown/ScrollHorizontalと同じ規約 -- 正のdyが上、負のdyが
  下、正のdxが左、負のdxが右。`Automation_linux.cpp`のX11ホイールボタン6/7の割り当てに
  由来する規約で、`SetupActionEditorDialog`の「方向」コンボはこの符号変換を内部で
  隠蔽している）。
- **メニュー項目を選択**（`MenuSelect`）: 対象ウィンドウ基準の右クリック位置と、開いた
  メニューから選ぶ1項目を「項目名で指定」または「上から何番目かで指定」のいずれかで
  指定する（`ContextMenuSelectionMode`を流用）。既存の`RightClick`種別は開いたメニューを
  常に閉じるだけ（決定論的なセットアップの性質上、ランダムな項目選択は行わない設計
  だった）なのに対し、`MenuSelect`は常に同じ1項目を選ぶ専用の種別として新設した。
  実行時（`trySetupAction`）は右クリック→`handlePossibleContextMenu`と同様の
  待機・フォーカス再確認を行った上で、`PlatformAutomation::clickContextMenuItem`/
  `clickContextMenuItemAt`で指定項目をクリックする。指定した項目が見つからなかった
  場合（対象アプリのメニュー内容が実行ごとにわずかに変わる場合を想定。例:
  クリップボードが空の時だけ「貼り付け」が無効化される等）は、セットアップ全体を
  失敗させず、メニューを閉じてログに記録するだけに留める（フェイルソフト）。
- 両種別とも`SetupActionEditorDialog`に専用の入力ページを追加し、位置の選択は
  既存のクリック系種別と同じ`m_point`/`m_pointPicked`を共有する（ボタンを押す先の
  ページが違うだけで、書き込み先の内部状態は同じ）。`TestConfigJson`のJSON
  シリアライズ・`MainWindow::describeSetupAction`の一覧表示にも対応した。
- 実機（Xvfb + openbox）で、①起動時セットアップの「追加...」→「種類」に
  「ホイールスクロール」「メニュー項目を選択」が選択肢として追加され、それぞれの
  専用ページ（位置・方向・量／位置・項目名またはindex）が正しく表示・切り替わる
  ことを確認した。ホイールスクロールを実際に追加し、①一覧に
  「1: スクロール (544, 470) dx=0 dy=3」のように正しい符号・値で表示されることを
  確認した（TestTargetの縦スクロール領域上で「上」方向・量3を選択）。メニュー項目
  選択ページでは「項目名で指定」がデフォルトで選ばれ、対応する入力欄が有効化される
  ことも確認した。-Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。

起動時セットアップの記録機能にホイール操作や特定のメニューの選択が記録されるようにして
ください。メニューの選択は選択肢の文字列を指定する方法と上から何番目かを指定する方法の
二つを記録終了後に選択できるようにしてください。

実装状況（記録機能をホイールスクロール・メニュー項目選択に対応、v0.66）:
`InputRecorder`（プラットフォーム共通部分は`InputRecorder.cpp`、Linux/macOS個別の
フック部分は`InputRecorder_linux.cpp`/`InputRecorder_mac.mm`）を拡張した。

- **ホイールスクロール**: 連続するホイール1notch分の`XI_RawButtonPress`
  （X11のボタン4/5/6/7、`Automation_linux.cpp`のscroll()が合成する時と同じ
  マッピング）/`kCGEventScrollWheel`（macOS）を、他の入力が割り込むまで
  `m_wheelAccumDx`/`Dy`に貯め続けてから1つの`Scroll`アクションとして確定する
  （`m_textBuffer`の連続文字入力をまとめる既存方式と同じパターン）。
- **メニュー項目選択**: 右クリックの離すイベントを即座に`RightClick`として確定
  せず、`kMenuSelectionGraceMs`（600ms）の猶予時間だけ`QTimer`で保留する。猶予中に
  左クリックが来たら、`PlatformAutomation::accessibleNameAtPoint()`（AT-SPI/
  Accessibility越しのベストエフォートなポイント直下の名前取得）でクリック位置の
  項目名を調べ、取得できれば右クリック位置・項目名を1件の`MenuSelect`アクションに
  まとめる。項目名が取得できない場合（AT-SPI/Accessibilityが使えない環境、対象
  アプリがアクセシビリティ未対応、等）や、猶予時間内に何も来ない場合は、フェイル
  ソフトで従来通り単なる`RightClick`として記録し、後続のクリックが来ていれば
  それも普通の`Click`/`Drag`として取りこぼさず記録を続ける。
- 実機（Xvfb + openbox）で、①右クリック→フェイルソフトパス（項目名が取得できない
  ケース）が正しく機能することを確認した: プレーンなボタンへの右クリック→別ボタンへの
  左クリックで、`右クリック`と`クリック`の2件が別々のアクションとして正しい座標で
  記録され、猶予時間（600ms）を待たずに素早く連続してもタイミングの問題は起きない
  ことを確認した。
- 一方、AT-SPI経由で項目名が実際に取得できて`MenuSelect`として1件にまとまる方の
  パス（本物のコンテキストメニューの項目を選ぶケース）は、このテスト環境では
  以下の2つの理由により実機確認ができなかった:
  1. このプロジェクトのXvfb+openboxテスト環境には`DBUS_SESSION_BUS_ADDRESS`が
     設定されておらず（デスクトップセッション自体が存在しないヘッドレス環境の
     ため）、`Automation_linux.cpp`の`atspiUsable()`が常にfalseを返す設計に
     なっている（AT-SPIバスに接続できない状態で`atspi_init()`を呼ぶと
     `g_error()`で即座にプロセスごとabortしてしまうことが確認済みのため、
     その手前で意図的に短絡させている。8章参照）。そのため
     `accessibleNameAtPoint()`はこの環境では常に空文字列を返し、常に
     フェイルソフト側の経路しか通らない。
  2. AT-SPIの問題とは独立に、本物の`QMenu`（ネイティブなX11の能動グラブを
     保持するポップアップ）が開いている間に`XTestFakeButtonEvent`で
     合成したクリックをその上で行うと、対象アプリ自身はクリックを正しく
     受け取って処理する（TestTarget自身のログで確認済み）にもかかわらず、
     このXvfbでは`XI_RawButtonPress`/`Release`が一切配信されないことを
     直接の実機テストで確認した（既存の8章記載の「合成クリックで
     `XI_RawButtonRelease`が来ない」不具合の、グラブが絡んだ場合のさらに
     広い版）。これは`InputRecorder`側の問題ではなく、このテスト環境固有の
     Xvfbの制約であり、実際の人間の操作（本物のマウスハードウェア由来の
     イベント）やAT-SPIバスが動いている通常のLinuxデスクトップ環境では
     発生しないと考えられる。
  この2点はいずれもこのプロジェクトの既存のテスト環境の既知の制約
  （8章）と同じ性質のものであり、`accessibleNameAtPoint()`が空文字列を
  返した場合のフェイルソフト経路は実機で確認済みであることから、
  `MenuSelect`側の実装自体に欠陥があるとは考えにくいと判断した。
  -Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。

設定した起動時セットアップが実際に問題なく動くかどうかの確認するために、起動時セットアップを
実行する機能が欲しいです。

実装状況（「起動時セットアップを試す」ボタン、v0.67）: ①起動時セットアップの
「● 記録...」の下に「▶ 起動時セットアップを試す」ボタンを追加した。押すと、①で
選択中の対象ウィンドウに対して、この一覧に登録済みのセットアップだけを実行し、
完了次第自動的に停止する（②のステップ構成は一切実行しない -- 設定されていなくても
構わない）。

- `RandomActionEngine`に`startSetupOnly(const TestConfig &config)`を追加した。
  内部的には既存の`start()`をそのまま呼んだ直後に`m_setupOnlyRun`フラグを立てる
  だけで、通常の起動時セットアップ実行フェーズ（`performSetupAction()`）自体は
  完全に共有している。唯一の違いは、セットアップの最後の1件まで終わった時点で
  `m_setupOnlyRun`が立っていれば、通常時のように②のランダム操作ループへ
  引き継ぐ代わりに、そのまま`doStop()`で停止する分岐を追加しただけである
  （②が空でも「ステップが設定されていません」のような無関係な停止理由には
  ならない）。
- `MainWindow`側は既存の`buildConfigFromUi()`（②に最低1ステップの設定を
  必須とする）とは別に、対象ウィンドウの選択と①のセットアップが1件以上
  あることだけを検証する`buildSetupOnlyConfigFromUi()`を新設し、
  `onTestSetupActions()`から使う。開始前の安全確認（対象ウィンドウの
  frontmost確認）は`beginRun()`と同じロジックを流用した。
- この実行は`RandomActionEngine::finished()`/`summaryReady()`を通常の
  テスト実行と共有しているため、既存の①②③無効化・■停止ボタン・
  実行結果サマリー表示・異常終了時のクラッシュダイアログ等の仕組みを
  そのまま再利用できる。一方で、②のステップ構成に基づくクラッシュ率統計
  （③統計、`TestStatistics::addRun()`）と異常時のプリセット/操作領域画像の
  自動保存は、この実行では意味を持たない（②を実行していないため
  `crashStepIndex`等が対応しない）ため、`MainWindow`側に一時フラグ
  （`m_setupOnlyTestRun`）を設け、この実行の間だけ両方をスキップするように
  した。`RandomActionEngine::doStop()`内部のスクリーンショット/画面録画/
  クラッシュダンプ収集自体（`captureAnomalyArtifacts()`）はこのフラグと
  無関係にそのまま動作するため、セットアップ実行中に異常が起きても
  診断情報が失われることはない。
- 実機（Xvfb + openbox）で、①未設定（起動時セットアップが空）の状態で押すと
  「起動時セットアップが設定されていません。「追加...」または「記録...」で
  セットアップを作成してください。」という設定エラーダイアログが出ることを
  確認した。続けてTestTargetのボタンをクリックする`クリック`セットアップを
  1件登録してから押すと、TestTarget側のログに実際にクリックが届いたことが
  記録され（「ボタン5: 左クリック」、カウンタも左:1に増加）、①②③が
  無効化されたあとステータス欄に「停止: 起動時セットアップの実行確認が
  完了しました（1件）」と表示されて自動的に①②③が再度有効化されることを
  確認した。③の「このテスト設定での実行記録」欄が実行前後で変化しない
  （＝クラッシュ率統計に混入しない）ことも確認した。-Wall -Wextra
  -Wpedantic付きのクリーンビルドで警告0件を維持。

### 6.14 CLI/ヘッドレス実行モード (v0.69)

設定した起動時セットアップや操作領域、ステップ構成などの情報を保存できるようして
ください。その上でGUIを立ち上げなくてもターミナル実行で保存した手順を引数として
与えることでテストを実行できるようにしてください。その際連続実行の回数も引数として
設定できるようにしてください。

実装状況（v0.69）: テスト設定のプリセット保存/読み込み自体は既にJSON形式で対応済み
（6.1節v0.51、10章参照）だったため、今回追加したのは「GUIを一切表示せずターミナル
から直接実行する」経路。`EnduranceTestGUI --run <preset.json> [--repeat N]`という
コマンドライン引数を新設した（`QCommandLineParser`、`--help`で使い方を表示）。

- `--run`が指定されると、`MainWindow`は生成するが`show()`を一切呼ばず（`main.cpp`）、
  代わりに`MainWindow::runHeadless(presetPath, repeatCount)`を呼ぶ。この関数は
  指定されたプリセットJSONを読み込み（`loadPresetFromPath()` -- 既存の
  `onLoadPreset()`からQFileDialog依存部分を除いて切り出したもの）、
  `--repeat`（省略時は1）を①の「連続実行回数」に設定した上で、「⟳ 連続実行」
  （`onContinuousRun()`）をそのまま呼ぶ。つまりヘッドレスモードは常に
  「対象アプリを一度キル→起動コマンドで新規起動→起動待ち→実行」の
  サイクルを指定回数繰り返す、`連続実行`ボタンを押すのと全く同じ経路を通る
  （プリセットに`targetLaunchCommand`が設定されていない場合はエラーで即終了する
  -- 人間が手で再起動して次回を始めるという選択肢がヘッドレス実行には無いため）。
- ①②③のパネルは（一度も表示されないため）人が見ることはないが、
  `MainWindow::appendLog()`がヘッドレスモード中は同じ内容をそのまま標準出力
  （`stdout`）へもミラーするようにしたため、進捗・各操作のログ・実行結果サマリーは
  すべて端末に流れる。読み込み失敗など、Qtのイベントループを開始する前に判明する
  エラーは標準エラー出力（`stderr`）に出して終了コード1で即座に終了する。
- 全ての繰り返しが完了した時点、またはいずれかの繰り返しで（権限未確認・設定
  エラー・安全確認失敗などにより）`連続実行`自体が続行不能になった時点で、
  自動的にプロセスを終了する（`MainWindow::checkHeadlessCompletion()` --
  `continueBatchIfNeeded()`の「全回終了」分岐と、`beginRun()`の非対話時
  失敗3箇所から呼ばれる）。終了コードは、実行した中のいずれか1回でも
  `RunSummary::anomaly`（クラッシュ・安全停止等の異常）が発生していれば1、
  1回も無ければ0とし、シェルスクリプト・CIパイプライン等からの成否判定に使える
  ようにした。
- 実機（Xvfb + openbox）で、GUIで①〜③を設定（対象自動起動コマンド・1ステップ・
  最大実行回数10）してプリセットを保存 → GUIを終了 → 別プロセスとして
  `--run <preset.json> --repeat 2`で起動、を確認した。ウィンドウが一切表示されない
  まま、標準出力に「連続実行を開始します」「対象アプリ（PID ...）が残っているため
  終了します」「...新しいインスタンスを起動します」「...起動を検知しました」
  「1/2回目を開始します」に続けて実際のクリック操作ログ（対象ウィンドウ相対座標
  付き）と実行結果サマリーが流れ、2回目も同様に実行された後「連続実行が完了しました
  （2/2回）」と表示されてプロセスが正常終了（終了コード0）することを確認した。
  TestTarget自身も実際に対象アプリとして起動・操作され、クリックが届いていることを
  ログで確認した。存在しないプリセットパスを指定した場合は、ウィンドウも
  Qtイベントループも一切開始せずに即座にエラーメッセージを標準エラー出力へ出し、
  終了コード1で終了することも確認した。-Wall -Wextra -Wpedantic付きの
  クリーンビルドで警告0件を維持。

## 7. 動作確認用アプリ (TestTarget)

`testtarget/` にある簡易Qtアプリ。EnduranceTestGUI が実際に他プロセスへ入力を
送信できているかを目視確認するための道具。

- 3x3のボタン（各ボタンごとに左/右/二重クリック回数を表示。`enableDoubleClick`/
  `doubleClickWeight`の確認用）
- チェックボックス（切替回数を表示）
- スライダー（値と変化回数を表示、ドラッグ確認用）
- テキスト入力欄（受信したキー入力とその文字を表示。Tab/Return/Escape/Backspace/Delete/
  矢印キーなど`text()`が空になる特殊キーも`QKeySequence`名で表示。Ctrl+C/V/A/Zは
  「ショートカット受信」として別集計され、`shortcutSequences`の確認に使う）
- 縦スクロール領域と横スクロール領域（それぞれ回数を別集計。`enableScrollHorizontal`/
  `scrollHorizontalWeight`の確認用）
- 右クリックメニュー対象ボタン: 「項目1」「項目2」「項目3」「キャンセル」を持つ本物の
  `QMenu`を表示し、選択された項目ごとに回数を集計する。`enableContextMenuSelection`と
  `contextMenuItemNames`（例:「項目1」「項目2」）または`contextMenuIndices`の動作確認用
- ウインドウ位置・サイズを常時表示するラベルと、最小化/最大化の検出回数
  （`windowOpMove`/`Resize`/`Minimize`/`Maximize`の確認用）
- 全体統計（起動からの経過時間・総クリック数・キー入力数・ショートカット数・
  縦横スクロール数・最小化/最大化回数）
- 「カウンタをリセット」ボタン
- 「終了（クリック厳禁）」ボタン: 誤操作防止のため、クリックすると確認ダイアログが出て
  「はい」を押すまで実際には終了しない。除外(マスク)領域機能の動作確認に使う想定
  （このボタンを除外領域で覆い、ランダムクリックが当たらないことを確認する）。

## 8. 既知の制約・今後の検討事項

- この開発機では対話的なGUI操作の自動化・検証が一切できないことを確認済み。`osascript`
  経由のSystem Eventsによる UI操作は権限エラー（-1743、Automation権限未許可）で失敗し、
  独立にコンパイルした`AXIsProcessTrusted()`呼び出しの結果も`0`（Accessibility権限なし）
  だった。したがって、このシェル環境からは（EnduranceTestGUI自身がmacOS上で他プロセスへ
  合成入力を送るのと同じ理由で）マウス/キーボード操作の自動送信自体ができず、ビルド成功と
  プロセスの起動・生存確認（クラッシュしていないこと）以上の動作確認ができない。実際に
  ステップを設定してテストを開始し、対象アプリへ操作が届くかどうかの確認は毎回ユーザー側の
  実機でのみ可能。
- 名前付き操作領域（固定矩形）はスクリーン絶対座標で保存されるため、対象ウィンドウが
  移動すると追従しない（「対象GUIの全領域」を選んだステップのみ追従する）。
- ドラッグ操作の終点は除外領域チェックをしていない。
- 「対象GUIの全領域」を使うステップには除外(マスク)領域を設定できない（除外領域は
  名前付き操作領域のみが持てる）。この領域内の一部を除外したい場合は、固定矩形の
  操作領域を登録してそれを参照するステップに変更する必要がある。
- 操作領域の削除は参照中のステップがあると拒否されるが、名前変更時は参照している
  ステップ側を自動更新するため、同時に複数の場所（`MainWindow`とバックグラウンドの
  `RandomActionEngine`実行中の設定コピー）を編集するような操作は想定していない
  （実行中は①②③のパネルが無効化されるため通常は問題にならない）。
- ウィンドウ操作アクション（移動/リサイズ/最小化/最大化）はmacOS/Linuxとも実装したが、
  この開発機では対話的なGUI操作を伴う動作確認ができないため実機検証は未実施。特に
  macOS側のAXUIElement特定（`copyAXWindow`、現在のboundsとの一致で突き合わせる方式）は、
  同一の位置・サイズを持つ複数ウィンドウが存在する場合に誤って別ウィンドウを操作する
  可能性が理論上ある（通常は起こりにくいが、既知の制約として明記する）。
- 乱数シードを指定した場合の再現性は、操作列（クリック座標・キー・タイミング等）の
  再現に限られる。対象アプリ自身の内部状態やOS/デスクトップ環境側のタイミング依存の
  挙動（アニメーション、ネットワーク応答等）までは再現しないため、完全な再現を保証する
  ものではない。
- リソース使用量（CPU/メモリ）のサンプリング間隔は5秒固定（UIから変更不可）。
- ウィンドウ一覧のタイトルは、macOSでは画面収録権限がない場合に空になることがある
  （アプリ名・PIDでの識別は可能）。
- Linuxではウィンドウ間の物理的な入力ルーティングはカーソル位置とフォーカスに依存する
  ため、キー入力を確実に対象へ届けるには対象ウィンドウがアクティブである必要がある
  （`keepTargetActive` オプションで対応）。
- **（v0.55で解消）** `RegionSelectorOverlay`・`PointPickerOverlay`・
  `RegionHighlightOverlay`（①操作領域一覧選択時のハイライト表示、6.3節）は
  v0.54まで`Qt::WA_TranslucentBackground`で半透明表示を行っていたが、Linux/X11では
  コンポジット（compositing）ウィンドウマネージャが動作していない環境（`openbox`単体
  など）ではこの属性が効かず画面全体が不透明な黒塗りになり、対象アプリはおろか
  操作領域設定ダイアログ自体まで見えなくなって操作不能になる不具合があった。
  v0.55でコンポジットに依存しない方式（オーバーレイを表示する直前に画面全体の
  スクリーンショットを取得し、それを不透明な背景として自前で描画した上に矩形/
  ラベル等を重ねて描く）に変更し、コンポジットの有無によらず同じ見た目で
  「素通し」になるよう修正した。詳細は6.13節のv0.55を参照。
- 右クリック時のメニュー項目選択は実験的機能で、このセッションでは画面操作を伴う
  対話的な動作確認ができない環境だったため、実機での動作検証は未実施（6.5節参照）。
  Linux側はAT-SPI開発ヘッダが無い環境ではビルド時に自動でno-opにフォールバックする。
- メニュー項目の「上から何番目かで指定」モードは、メニューの項目数・並びが状況依存で
  変わる場合に意図しない項目を選んでしまうリスクがある（8章の一般的注意点として、
  基本的には項目名指定の方を推奨）。
- Windows対応は未実装（`src/platform/` に新しいバックエンドを追加する形で拡張可能）。
- 複数ディスプレイ環境でのオーバーレイ領域選択は、全ディスプレイを結合した仮想デスクトップ
  座標系で動作する想定（実機での複数モニタ検証は未実施）。
- グローバルホットキー（Ctrl+Alt+Shift+Escape）による緊急停止をv0.41で実装済み
  （10章参照）。固定の組み合わせのみでユーザー設定不可、他アプリ/デスクトップ環境が
  同じ組み合わせを既に使っている場合は登録に失敗し（ログに表示）、その場合はフローティング
  パネルのボタンのみが停止手段となる。
- 対象アプリの無応答（ハング）検知（v0.41、10章）はLinuxのみ対応（`_NET_WM_PING`）。
  macOSは非公開APIへの依存を避けるため意図的に未対応（`Unsupported`を返し続け、
  ハング検知はスキップされる）。またLinux実装も、`_NET_WM_PING`に対応すると
  宣言しながら実際には一切応答しないウィンドウ（実機で確認済み、6.10/10節参照）を
  「一度も応答しない」ものとして自動的にハング検知を無効化する設計になっているため、
  そもそも一度も正常に応答しないツールキット/実装に対しては実質的にこの機能が
  効かない（誤検知よりは安全側に倒した結果）。応答が遅い時の操作間隔自動延長
  （`autoSlowdownEnabled`、v0.71、6.7節）も同じ応答確認を土台にしているため、同じ制約を
  そのまま引き継ぐ。
- Qt5でのビルドは未確認（開発機にQt6のみが入っているため。コード上はQt5/6両対応の分岐を
  入れている）。
- Linux/AT-SPIでのメニュー項目選択（6.5節）は、対象アプリのpidにスコープされておらず
  デスクトップ全体を探索する。誤って他アプリのメニューを操作してしまわないための主な
  防御は、メニュー選択直前に`activeProcessPid()`でアクティブなプロセスが対象と一致するか
  確認すること（6.7節）だが、その確認からメニュー探索・クリックまでの間にごくわずかな
  タイムウィンドウが残る。macOS側は最前面ウィンドウの所有pidを直接確認しているため
  この種のギャップはない。
- Linuxでの基本動作（ウィンドウ列挙・対象選択・クリックステップの実行・シーケンス上限
  停止・クラッシュ/誤操作時のfail-closed安全停止・異常時スクリーンショット保存）は
  v0.40でEWMH対応ウィンドウマネージャ（`openbox`）上で実機確認済み（9章参照）。
  ただしドラッグ・スクロール・キー入力/ショートカット・ウィンドウ操作・右クリック
  メニュー項目選択（AT-SPI）・除外領域・一時停止/再開・複数モニタは未検証のまま残って
  いる。また、6.7節の安全チェック機能は`_NET_CLIENT_LIST_STACKING`・`_NET_ACTIVE_WINDOW`
  というEWMHヒントに依存するため、これらを実装していない一部のウィンドウマネージャ
  （タイル型WM等）では、安全確認が常に失敗しテスト開始前のプリフライトチェックで
  即座に拒否される可能性がある（この場合はfail-closed設計により意図的に動作しない）。
- （v0.46）Linux側のAT-SPI呼び出し（クリック地点のUI部品名取得・右クリックメニュー
  項目選択の両方が内部で使用）は、`DBUS_SESSION_BUS_ADDRESS`環境変数が設定されている
  ことを事前チェックしてから呼ぶガード（`atspiUsable()`）を入れているが、この環境変数の
  存在は「AT-SPI呼び出しが安全である」ための必要条件であって十分条件ではない。
  変数が設定されていても、そのバス上にAT-SPIレジストリ（`org.a11y.Bus`等）自体が
  実際には起動していない場合、AT-SPIのライブラリ内部エラー（GLibの
  `G_LOG_LEVEL_ERROR`、常にfatalでマスク不可）によりプロセスがabortする可能性が理論上
  残る（詳細な発見の経緯は6.8節参照）。通常のデスクトップ環境（GNOME/KDE等）では
  アクセシビリティサービスが標準で起動しているためこのリスクは小さいが、最小構成の
  Linux環境（今回のテストコンテナのような、D-Busセッションバス自体が無い/一部のみの
  環境）を対象にテストする場合は注意が必要。
- （v0.46）新設の`PlatformAutomation::accessibleNameAtPoint`/`findRecentCrashReport`の
  macOS実装（`Automation_mac.mm`）は、他のmacOS専用コードと同様にこの開発機では
  対話的な動作確認ができないため未検証。

### 8.1 耐久テストのバリエーション拡張（v0.16で全て実装済み）
6.10節に詳細を記載。以下は当初提案として挙げていたが、全て実装済みになった:
ウィンドウ操作系アクション、名前付きキーのランダム送信、ダブルクリック、操作種別ごとの
発生頻度（重み）調整、乱数シードの記録・指定、対象プロセスのCPU/メモリ使用量の記録、
一時停止/再開、異常検知時のスクリーンショット保存。

## 9. 変更履歴

- v0.1 (プロトタイプ): 初版。macOS/Linux両対応、複数領域+除外マスク、
  クリック/ドラッグ/キー/スクロールの4操作種別、緊急停止パネル、動作確認用TestTargetアプリを実装。
- v0.2: Qt5/Qt6両対応、領域ごとに操作種別・回数を指定する「ステップ構成」への刷新
  （旧来のグローバルな「含める領域＋操作種別」設定を廃止）、ドラッグ方向指定
  （ランダム/上/下/左/右）、ショートカットキー操作、対象プロセスのクラッシュ検知を実装。
- v0.3: ステップ全体（シーケンス）の繰り返し回数の上限設定、右クリック時のメニュー項目
  選択（実験的機能、macOS Accessibility API / Linux AT-SPI）を実装。
- v0.4: 1〜8章を現在の実装（ステップ構成モデル）に合わせて全面的に整理
  （本ドキュメント更新のみ、コード変更なし）。
- v0.5: 右クリック時のメニュー項目選択に「上から何番目かで指定」モードを追加
  （`ContextMenuSelectionMode`、既存の項目名指定と選択可能）。項目名指定はメニューの
  項目数・並びが変わっても安全に同じ意味の項目を選べる一方、番号指定は項目名を
  調べずに手軽に設定できる代わり、メニューが状況依存で変わる場合は意図しない項目を
  選ぶリスクがある（UIで両モードを切替可能にし、注意文言も表示）。
  `PlatformAutomation::clickContextMenuItemAt(int index)` を新設し、macOS/Linuxの
  両バックエンドに実装。
- v0.6: GUIを3カラムレイアウト（①対象選択/②ステップ構成/③操作パラメータ）に刷新し
  （6.9節）、操作パラメータをステップ単位でデフォルト/カスタムに一括切替できるように
  変更。`ActionParams`構造体を新設して`TestConfig::defaultActionParams`と
  `RegionStep::customActionParams`に分離し、`RegionStep::useDefaultActionParams`で
  どちらを使うか切り替える（操作種別ごとの個別切替は行わず、ステップ単位でまとめて
  切り替える設計 — 詳細は6.4節）。パラメータ編集UIは`ActionParamsEditor`として
  切り出し、③パネルで②パネルの選択中ステップに連動するマスター・ディテールUIとして
  再利用。`RandomActionEngine`は`effectiveParams(step)`でステップごとの有効な
  `ActionParams`を解決するように変更。
- v0.7: macOSでのアクセシビリティ権限が認識されない不具合の調査・修正。
  - `TestTarget`にバンドルID（`com.asobi.testtarget`）を付与するInfo.plist.inを新設し、
    `EnduranceTestGUI`/`TestTarget`とも自動でad-hoc署名するビルドステップを追加
    （未署名/バンドルID未設定だとTCCが正しく機能しないため）。
  - `MainWindow`がアプリのフォアグラウンド復帰時（`QGuiApplication::applicationStateChanged`）
    に権限状態を自動再チェックするように変更（従来は起動時と「更新」ボタン押下時のみ）。
  - 副次的な対策として、`~/Qt/<version>/macos`（Qt公式インストーラ、universal binary）を
    優先的に検出するようCMakeLists.txtを変更し、Apple SiliconでRosetta 2経由の
    x86_64ではなくネイティブarm64ビルドに切り替えられるようにした（4節参照）。
    ただしこの時点ではまだ問題は再現し、根本原因ではなかった（v0.8参照）。
- v0.8: 【根本原因判明】macOSのアクセシビリティ権限がターミナルから起動すると反映されない
  問題の実際の原因を特定した。TCCの`log stream`を直接観測した結果、ターミナル
  （Terminal.app等）から起動したプロセスは「responsible process（責任元プロセス）」が
  起動元のターミナルアプリ自身に委譲されることが判明（`open`コマンド経由でも同様）。
  EnduranceTestGUI自体をアクセシビリティ一覧で許可しても、ターミナルアプリが許可されて
  いなければ拒否される。Finderからのダブルクリック起動に切り替えることで解決を確認。
  詳細と対処法は5節に記載。コード変更なし（原因調査とドキュメント化のみ）。
- v0.9: 除外(マスク)領域を全ステップ共通のグローバル設定から、ステップ（＝操作領域）ごとの
  設定に変更した（6.3節）。`TestConfig::excludeRegions`を廃止し、`RegionStep::excludeRegions`
  を新設。`RandomActionEngine::pointExcluded`/`pickRandomPoint`は実行中のステップの
  `excludeRegions`のみを参照するように変更。UIは`MainWindow`の①パネルにあった
  グローバルな「除外(マスク)領域」グループを廃止し、`StepEditorDialog`でステップの領域を
  描画した直後にそのステップ専用の除外領域を描画・一覧・削除できるようにした（一覧は
  「領域Ν」「除外Ν」の自動ラベル付き）。また操作間隔のデフォルト値を200〜1000msから
  20〜100msに変更（`TestConfig`のデフォルト値とUIの初期値の両方）。
- v0.10: 【バグ修正】macOSで領域選択オーバーレイを開くと画面全体が不透明な黒で覆われ、
  対象アプリのウィンドウが見えなくなる不具合を修正した。原因は`RegionSelectorOverlay::run`
  が`showFullScreen()`を呼んでいたこと — macOSではネイティブのフルスクリーン（専用Space）
  に切り替わり、他の全ウィンドウ（対象アプリを含む）が隠れる上、半透明背景がフルスクリーン
  モードでは正しく機能せず不透明に見える。`showFullScreen()`をやめ、コンストラクタで既に
  設定している仮想デスクトップ全体のジオメトリのまま通常の`show()`で表示するように変更
  （枠なし・常に最前面のウィンドウとして表示されるため、Spaceを切り替えずに済み、下にある
  対象アプリのウィンドウが半透明オーバーレイ越しに見えるようになる）。Linux/X11でも同じ
  コードパスを通るため同時に修正されるが、コンポジット未対応の環境では別要因（8章参照）で
  透過が効かない可能性があり、実機未検証。
- v0.11: 【バグ修正・実機未検証】領域選択オーバーレイを閉じた後、アプリが操作不能になる
  不具合に対する修正。`StepEditorDialog`はQDialogの`exec()`（macOSではネイティブの
  アプリケーションモーダルセッション）で表示しており、その中で`RegionSelectorOverlay`
  （無関係な別の最上位ウィジェット）を表示する際に、従来は呼び出し元ダイアログを
  `hide()`→`show()`していた。しかし`exec()`のモーダルセッションは`hide()`しても
  終了しないため、モーダルセッション実行中に無関係な別ウィンドウを出し入れする形になり、
  macOSのウィンドウサーバ側の状態が壊れて以降操作を受け付けなくなっていたと推測される。
  対策として (1) `StepEditorDialog`側の`hide()`/`show()`呼び出しを廃止し、
  (2) `RegionSelectorOverlay`自体を`Qt::ApplicationModal`にして、表示中は呼び出し元の
  モーダルセッションと衝突せずそれ自体がアクティブなモーダル面になるようにした。
  この開発機では対話的なGUI操作を伴う検証ができないため、実機での確認が必要。
- v0.12: 最大実行回数とステップ全体（シーケンス）の繰り返し回数について、「無制限」を
  選択できないようにした（6.6節）。放置しても永久に動き続けるテストが誤って構成される
  ことを防ぐための安全策。`TestConfig`のデフォルト値を`maxIterations=1000`,
  `maxSequenceLoops=10`に変更し、UIの該当スピンボックスは下限を1にして
  `setSpecialValueText("無制限")`を廃止した。
- v0.13: 最大実行時間についても「無制限」を選択できないようにした（6.6節）。単位を分から
  秒に変更し（`MainWindow`の該当スピンボックスを`m_maxDurationMinSpin`から
  `m_maxDurationSecSpin`にリネーム、`config.maxDurationSec`への変換で`*60`していた
  処理を削除）、下限を1秒・デフォルト値を120秒に設定。`TestConfig::maxDurationSec`の
  デフォルト値も120に変更。これで最大実行回数・シーケンス繰り返し回数・最大実行時間の
  3つとも無制限を選択できない（v0.12と合わせて必ず何らかの上限が設定される）。
- v0.14: 対象アプリ以外を誤操作しそうになった場合に必ずテストを停止する安全機能を実装
  （6.7節）。`PlatformAutomation`に`windowPidAtPoint(pt)`（指定座標の最前面ウィンドウの
  所有プロセスID）と`activeProcessPid()`（現在アクティブなプロセスID）を新設し、
  macOS（`CGWindowListCopyWindowInfo`のz順走査 / `NSWorkspace.frontmostApplication`）・
  Linux（`_NET_CLIENT_LIST_STACKING`を逆順走査 / `_NET_ACTIVE_WINDOW`）双方に実装。
  `RandomActionEngine`は各操作を送信する直前に、クリック/ドラッグ/スクロールはその座標の
  ウィンドウが対象PIDと一致するか、キー入力/ショートカットはアクティブなプロセスが対象PIDと
  一致するかを確認し、不一致（または判定不能）ならスキップではなく即座にテスト全体を
  停止するようにした。ドラッグは開始点・終了点の両方を確認する。
- v0.15: 全機能レビュー（①耐久テストとしての操作バリエーションの十分性、②対象アプリ以外を
  誤操作しないか）を実施し、②に関して見つかった安全上のギャップを3件修正した。
  1. コンテキストメニュー選択（右クリック後のメニュー項目選択）が、メニュー探索の直前に
     対象アプリがアクティブかどうかを再確認していなかった。150ms待機中に他のウィンドウへ
     フォーカスが移った場合、無関係なメニューを操作してしまう可能性があったため、
     `activeProcessPid()`による再確認と、不一致時の即時停止を追加した。
  2. macOSのメニュー探索（`findOpenMenuElement`）が「最前面のウィンドウ＝開いたメニュー」
     という前提のみで動作し、そのウィンドウの所有者が対象プロセスかどうかを確認していな
     かった。`listOpenContextMenuItems`/`clickContextMenuItem`/`clickContextMenuItemAt`に
     `expectedOwnerPid`引数を追加し、最前面ウィンドウの所有pidが一致しない場合は
     「メニューなし」として扱うようにした（Linux/AT-SPI側はデスクトップ全体を探索する
     実装のままで、この引数は現時点では未使用 — 1.の確認が主な防御線。8.1節参照）。
  3. `MainWindow::onStart()`に開始前のプリフライト安全確認を追加した。対象ウィンドウの
     現在のboundsの中心点に対して`windowPidAtPoint`を実行し、対象PIDと一致しない場合は
     開始せず警告を表示する（対象ウィンドウが覆われている、またはこの環境で安全チェックAPI
     自体が機能していない、のいずれかを開始前に検出できる）。
  ①（操作バリエーション）については既存実装を妥当と判断したが、追加候補を8.1節に提案として
  記載した（未実装）。macOS: ビルド・起動確認済み。Linux: この開発機がmacOSのみのため
  実機でのビルド・実行検証はできておらず、コードレビューのみ（8章に既知の制約を追記）。
- v0.16: v0.15で提案した8機能全てを実装（6.10節に詳細）。
  - ウィンドウ操作アクション（`ActionKind::WindowOp`）: 移動/リサイズ/最小化/最大化。
    `PlatformAutomation`に`moveWindow`/`resizeWindow`/`minimizeWindow`/`maximizeWindow`を
    新設（macOS: AXUIElement、Linux: Xlib）。副次的に`activateProcess`が最小化済み
    ウィンドウを復帰させるよう修正（Linux側は`collectWindows()`が可視ウィンドウのみを
    対象としていたため最小化中のウィンドウを見失う既存バグも修正）。
  - 名前付きキー（Tab/Return/Escape/Backspace/Delete/矢印キー）をキー入力の候補に追加。
    `PlatformAutomation::keyTapNamed`を新設。Linuxの`keyShortcut`がASCII文字以外の
    キーを正しく変換できていなかった既存バグも`qtKeyToX11Keysym`共通化で修正。
  - ダブルクリック（`ActionParams::doubleClickPercent`、左クリックのみ）。
  - 操作種別ごとの重み（`RegionStep::*Weight`、`pickWeightedActionKind`で加重選択）。
  - 乱数シード（`TestConfig::rngSeed`）。`RandomActionEngine`の乱数生成器を専用インスタンス
    化し、シード値を開始時にログへ記録するよう変更。
  - 対象プロセスのCPU/メモリ使用量を5秒おきにサンプリングしログ表示
    （`PlatformAutomation::queryProcessStats`、macOS: `proc_pidinfo`、
    Linux: `/proc/<pid>/status`・`/proc/<pid>/stat`）。
  - 一時停止/再開（`RandomActionEngine::pause`/`resume`。一時停止中は経過時間の計測も
    止め、最大実行時間の制限に含めない）。
  - 異常検知時（クラッシュ・対象ウィンドウ消失・安全停止）の全画面スクリーンショット保存
    （`~/Pictures/EnduranceTestGUI_Screenshots/`）。
  macOS: ビルド・起動確認済み（対話的なGUI操作を伴う機能ごとの動作確認は未実施）。
  Linux: 実機でのビルド・実行検証はできておらず、コードレビューのみ。
- v0.17: スクロールを「上」「下」で完全に独立した操作種別に分割した（6.4/6.5節）。
  `RegionStep::enableScroll`/`scrollWeight`を`enableScrollUp`/`scrollUpWeight`と
  `enableScrollDown`/`scrollDownWeight`に分離し、`ActionParams::scrollMinAmount`/
  `scrollMaxAmount`（共通1組）も`scrollUpMinAmount`/`scrollUpMaxAmount`と
  `scrollDownMinAmount`/`scrollDownMaxAmount`（上下別々）に分離した。
  `RandomActionEngine::ActionKind`は`Scroll`を`ScrollUp`/`ScrollDown`に置き換え、
  `pickWeightedActionKind`ではステップごとに有効化・重み付けされた上下それぞれが
  独立した候補として重み抽選に参加する（例: 下スクロールだけ多めに発生させる、
  上スクロールは無効にする、といった調整が可能）。横スクロールは共通のトグルのまま、
  発生した上/下いずれかの量をその都度流用する。`StepEditorDialog`・
  `ActionParamsEditor`のUIもそれぞれ上下別のチェックボックス・重み・レンジ入力に分割。
  この開発機では対話的な動作確認ができないため実機未検証。
- v0.18: 操作領域を「ステップに埋め込む一時的な矩形」から「①対象選択パネルで一元管理する
  名前付きの再利用可能なプール」に刷新した（6.2/6.3/6.9節）。
  - `TestConfig.h`に`NamedRegion`（`name`/`regions`/`excludeRegions`）を新設し、
    `TestConfig::namedRegions`としてプールを保持。`RegionStep`から`regions`/
    `excludeRegions`を削除し、`useWindowBounds`を`useWholeWindow`にリネームした上で
    `regionName`（参照先の`NamedRegion::name`）を追加（`useWholeWindow`が偽の場合に
    使用）。
  - 新設`NamedRegionEditorDialog`（名前欄＋領域描画＋除外領域描画、旧`StepEditorDialog`
    の領域編集部分を分離・移設したもの）で操作領域を作成・編集する。
  - `MainWindow`の①対象選択パネルに「操作領域」グループ（一覧・追加/編集/削除ボタン）を
    新設。削除は参照中のステップがあれば拒否（該当ステップ番号を表示）。名前変更時は
    参照している全ステップの`regionName`を自動追従させる。
  - `StepEditorDialog`は領域描画UIを廃止し、「対象GUIの全領域」/「登録済みの操作領域から
    選択（コンボボックス）」のラジオボタンに変更。登録済み操作領域が1つもない場合は
    後者を選べないようにする。
  - `RandomActionEngine::computeEffectiveIncludeRegions`を`resolveStepRegion`に置き換え、
    `useWholeWindow`なら対象ウィンドウの最新boundsを、そうでなければ`namedRegions`から
    `regionName`に一致するものを検索してその`regions`/`excludeRegions`を返すようにした
    （見つからない場合は安全側でテストを停止）。
  - 同じ操作領域を複数のステップから参照できるようになった（以前はステップごとに
    領域を都度描画する必要があった）。
  この開発機では対話的な動作確認ができないため実機未検証。
- v0.19: TestTarget（7章）をこれまでに追加した機能に対応させて拡充した。
  - `CounterButton`に`doubleClicked`信号を追加し、ボタングリッドで左/右/二重クリックを
    別々に集計するようにした（`doubleClickPercent`設定の確認用）。
  - キー入力欄の受信ロジックを拡張し、Ctrl+C/V/A/Zを「ショートカット受信」として
    通常のキー入力と別集計するようにした（`shortcutSequences`の確認用）。また
    Tab/Return/Escape/Backspace/Delete/矢印キーなど`text()`が空になる特殊キーも
    `QKeySequence`名でログ表示するようにした（`allowedKeyChars`と各`keyInclude*`
    オプションの確認用）。
  - 横スクロール領域を新設し、縦スクロールと回数を別集計するようにした
    （`scrollHorizontal`設定の確認用）。
  - 右クリックメニュー対象ボタンを新設し、「項目1」「項目2」「項目3」「キャンセル」を
    持つ本物の`QMenu`を表示・選択集計するようにした（`enableContextMenuSelection`/
    `contextMenuSelectionMode`/`contextMenuItemNames`/`contextMenuIndices`の確認用。
    以前はTestTargetに右クリックメニューが存在せず、この機能を確認する手段がなかった）。
  - ウインドウの位置・サイズを常時表示するラベルと、最小化/最大化の検出回数表示を
    追加した（`resizeEvent`/`moveEvent`/`changeEvent`のオーバーライドで検出。
    `windowOpMove`/`Resize`/`Minimize`/`Maximize`設定の確認用）。
  - 上記すべてのカウンタを「カウンタをリセット」ボタンでリセット対象に含めた。
  この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュなしの
  起動確認のみ）。
- v0.20: ②ステップ構成の「追加」ボタンを領域選択のみに限定し、操作種別・重み・回数は
  ③操作パラメータパネルで設定する方式に変更した（6.2/6.4/6.9節）。
  - `StepEditorDialog`から操作種別チェックボックス・重みスピン・操作回数スピンを全て
    削除し、「対象GUIの全領域」/「登録済みの操作領域から選択」のラジオボタン+コンボ
    ボックスのみの小さなダイアログにした。戻り値も`RegionStep`一式ではなく
    `useWholeWindow()`/`regionName()`の2つのゲッターに変更（他のフィールドを誤って
    リセットしないよう、呼び出し側が領域だけを上書きするようにした）。
  - `MainWindow::onAddStep`は領域選択後、既定値（左クリック有効・回数50など、
    `RegionStep`のデフォルト）でステップを追加してリスト選択状態にする。
    `onEditSelectedStep`は領域のみを変更し、他のフィールドは変更しない
    （以前は`useDefaultActionParams`/`customActionParams`を手動で退避・復元していたが、
    その必要がなくなった）。
  - ③操作パラメータパネルに新しいグループ「このステップの操作種別・重み・回数」を追加。
    ②でステップが選択されていない間は無効化（このグループにはデフォルト値という概念が
    ないため）。②の選択が変わるたびに`MainWindow::flushActionParamsEditor`/
    `loadActionParamsEditorForSelection`が、既存の`ActionParams`エディタと合わせてこの
    グループの読み書きも行う（同じ「選択が変わったら直前の内容を保存」の仕組みに相乗り
    させた）。
  - ②の「追加」時点では操作種別が1つも有効でなくてもエラーにしないようにし（デフォルトで
    左クリックが有効なので実際には常に満たされる）、代わりに「開始」時の設定検証
    （`MainWindow::buildConfigFromUi`）でステップごとに`hasAnyActionEnabled()`を
    チェックするようにした。③でユーザーが全チェックボックスを外した場合はここで
    検出される。
  - 操作領域の名前変更時（`onEditSelectedNamedRegion`）に②のリスト選択が
    `refreshStepList()`で失われ、③のグループが意図せず無効化される既存の副作用を、
    選択行を保存・復元するように修正した。
  この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュなしの
  起動確認のみ）。
- v0.21: ③操作パラメータパネル右上に「デフォルト」ボタンを追加し、ダブルクリックと
  横スクロールを独立した操作種別に昇格した（6.4/6.5/6.9節）。
  - `MainWindow::onEditDefaultParams`（「デフォルト」ボタンのスロット）は
    `m_stepListWidget->setCurrentRow(-1)`を呼ぶだけで、②のステップ選択解除時に
    デフォルト値編集モードへ切り替わる既存の`loadActionParamsEditorForSelection`の
    経路をそのまま再利用する。
  - `ActionParams::doubleClickPercent`（左クリック発生時にその確率でダブルクリックへ
    変える方式）を廃止し、`RegionStep::enableDoubleClick/doubleClickWeight`という
    独立した操作種別に置き換えた。`RandomActionEngine::ActionKind`に`DoubleClick`を
    追加し、常に左ボタンで80ms間隔の2回クリックを送信する（右ダブルクリックはOS上の
    意味が不定なため引き続き非対応）。
  - `ActionParams::scrollHorizontal`（上下スクロール発生時に50%の確率でその量を
    横方向へ流用するフラグ）を廃止し、`scrollHorizontalMinAmount/MaxAmount`という
    横スクロール専用のレンジと、`RegionStep::enableScrollHorizontal/
    scrollHorizontalWeight`という独立した操作種別に置き換えた。`ActionKind`に
    `ScrollHorizontal`を追加し、発生のたびに左右どちらかをランダムに選ぶ。
  - ③の「このステップの操作種別・重み・回数」グループに「ダブルクリック（左のみ）」
    「スクロール（横）」のチェックボックス+重みスピンボックスを追加。`ActionParamsEditor`
    からは廃止した`doubleClickPercent`スピンボックスと`scrollHorizontal`チェックボックス
    を削除し、代わりに「スクロール量(横)」のレンジ入力を追加した。
  - `RegionStep::hasAnyActionEnabled()`と`MainWindow::describeStep`を新しい2種別に
    対応させた。TestTarget（7章）は仕様変更なし（既存のボタン二重クリック集計・
    横スクロール領域がそのまま新しい操作種別の確認に使える）。
  この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュなしの
  起動確認のみ）。
- v0.22: ③操作パラメータパネルの「デフォルト」ボタンを、②の選択を解除する方式から
  専用のモーダルダイアログを開く方式に変更した（6.9節）。
  - 新設`DefaultActionParamsDialog`（`ActionParamsEditor`を1つ載せたダイアログ、
    OK/キャンセル）を追加。`MainWindow::onEditDefaultParams`はまず
    `flushActionParamsEditor()`で未保存の編集内容を確定させてから、現在の
    `m_defaultActionParams`を渡してこのダイアログを開く。OKなら結果を
    `m_defaultActionParams`へ書き戻し、③パネルが現在デフォルト値を表示中
    （ステップ未選択、またはそのステップが「デフォルトを使う」設定）であれば
    その場に反映する。
  - この変更により、②で特定のステップを選択したまま（選択を失わずに）デフォルト値だけを
    素早く確認・編集できるようになった。②で何も選択していないときに③パネルへ直接
    デフォルト値を表示・編集できる従来の経路はそのまま残している。
  - `CMakeLists.txt`のSOURCES/HEADERSに`src/DefaultActionParamsDialog.cpp/.h`を追加。
  この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュなしの
  起動確認のみ）。
- v0.23: 「デフォルト操作は操作種別の選択と詳細設定のデフォルトを設定する」という考え方
  に合わせ、「デフォルト」ボタンで操作種別・重み・回数も選択できるようにした（6.9節）。
  - ③の「このステップの操作種別・重み・回数」グループのUI構築コードを、新設した
    再利用可能ウィジェット`ActionKindEditor`（`setKinds(const RegionStep&)`/
    `applyKindsTo(RegionStep&) const`。`RegionStep`のうち領域と
    ActionParams参照方式（`useWholeWindow`/`regionName`/`useDefaultActionParams`/
    `customActionParams`）以外のフィールドだけを読み書きする）に切り出した。
    `MainWindow`は個別のチェックボックス・スピンボックスのメンバ変数群を削除し、
    `ActionKindEditor*`1個で置き換えた（`flushActionParamsEditor`/
    `loadActionParamsEditorForSelection`もそれぞれ`applyKindsTo`/`setKinds`の
    1行呼び出しに簡略化）。
  - `MainWindow`に`RegionStep m_defaultActionKinds`（②で新規ステップを追加する際の
    操作種別・重み・回数の初期値のひな型。既存ステップには影響しない、一度限りの
    コピー元）を追加。`onAddStep`は`RegionStep()`のハードコードされた既定値ではなく
    `m_defaultActionKinds`から新しいステップの種別部分をコピーするようにした。
  - `DefaultActionParamsDialog`のコンストラクタを`(const RegionStep &initialKinds,
    const ActionParams &initialParams, QWidget *parent)`に変更し、`resultKinds()`/
    `resultParams()`の2つを返すようにした。ダイアログ内に`ActionKindEditor`
    （「デフォルトの操作種別・重み・回数」、③と同じUI）と`ActionParamsEditor`
    （「デフォルトの操作の詳細設定」）を両方配置し、それぞれの意味・適用範囲の違い
    （前者は新規ステップのひな型のみ、後者はライブな共有デフォルト）を説明する
    ラベルを添えた。`MainWindow::onEditDefaultParams`は両方の結果を
    `m_defaultActionKinds`/`m_defaultActionParams`へ書き戻す。
  - `CMakeLists.txt`のSOURCES/HEADERSに`src/ActionKindEditor.cpp/.h`を追加。
  この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュなしの
  起動確認のみ）。
- v0.24: ドキュメント整理のみ（コード変更なし）。6.4節「操作パラメータ」を、6.4.1「操作種別
  の選択」/6.4.2「操作の詳細設定」/6.4.3「デフォルト」ボタン・ダイアログの3小節に再構成し、
  冒頭に両分類の対比表（保持する構造体・内容・「デフォルト」の意味・②未選択時の③の表示・
  編集ウィジェット・個別トグルの有無）を追加した。特に「操作種別の選択にはライブな共有
  デフォルトが存在せず、②で新規ステップを作った瞬間だけコピーされる一度限りのひな型
  （`m_defaultActionKinds`）である」点と、「操作の詳細設定は`useDefaultActionParams`で
  ライブに共有されるデフォルト（`defaultActionParams`）である」点の違いを明記した。
  併せて6.9節の重複していた「デフォルト」ボタンの説明を6.4節への参照に簡略化し、6.4節末尾に
  あった操作間隔デフォルト値（20ms〜100ms、6.6節に既に同内容がある）の記載を削除した。
- v0.25: Qtのライセンス表示に対応した（6.11節）。
  - `MainWindow`にメニューバーを新設（`buildMenuBar`、`buildUi`の先頭で呼ぶ）。
    「ヘルプ」メニューに「EnduranceTestGUIについて...」（`onAboutApp`。自作の
    `QMessageBox::about`。使用中のQtバージョン＋LGPLv3（一部GPL）で配布されている旨を
    記載）と「Qtについて...」（`onAboutQt`。Qt標準の`QMessageBox::aboutQt(this)`を
    そのまま呼ぶ。「Show License」ボタンで実際のライセンス条文全体を表示できるため、
    条文をアプリ側で同梱・複製する必要がない）を追加した。
  この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュなしの
  起動確認のみ）。
- v0.26: 操作領域の名前自動付与、②ステップ一覧のラベルのライブ更新、実行中ステップの
  可視化の3点を実装した（6.2/6.3節）。
  - `MainWindow::generateDefaultRegionName`を新設。「領域1」「領域2」…のうち既存の
    操作領域と重複しない最小の番号のものを返す。`onAddNamedRegion`はこれで名前を
    自動生成し、`NamedRegionEditorDialog`の名前欄にあらかじめ入力した状態で開くように
    した（元々6.3節の仕様に「操作領域には自動でラベルをつける」とあったが、実装は
    手動入力必須になっていたものを本来の仕様に合わせた）。名前欄自体は編集可能なまま
    残しており、自動生成された名前をそのまま使うことも、分かりやすい名前に変更する
    こともできる。
  - `ActionKindEditor`に`changed()`シグナルを追加（内部の全チェックボックス/スピン
    ボックスの`toggled`/`valueChanged`を中継）。`MainWindow`はこれを`flushActionParamsEditor`
    へ直接接続し、③で操作種別・重み・回数を変更するたびに②の該当行のラベルが即座に
    更新されるようにした（従来はステップ選択の切り替えや追加/削除等、flushが呼ばれる
    別の操作をするまでラベルが古いままだった）。`setKinds()`によるプログラム的な値設定中
    にも`changed()`が連続発火しうるが、`setKinds()`が最終的に全ウィジェットを対象ステップの
    値に一致させるため、書き戻される内容は最終的に必ず正しい値になる。
  - `RandomActionEngine`に`currentStepChanged(int index)`シグナルを追加し、`start()`と
    `advanceToNextStep()`で発行するようにした。`MainWindow::onCurrentStepChanged`が
    旧・新の該当行のテキストだけを`describeStep`で再生成して差し替え、現在実行中の
    ステップの行の先頭に「▶ 実行中」を表示する（`m_currentRunningStepIndex`で管理。
    `onEngineFinished`で消去）。リスト全体の再構築（`refreshStepList`）は行わず、
    影響する2行のみを更新することで、実行中のリスト選択やスクロール位置を乱さない
    ようにした。
  この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュなしの
  起動確認のみ）。
- v0.27: 「右クリックでたまにメニューが開いたままテストが固まる」不具合を修正した
  （6.4.2/6.5節）。
  - 原因: `RandomActionEngine::performRandomAction`の`ActionKind::Click`で、右クリック後の
    メニュー処理（150ms待って`listOpenContextMenuItems`で探索・選択、該当なしなら
    `dismissContextMenu`でEscape）が`params.enableContextMenuSelection`のif文の中に
    まるごと入っていた。この設定はデフォルトで無効（false）なので、`enableRightClick`を
    有効にしただけの一般的な設定では、対象アプリが実際にネイティブなコンテキストメニュー
    を開いた場合にそれを閉じる処理が一度も実行されず、メニューが開いたまま残り続けて
    いた。開いたメニューは対象プロセスが所有するウィンドウのため`windowPidAtPoint`の
    安全チェックは素通りしてしまい、以降のランダム操作がそのメニューに吸われる/干渉する
    形で「たまに右クリックの後で固まる」ように見えていた。
  - 修正: 右クリック後の「150ms待つ→フォーカス再確認→（`enableContextMenuSelection`が
    有効なら）該当項目を探して選択→選択できなければ`dismissContextMenu`でEscape」という
    処理を`enableContextMenuSelection`の値にかかわらず常に実行するようにした
    （選択を試みる部分だけを`if (params.enableContextMenuSelection)`で囲い、`selected`
    フラグで選択できたかを追跡し、`!selected`なら無条件でEscapeを送る構造に変更）。
  この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュなしの
  起動確認のみ）。ユーザーからの不具合報告を受けての修正であり、実際に改善したかは
  実機でのご確認をお願いしたい。
- v0.28: ウインドウ全体の幅が小さい場合やカラム幅を狭めた場合でも操作しやすいよう、
  各画面のレイアウトを見直した（6.9節）。
  - `ActionParamsEditor`: ドラッグ距離/方向の行を2行に分割、名前付きキーの6チェック
    ボックスをラベル+3個ずつ2行に分割、ウィンドウ操作の4チェックボックスの前にあった
    ラベルを独立した行に分割。長い説明用`QLabel`（ショートカット一覧・候補項目名・
    候補番号の説明）に`setWordWrap(true)`を設定。右クリックメニュー選択の長い
    チェックボックス文言に明示的な`\n`を挿入。
  - `ActionKindEditor`: ウィンドウ操作チェックボックスの長い文言に`\n`を挿入。
  - `MainWindow`: 権限状態ラベルと「権限設定を開く」ボタンを横並びから縦積みに変更し、
    ラベルに`setWordWrap(true)`を設定。下部の実行制御行を「開始/停止/一時停止/状態」と
    「メモリ・CPU使用率/経過時間/実行回数」の2段に分割。①のタイミング・制限
    `QFormLayout`に`setRowWrapPolicy(QFormLayout::WrapLongRows)`を設定し、長いラベルの
    行は幅が足りない場合に入力欄をラベルの下へ自動折り返しするようにした。
  - `StepEditorDialog`・`NamedRegionEditorDialog`・`DefaultActionParamsDialog`の説明用
    `QLabel`にも`setWordWrap(true)`を設定。
  この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュなしの
  起動確認のみ。画面が実際に小さい幅でどう見えるかは目視確認できていない）。
- v0.29: 操作間隔を「ms指定」と「回数/秒指定」のどちらでも設定できるようにした（6.6節）。
  - ①対象選択パネルの「操作間隔:」欄に`QRadioButton`（「ms指定」/「回数/秒指定」）と
    `QStackedWidget`（`MainWindow::m_intervalStack`）を追加し、選択したモードの入力欄
    （ms: `m_minIntervalSpin`/`m_maxIntervalSpin`。回数/秒: 新設`m_minRateSpin`/
    `m_maxRateSpin`、`QDoubleSpinBox`で0.01〜100.00、小数点2桁）を切り替え表示する。
    両方の入力欄は常に値を保持したままなので、モードを切り替えても入力し直す必要はない。
  - `MainWindow::buildConfigFromUi`で、回数/秒指定が選択されている場合のみ
    `interval = 1000 / rate`で最小/最大ms間隔に変換する（最小回数/秒→最大ms間隔、
    最大回数/秒→最小ms間隔、という逆数の関係）。`RandomActionEngine`・`TestConfig`は
    変更なし（ms単位の`minIntervalMs`/`maxIntervalMs`のみを扱うのは従来通りで、
    回数/秒という概念はUI側の入力補助にとどめた）。
  この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュなしの
  起動確認のみ）。
- v0.30: ①対象選択パネルの「操作領域」リストで項目を選択すると、その操作領域を画面上に
  ハイライト表示するようにした（6.3節）。
  - 新設`RegionHighlightOverlay`（`QWidget`、`Qt::WindowTransparentForInput`で
    マウス/キーボード入力を一切受け取らない非対話オーバーレイ）を追加。含める領域は緑、
    除外領域は赤で半透明に描画し、`RegionSelectorOverlay`（描画用オーバーレイ）と
    配色を揃えた。含める領域の1つ目の左上に選択中の操作領域名を表示するラベルも描画する。
  - `MainWindow::m_namedRegionListWidget`の`currentRowChanged`に`onNamedRegionSelectionChanged`
    を接続し、`updateRegionHighlight`が選択中の行に応じて`RegionHighlightOverlay`を
    表示/更新/非表示する（未選択なら非表示）。
  - `onAddNamedRegion`/`onEditSelectedNamedRegion`は、`NamedRegionEditorDialog`自身の
    描画用オーバーレイと重ならないよう、ダイアログを開く前にハイライトを隠し、ダイアログが
    閉じた後（キャンセル・エラー・保存のいずれでも）に再表示する。追加直後・保存直後は
    その操作領域を選択状態にして即座にハイライトが見えるようにした。
  - `MainWindow::setControlsEnabled`で、テスト実行開始時（①パネル無効化時）にハイライトを
    隠し、停止時（再有効化時）に選択中の操作領域があれば再表示するようにした。
  - `CMakeLists.txt`のSOURCES/HEADERSに`src/RegionHighlightOverlay.cpp/.h`を追加。
  この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュなしの
  起動確認のみ。オーバーレイの実際の見た目・重なり方はこの環境では目視確認できていない）。
- v0.31: 操作領域自体の自動生成名と、`NamedRegionEditorDialog`内で描画した個々の矩形の
  ラベルが両方とも「領域1」のように同じ形式で紛らわしいという指摘を受け、採番を分けた
  （6.3節）。
  - `MainWindow::generateDefaultRegionName`が生成する操作領域の既定名を「領域N」から
    「操作領域N」に変更した。
  - `NamedRegionEditorDialog`の個々の矩形リストのラベルを「領域N」から「矩形N」に変更
    （`refreshRegionList`）。除外矩形リストは元々「除外N」だったため変更なし。
    あわせて説明文・ボタン文言も「矩形」表記へ統一した（「矩形を画面上で描画してください」
    「矩形を描画...」「この操作領域内でクリックしたくない除外(マスク)矩形があれば」
    「除外矩形を描画...」）。
  - `MainWindow::describeNamedRegion`が①の操作領域リストに表示する内訳表示も
    「領域N個・除外M個」から「矩形N個・除外M個」に変更し、用語を統一した。
  この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュなしの
  起動確認のみ）。
- v0.32: 「操作領域を複数設定した後にリストから選ぶと、画面上のハイライト表示がずれる」
  不具合を修正した（6.3節）。
  - 原因: `MainWindow::updateRegionHighlight`が`RegionHighlightOverlay`を生成する際、
    `new RegionHighlightOverlay(this)`と`MainWindow`自身を親（parent）に指定していた。
    `RegionHighlightOverlay`は`Qt::Tool`を含むウィンドウフラグを持つトップレベル
    ウィンドウとして、`setGeometry()`に画面の絶対座標（`virtualDesktopGeometry()`）を
    渡す前提で実装されていたが、親を指定すると環境によってはその座標が
    「`MainWindow`からの相対位置」として解釈され、`MainWindow`の現在の画面上の位置
    （どこにウィンドウを置いているか）の分だけハイライト全体がずれて表示される
    可能性があった。操作領域が1つのときは気付きにくいが、複数の操作領域を見比べる
    ことで、実際の矩形の位置とハイライトの位置が食い違っていることが分かりやすくなる。
  - 修正: `RegionHighlightOverlay`の生成時に親を指定しないようにした
    （`new RegionHighlightOverlay()`）。同じ「画面の絶対座標に自分自身で位置決めする
    トップレベルのオーバーレイ/パネル」である`RegionSelectorOverlay`・`StopPanel`も
    元々親を指定しない実装になっており、これに合わせた。親を指定しなくなったことで
    `MainWindow`の子として自動的には破棄されなくなるが、アプリ終了時にプロセスごと
    解放されるため実害はない。
  この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュなしの
  起動確認のみ）。ユーザーからの不具合報告を受けての修正であり、実際に改善したかは
  実機でのご確認をお願いしたい。
- v0.33: v0.32の修正後も「操作領域のハイライト表示がずれる」不具合が解消しないという
  報告を受け、原因をさらに調査した上で`RegionHighlightOverlay`を再設計した（6.3節）。
  - ユーザーへの確認により、外部モニタを接続して2台以上のディスプレイ構成であることが
    判明した。これにより、有力な原因は「解像度・スケール（Retina内蔵ディスプレイ＋
    非Retina外部モニタ等）が異なる複数モニタをまたいで1つのウィンドウを表示すると、
    Qtはウィンドウ単位で1つのバックストアのスケール係数しか持てないため、またいだ
    先のモニタ上では描画位置がずれる」というQt/OS側の既知の制約であると判断した。
    以前の実装は仮想デスクトップ全体（すべてのモニタを1つの矩形として結合した領域）
    を覆う単一の`QWidget`だったため、この制約にちょうど当てはまる。
  - 対策として、単一のウィジェットで仮想デスクトップ全体を覆う方式をやめ、
    **`QScreen`（モニタ）ごとに1つ、そのモニタの`geometry()`と完全に一致する
    ウィンドウを作る**方式に再設計した。`RegionHighlightOverlay`は`QWidget`の
    サブクラスではなく`QObject`になり、内部で`RegionHighlightScreenWindow`
    （`.cpp`内のプライベートクラス）のリストを、選択されている操作領域が変わるたびに
    作り直して管理する（モニタの脱着があっても`QScreen*`の無効化を気にせず済むよう、
    毎回作り直す設計にした）。各`RegionHighlightScreenWindow`は自分のモニタの座標
    だけを基準に矩形を描画するため、モニタごとに異なるスケール係数で正しく描画され、
    モニタの境界をまたぐ矩形は、それぞれのモニタ側のウィンドウに収まる部分だけが
    自然に描かれる（`QPainter`がウィジェット範囲外の描画を自動的にクリップするため、
    範囲外に出た部分は単に表示されない）。
  - `MainWindow`側の呼び出し（`showRegion`/`hide`）はそのまま変更なし
    （`RegionHighlightOverlay`が`QObject`になった以外は呼び出し側から見える
    インターフェースは変わっていない）。
  - v0.32で導入した「親を指定しない」「`show()`の後に`setGeometry()`」「座標変換に
    使う原点を自分で保持する」という対策は、この再設計後も（各`RegionHighlightScreenWindow`
    に対して）引き続き有効にしている。また、v0.32で追加した診断用ログ
    （`MainWindow::updateRegionHighlight`が出力する「操作領域「名前」をハイライト
    表示: (x,y w x h), ...」）もそのまま残している。
  この開発機ではモニタを複数接続した対話的な検証ができないため実機未検証
  （ビルド成功とクラッシュなしの起動確認のみ）。複数モニタでの動作確認をお願いしたい。
  もし単一モニタでも発生していた、あるいはこの修正後もずれが残る場合は、ログの座標と
  実際の表示位置のずれ方（方向・おおよその量、どのモニタ上でずれるか）を教えていただけると
  原因の特定に役立つ。
- v0.34: ステップ構成の中に待機（待ち時間）を挿入できるようにした（6.2節）。
  - `RegionStep`に`isWaitStep`（既定false）/`waitDurationMs`（既定1000）を追加。
    `isWaitStep`が真のステップは領域・操作種別・`ActionParams`を一切使わない
    「待つだけ」の特別なステップとして扱う。`hasAnyActionEnabled()`は待機ステップに対し
    常にtrueを返すようにした（操作種別が無いことをエラー扱いしないため）。
  - ②ステップ構成パネルに「待機を追加...」ボタンを新設（`MainWindow::onAddWaitStep`）。
    `QInputDialog::getInt`で待機時間（ms、1〜600000）を聞くだけの最小限のUIとした
    （専用のダイアログクラスは新設していない）。「編集...」ボタンは選択中のステップが
    待機ステップなら同じ入力ダイアログを開くよう`onEditSelectedStep`を分岐させた。
  - `MainWindow::describeStep`は待機ステップを「ステップN: 待機（X ms）」と表示し
    （実行中マーカーの付与は通常のステップと同様）、
    `loadActionParamsEditorForSelection`は待機ステップ選択時に③操作パラメータパネル
    全体（操作種別グループ・詳細設定グループとも）を無効化するようにした
    （`MainWindow::setControlsEnabled`の`m_stepKindGroup`再有効化ロジックも、選択中の
    ステップが待機ステップの場合は有効化しないよう修正）。
  - `MainWindow::buildConfigFromUi`の開始時バリデーションループは待機ステップを
    スキップする（`continue`）ようにした。
  - `RandomActionEngine::performRandomAction`は現在のステップが待機ステップの場合、
    対象への操作を一切送信せず、ログに待機時間を出力したうえで`advanceToNextStep()`を
    呼び、通常の（`minIntervalMs`〜`maxIntervalMs`からランダムに選ぶ）待ち時間の代わりに
    `waitDurationMs`そのものをタイマーにセットする。待機は`m_iterationCount`
    （実行回数）には含めない。
  この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュなしの
  起動確認のみ）。
- v0.35: GUIのレイアウトを、3カラム横並び（①対象選択/②ステップ構成/③操作パラメータを
  左から並べ、実行制御とログをその下に全幅で配置）から、縦3段組み（1段目: ①対象選択、
  2段目: ②ステップ構成/③操作パラメータを左右に並べたもの、3段目: 実行制御+ログ）に
  変更した（6.9節）。
  - `MainWindow::buildUi`で、外枠を`QSplitter(Qt::Horizontal)`から`QSplitter(Qt::Vertical)`
    に変更。1段目に①（`buildTargetColumn`）、2段目に②③を左右に並べた
    `QSplitter(Qt::Horizontal)`（入れ子）、3段目に実行制御（開始/停止/一時停止・状態表示・
    メモリ/CPU使用率・経過時間・実行回数）とログをまとめた`QWidget`を配置する3つの
    トップレベル要素にした。従来は実行制御とログを3カラムsplitterの外（`rootLayout`に
    直接）に積んでいたが、新レイアウトではそれらを縦splitterの3段目という1つの
    パネルにまとめている。
  - ①②③のグループ構成・見た目自体（`buildTargetColumn`/`buildStepsColumn`/
    `buildActionParamsColumn`の中身）は変更していない。「①/②/③」という呼び方や
    コード中のコメント・ユーザー向けメッセージ内の「②で...」「③操作パラメータ
    パネルで...」といった参照も、配置が変わっただけで指している内容は変わらないため
    そのまま維持している。
  - 縦・横どちらのsplitterの境界もドラッグでき、6.9節「狭い幅・高さでの操作性」
    （v0.28）で行った折り返し・複数行分割等の対応は新レイアウトでもそのまま有効。
  この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュなしの
  起動確認のみ。実際の見た目・使用感はこの環境では目視確認できていない）。
- v0.36: 「操作領域を選択するとアプリが固まる」不具合を修正した（6.3節）。
  - 原因: v0.33で`RegionHighlightOverlay`をモニタごとのウィンドウに分割した際、
    `showRegion()`が呼ばれるたび（＝①の操作領域リストで選択が変わるたび）に、
    既存のウィンドウを全て`qDeleteAll`で破棄してから同数を新規生成し直す実装にして
    いた。`hide()`も同様に全ウィンドウを破棄していた。OSレベルのトップレベルウィンドウ
    （macOSでは実際のNSWindow）の生成・破棄はコストが高く、これをリスト選択のたびに
    （モニタが複数ある場合はその台数分）繰り返していたため、選択するたびに数秒程度
    アプリが応答しなくなり、「固まった」ように見えていたと考えられる。
  - 修正: `RegionHighlightOverlay`がモニタごとのウィンドウを**使い回す**ように変更した。
    `showRegion()`はモニタの台数が実際に変化した場合（モニタの接続/切断）だけ
    ウィンドウを作り直し、それ以外は既存のウィンドウに対して`setContent()`
    （位置・内容の更新と`show()`/`raise()`/`update()`）を呼ぶだけにした。`hide()`も
    ウィンドウを破棄せず非表示にするだけに変更した（実際に破棄するのは
    `RegionHighlightOverlay`自身のデストラクタ、つまりアプリ終了時のみ）。
  - 念のため、`MainWindow::onNamedRegionSelectionChanged`から`updateRegionHighlight`
    （オーバーレイの表示更新、内部でネイティブなウィンドウ操作を行う）を呼ぶタイミングを
    `QTimer::singleShot(0, ...)`で1回分遅延させるようにした。`currentRowChanged`シグナルは
    リストのクリックというネイティブなマウスイベント処理の最中に同期的に発火するため、
    その中でさらにネイティブなウィンドウ操作（`show`/`setGeometry`/`raise`）を行うと、
    macOS側のイベント処理と衝突して不安定になる可能性を考慮した予防的な対策。
  この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュなしの
  起動確認のみ）。ユーザーからの不具合報告を受けての修正であり、実際に改善したかは
  実機でのご確認をお願いしたい。
- v0.37: 「操作リスト内の操作領域を選択しても対象領域を表示しないようにし、代わりに
  操作領域を編集する時に可視化してほしい」という要望を受け、ハイライト表示の
  トリガーを①の「操作領域」リストの選択から、`NamedRegionEditorDialog`（追加/編集
  ダイアログ）が開いている間へ移した（6.3節）。v0.33/v0.36でリスト選択時の表示を
  何度か修正してきたが、選択のたびにオーバーレイを出し入れする設計自体をやめる形で
  対応した。
  - `MainWindow`から`RegionHighlightOverlay`関連の機能を全て削除した:
    `updateRegionHighlight()`・`onNamedRegionSelectionChanged()`・
    `m_regionHighlightOverlay`メンバー、`m_namedRegionListWidget`の
    `currentRowChanged`への接続、`onAddNamedRegion`/`onEditSelectedNamedRegion`の
    ダイアログ前後でのhide/show呼び出し、`setControlsEnabled`でのhide/show呼び出し。
  - 代わりに`NamedRegionEditorDialog`が自前で`RegionHighlightOverlay`（ダイアログの
    子として生成、破棄はダイアログの破棄時に自動）を持つようにした。
    `updateHighlight()`を新設し、矩形の追加・削除（`onDrawRegions`/
    `onRemoveSelectedRegion`/`onDrawExcludeRegions`/`onRemoveSelectedExcludeRegion`）
    のたびに呼んで、その時点の`m_regions`/`m_excludeRegions`（＝編集中の内容、保存前
    でも反映される）を表示し続ける。ダイアログが閉じるとき（`QDialog::finished`
    シグナル。保存・キャンセルいずれも発火）にハイライトを隠す。
  - `onDrawRegions`/`onDrawExcludeRegions`（「矩形を描画...」/「除外矩形を描画...」）
    では、`RegionSelectorOverlay`（描画用の別の常時最前面オーバーレイ）を開く前に
    一旦ハイライトを隠し、描画セッションが終わったら`updateHighlight()`で再表示する
    ようにした（2つの常時最前面オーバーレイが同時に出て視覚的にぶつかるのを防ぐため）。
  この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュなしの
  起動確認のみ）。
- v0.38: ①対象選択パネル内の「操作領域」パネルと「タイミング・制限」パネルを、縦に
  積む配置から横並びの配置に変更した（6.9節）。
  - `MainWindow::buildTargetColumn`で、`m_namedRegionGroup`を追加していた場所から
    単独の`layout->addWidget(m_namedRegionGroup)`呼び出しを削除し、`m_timingGroup`の
    構築が終わった後に`QHBoxLayout`（`namedRegionAndTimingRow`）へ両グループを
    ストレッチ比1:1で追加してから①のメインレイアウトに加える形に変更した
    （`m_targetGroup`（対象選択）は変更なく単独で上段に残る）。
  この開発機では対話的な動作確認ができないため実機未検証（ビルド成功とクラッシュなしの
  起動確認のみ）。
- v0.39: コードレビューを実施し、見つかった不具合5件を修正した（6.2/6.3/6.4節関連）。
  レビュー後、実際にテスト実行して挙動を確認するよう依頼があったが、この開発機の
  シェル環境にはAccessibility/Automation権限が一切なく（8章に検証結果を記載）、GUI
  操作の自動化自体ができないため、ビルド成功・両アプリ（EnduranceTestGUI/TestTarget）
  の同時起動でのクラッシュなし確認にとどまる。実際の操作確認はユーザーの実機でお願いしたい。
  - **【重要度高】ステップ切替時、③の操作種別編集がステップのデータを破損させうる不具合を
    修正**: `MainWindow::loadActionParamsEditorForSelection`は`const RegionStep &step =
    m_steps[row];`と`m_steps[row]`への**参照**を`ActionKindEditor::setKinds(step)`に渡して
    いた。`setKinds()`が各チェックボックス/スピンボックスを順にプログラム的に設定する際、
    値が変化するたびに`ActionKindEditor::changed()`が発火し、これに直結している
    `MainWindow::flushActionParamsEditor`が同期的に呼ばれる。この時点で`m_lastEditedStepRow`
    は既に新しい`row`を指しているため、flushは`applyKindsTo(m_steps[row])`で**同じ
    `RegionStep`オブジェクト**（`step`が参照しているものと同一）へ、その時点のウィジェット
    群の値（まだ`setKinds()`が反映し終えていないフィールドは直前のステップの値のまま）を
    書き込んでしまう。さらに悪いことに、`step`は参照なので、`setKinds()`がまだ読んでいない
    残りのフィールドを後続の行で`step.enableXxx`のように読む際、直前のflushが**その
    ステップ自身に書き込んだ値**を読むことになり、意図した値ではなく上書きされた値が
    ウィジェットに設定されてしまう可能性があった。修正として`step`を参照ではなく**値の
    コピー**（`const RegionStep step = m_steps[row];`）に変更し、`setKinds()`が読む
    ソースを、途中のflushによる書き込みから独立させた。
  - **右ボタンドラッグでメニューが開いたまま残る可能性を修正**: v0.27でクリック時の
    右クリックについては「メニューが開いたら必ず閉じる/選択する」ようにしたが、
    ドラッグ（`RegionStep::enableRightClick`が有効なステップでは約50%の確率で右ボタンの
    ドラッグも発生しうる）には同等の処理が無かった。クリック側のメニュー処理ロジックを
    `RandomActionEngine::handlePossibleContextMenu`という共通ヘルパーに切り出し、
    ClickケースとDragケースの両方（右ボタンが選ばれた場合）から呼ぶようにした。
  - **実行中に「デフォルト」ボタンが押せてしまう不具合を修正**: `MainWindow::
    setControlsEnabled`は`m_editDefaultParamsButton`を無効化対象に含めておらず、テスト
    実行中でも`DefaultActionParamsDialog`を開いて設定を変更できてしまっていた。
    `m_stepKindGroup`等と同様に`enabled`に連動させるようにした。
  - **操作領域編集ダイアログで、名前欄を書き換えてもハイライトのラベルが更新されない
    不具合を修正**: `NamedRegionEditorDialog`のオンスクリーンハイライト（v0.37）は矩形の
    追加/削除時にしか更新されず、名前だけを変更した場合はラベルが古いままだった。
    `m_nameEdit`の`textChanged`を`updateHighlight()`に接続した。
  - **メニュー項目選択の候補リストが、機能自体を無効にしていても操作可能なままだった
    不具合を修正**: `ActionParamsEditor::onContextMenuModeChanged`は「項目名で指定」/
    「上から何番目かで指定」のどちらのモードかだけで候補リストの有効/無効を切り替えて
    おり、大元の「右クリック後にメニュー項目を選択する」チェックボックス
    （`m_contextMenuCheck`）の状態を見ていなかった。モード選択ラジオボタン自体も
    含めて、大元のチェックボックスがオフの間は全て無効化するようにした。
- v0.40: 【今回初めてLinux実機での対話的な動作確認を実施】これまでの開発機は
  macOSのみでLinux側はコードレビューのみだったが（8章参照）、今回はLinux環境
  （Ubuntu、Qt6 6.4.2、Xサーバは`Xvfb`＋EWMH対応ウィンドウマネージャ`openbox`）で
  実際にビルド・起動・操作を行い、以下を確認した。コード変更は行っていない
  （ドキュメント更新のみ）。
  - **ビルド**: `cmake -B build -S .` → `cmake --build build` は警告なしで成功。
    さらに`-Wall -Wextra -Wpedantic`を付けて再ビルドしても警告は1件も出なかった
    （`EnduranceTestGUI`本体・`TestTarget`とも）。
  - **対象選択〜ステップ実行**: `openbox`下で`TestTarget`と`EnduranceTestGUI`を起動し、
    ①対象選択で`TestTarget`をEWMH（`_NET_CLIENT_LIST`）経由で正しく検出・選択できる
    ことを確認。②で「対象GUIの全領域」を使うクリックステップ（回数50、シーケンス
    繰り返し10回）を作成して「開始」し、XTest経由の合成左クリックが実際に
    `TestTarget`側のボタンに届いて集計が増える（ログにも「ボタンN: 左クリック」が
    記録される）ことを確認。シーケンスが指定の10回に達すると
    「シーケンスの繰り返し回数の上限に達したため停止しました」で自動停止し、
    実行回数表示（50×10=500）とも一致した。
  - **予期しないモーダルダイアログに対する挙動（副次的な発見）**: 除外領域を
    設定していない状態でランダムクリックが`TestTarget`の「終了（クリック厳禁）」
    ボタンに当たり、`TestTarget`自身の終了確認ダイアログ（`QMessageBox`、Yes/No）が
    開いた。このダイアログは対象プロセスの一部（同じpid）なので6.7節の安全チェックは
    素通りし、テスト自体は異常終了せずシーケンス上限まで動作を続けたが、ダイアログが
    開いている間の約490回のクリックはいずれも（Qtのモーダルグラブにより）
    ダイアログ外の主ウィンドウ側には届かず、`TestTarget`側のカウンタも増えなかった
    （ダイアログの「はい/いいえ」ボタン自体に当たることもなかった）。実害はなかったが、
    実行回数の大部分が意図した操作を何も検証できないまま消費される挙動が実機で
    確認できたため、6.10節の続きとして10章に改善提案を追記した。
  - **クラッシュ検知・安全停止・異常時スクリーンショット**: 新規に開始したテスト実行中に
    `TestTarget`プロセスを`kill -9`で強制終了したところ、直後のクリック直前チェックで
    `windowPidAtPoint`が対象pidと一致しなくなり、「対象アプリ以外のウィンドウを
    操作しそうになったため、安全のためテストを停止しました」でfail-closedに停止する
    ことを確認（`isProcessRunning`によるクラッシュ検知より先にこちらが働いた形になるが、
    いずれにせよ安全側に停止する設計どおりの結果）。同時に`isAnomaly=true`経路の
    スクリーンショット保存も動作し、`~/Pictures/EnduranceTestGUI_Screenshots/
    anomaly_<timestamp>_screen0.png`が実際に生成されることを確認した。
    【v0.48での補足】この時点では「どちらの経路で止まっても安全側に停止するので実害
    なし」という結論だったが、v0.48でクラッシュ専用の通知ダイアログ（6.7節）を追加した
    ことで、実際にはこの取りこぼし自体が問題になることが判明した。原因は
    `isProcessRunning`が`kill(pid, 0)`だけで判定しており、終了直後でまだ親プロセスに
    回収されていないゾンビプロセスを「まだ生きている」と誤判定していたため。v0.48で
    ゾンビ状態も確認するよう修正済み（詳細は6.7節のv0.48実装状況を参照）。
  - **未検証のまま残っている項目**: ドラッグ・スクロール・キー入力/ショートカット・
    ウィンドウ操作（移動/リサイズ/最小化/最大化）・右クリックメニュー項目選択
    （AT-SPI）・除外(マスク)領域・一時停止/再開・複数モニタ・
    `RegionSelectorOverlay`の半透明描画は今回の確認範囲に含まれておらず、引き続き
    実機（ユーザー環境）での確認が必要（8章参照）。
- v0.41: 10章で提案した6機能をすべて実装した。Linux実機（Xvfb + openbox、v0.40と
  同一環境）で全機能を実際に動作確認している。
  - **①予期しないモーダルダイアログの検出・自動解消**: `PlatformAutomation::
    listWindowIdsForPid(pid)`を新設（macOS/Linux）し、`RandomActionEngine::
    handleUnexpectedWindows()`が毎ティック、対象pidの現在のウィンドウ一覧を
    `config.targetWindowId`と比較する。一致しないウィンドウが存在する間はその
    ティックの本来の操作をスキップしてEscapeを送信（`PlatformAutomation::
    dismissContextMenu()`を汎用の「Escapeを送る」処理として流用）し、5回連続で
    解消できなければ異常停止＋スクリーンショット保存に切り替える
    （`kMaxUnexpectedWindowStrikes = 5`）。実機確認: v0.40で発見した「終了
    （クリック厳禁）」ボタン（右クリックを有効にしたステップで到達可能）を含む
    設定で500回のランダム操作（左右クリック混在）を最後まで実行し、スタックする
    ことなくシーケンス上限で正常終了することを確認した（対象領域を絞ってダイアログを
    強制的に踏み続けさせる、より直接的な確認は本セッションの非compositing環境の
    制約——後述——により未実施）。
  - **②ウィンドウ相対座標での名前付き領域**: `NamedRegion`に`followsTargetWindow`
    （bool）と`anchorTopLeft`（QPoint）を追加。`NamedRegionEditorDialog`に
    「対象ウィンドウの移動に追従させる」チェックボックスを追加し、MainWindow側で
    ダイアログを開く直前に現在選択中の対象ウィンドウの左上座標を取得して渡す
    （`MainWindow::currentTargetTopLeft`）。OKを押した時点の対象ウィンドウ位置を
    基準点として記録し、`RandomActionEngine::resolveStepRegion`は実行時に
    「現在の対象ウィンドウ位置 - 基準点」の差分だけ矩形群を平行移動してから使う。
    対象ウィンドウが見つからない場合は平行移動せず as-drawn の座標のまま使う
    フォールバックとした。実機確認: UIの表示・チェックボックスの有効/無効切替
    （対象未選択時は無効化）・保存/復元は確認したが、`RegionSelectorOverlay`
    による矩形描画自体がこのセッションのXサーバ（コンポジットマネージャなし）では
    半透明が効かず全画面が不透明に覆われてしまう既知の制約（8章）のため、実際に
    矩形を描画してウィンドウ移動に追従する様子の目視確認はできなかった
    （コードレビューと、非対話的に到達可能な範囲の動作確認にとどまる）。
  - **③グローバルホットキーによる緊急停止**: `GlobalHotkey`クラス（`src/platform/
    GlobalHotkey.h`、pimpl）を新設し、Ctrl+Alt+Shift+Escapeを固定のシステム
    グローバルホットキーとして登録する。Linux (`GlobalHotkey_linux.cpp`) は
    `XGrabKey`（Caps Lock/Num Lockの4通りの組み合わせを個別にグラブし、
    `XSetErrorHandler`+`XSync`で既に他アプリが使用中かを検出）と、別個の
    Xlib接続上の`QSocketNotifier`でイベントを受信する。macOS
    (`GlobalHotkey_mac.mm`) はCarbon Event Managerの`RegisterEventHotKey`
    （本アプリが既にリンクしているCarbon.frameworkを利用、CGEventTapと異なり
    追加の権限は不要）。`MainWindow`起動時に登録を試み、成否をログに出す
    （失敗時も「■停止」ボタンは通常どおり使える）。実機確認（Linux）: 登録成功
    ログを確認し、実行中にCtrl+Alt+Shift+Escapeを送信してテストが即座に停止する
    こと、専用のログメッセージが出ることを確認した。また2つ目のアプリインスタンス
    を起動して同じ組み合わせのグラブが失敗するケース（「登録できませんでした」）も
    意図せず確認できた。macOSでの実機確認は未実施。
  - **④実行後サマリーレポート**: `RandomActionEngine::RunSummary`（停止理由・
    異常フラグ・使用した乱数シード・総実行回数・完走シーケンス数・経過時間・
    操作種別ごとの回数）を新設し、`doStop()`で組み立てて`summaryReady`
    シグナルで`finished`の直前に発行する。`MainWindow`はこれをログに整形して
    追記するとともに、「実行結果サマリーを保存...」ボタン（テスト未実行時は
    無効）でJSON/テキストいずれかの形式でファイル保存できる。実機確認:
    複数回の実行で毎回ログに正しいサマリーが出ること、JSON/テキスト保存とも
    ファイルに正しい内容が書き出されることを確認した。
  - **⑤テスト設定のプリセット保存・読み込み**: `TestConfigJson.h/.cpp`に
    `NamedRegion`/`RegionStep`/`ActionParams`のJSON変換関数を新設。
    メニューバーに「ファイル」メニューを追加し、「テスト設定を保存...」で
    操作領域・ステップ構成・デフォルト操作パラメータ・デフォルト操作種別
    テンプレート・タイミング/制限一式をJSONファイルに書き出し、「テスト設定を
    読み込む...」で復元する（対象アプリのpidは実行ごとに変わるため復元対象外。
    保存時の対象アプリ名だけ参考情報として保存/表示し、①での選び直しを促す）。
    実行中はどちらのメニュー項目も無効化される。実機確認: ステップ・操作領域を
    含む設定を保存 → 全ステップ削除 → 読み込みで元の内容（ステップ数・領域・
    操作種別等）が復元されることを確認した。
  - **⑥対象アプリの無応答（ハング）検知**: `PlatformAutomation::
    checkWindowResponsive(windowId, pid)`を新設。Linuxは`_NET_WM_PING`
    （EWMH）を実装し、8秒間隔のタイマーで「前回送ったpingへの返答を確認しつつ
    次のpingを送る」トレーリング方式（`ResponsivenessCheck::Pending/
    Responding/NotResponding/Unsupported`）で毎回ブロッキングせずに済むように
    した。macOSは、Activity Monitor等が使う応答性判定が非公開のCoreGraphics
    シンボル（`CGSEventIsAppUnresponsive`）に依存する実装であるため、非公開API
    に頼るリスクを避け、意図的に`Unsupported`を返すのみとした（8章参照）。
    **実機確認で重要な問題を発見・修正**: 開発用の`TestTarget`（Qt6/xcbの
    ウィンドウ）は`WM_PROTOCOLS`に`_NET_WM_PING`を含めて対応を表明している
    ものの、実際にはウィンドウマネージャ以外から送ったpingに一切応答しない
    ことが分かった（独立した最小限のXlibテストプログラムでも再現し、本実装
    固有の不具合ではないことを確認済み）。素朴な実装だと「一度も応答しない
    アプリ」を「途中でハングしたアプリ」と誤検知し、正常なテストを毎回誤って
    停止させてしまう重大な不具合になるところだった。対策として、(a)
    直前の状態を持たない最初の呼び出しは`Responding`ではなく`Pending`
    （判定材料なし）を返すよう区別し、(b) 一度も`Responding`を観測して
    いないウィンドウに対しては`NotResponding`が続いても停止せず、3回連続で
    「一度も応答なし」の場合は「対応していないツールキット/実装の可能性が
    あります」とログに出してハング検知そのものを無効化する、という2段構えの
    誤検知防止を実装した。実機確認: 修正前は500回のテストが開始数秒〜十数秒後に
    毎回「ハングを検知したため停止しました」と誤停止することを確認し、
    修正後は同じ`TestTarget`に対して複数回実行しても誤停止せず、代わりに
    「対象アプリが応答確認（WM_PING）に一度も応答しないため、ハング検知を
    無効にします」とログに出た上でテストが最後まで正常実行されることを確認した
    （実際にハングしたアプリを正しく検知するケース自体は、実機で意図的に
    フリーズさせられるテスト対象を用意できなかったため未検証）。
  - ビルドは`-Wall -Wextra -Wpedantic`付きで警告0件を維持している。
- v0.42: ②ステップ構成で複数のステップを1つの「グループ」にまとめ、グループ全体で
  指定した合計呼び出し回数だけ、毎回メンバーの中から重み付きランダムに1つ選んで
  1操作ずつ実行する機能を実装した（詳細・実機確認結果は6.2節）。`RegionStep`への
  `isGroup`/`groupMembers`/`groupTotalCallCount`/`groupWeight`追加、②パネルへの
  「グループ化」/「グループ解除」ボタン追加、新規`StepGroupEditorDialog`（グループの
  合計呼び出し回数とメンバーごとの領域・操作種別・重み・詳細パラメータを編集する
  マスター・ディテールダイアログ）、`RandomActionEngine`の共通実行ロジック
  （`runOneAction()`/`ActionOutcome`）への切り出しと`performGroupAction()`の新設、
  名前付き領域の参照チェック/リネーム伝播・実行前バリデーションのグループメンバーへの
  再帰対応、JSONプリセットのシリアライズ対応を行った。実機確認中に
  `StepGroupEditorDialog`が1080px高の画面に収まらずOK/Cancelボタンごと画面外に出る
  不具合を発見し、他の同種ダイアログと同じ`QScrollArea`でのラップにより修正した。
- v0.43: v0.42のグループ化機能について、実装後に改めてクリーンビルド＋実機（Xvfb +
  openbox）で再検証した（詳細は6.2節）。クリック/ダブルクリックのように判別しやすい
  操作種別を2メンバーに割り当てて重み付き抽選の実際の出現比率を測定したところ、
  重み1:3・合計呼び出し200回では理論値どおりクリック50回・ダブルクリック150回と
  なった一方、重み1:1では78回・122回とやや理論値からずれた結果が出た。これが
  実装の偏りによるものか単なる統計的ばらつきかを切り分けるため、`pickWeighted
  GroupMemberIndex`と同じロジックを切り出したスタンドアロンプログラムで大規模
  シミュレーション（重みごとに20万回抽選、および「合計呼び出し200回のテスト実行」を
  2000回分模擬）を実施し、(a) 20万回規模では実際の比率が理論値にほぼ完全に収束する
  こと、(b) 200回程度の小さい試行数では78〜125回程度のばらつきが正常に起こりうる
  ことを確認した。以上によりアルゴリズム自体に偏りがないことを定量的に確認できた
  （コード変更なし、検証のみ）。
- v0.44: ランダムテスト実行中に、現在操作対象になっている領域を緑枠（除外領域は赤破線）
  で描き込んだ対象ウィンドウのスクリーンショットを内部に1枚だけ保持し、実行後に
  ログ欄の「操作領域画像を保存...」ボタンから任意のフォルダへPNGとして保存できる機能を
  実装した（詳細は6.6節）。撮影タイミングは「実行前に一度だけ」「ステップが変わるたび
  （デフォルト）」「一定間隔ごと（回数を指定可能）」の3種類から選べる
  （`TestConfig::screenshotCaptureMode`/`screenshotCaptureIntervalActions`、①パネルの
  「タイミング・制限」に追加）。`RandomActionEngine::runOneAction`が領域解決直後に
  `maybeCaptureRegionScreenshot`/`renderRegionScreenshot`を呼ぶ形で実装し、通常ステップ・
  グループメンバーのどちらから呼ばれても同じ経路を通る。JSONプリセットにも設定を保存/
  復元するようにした。実機（Xvfb + openbox）で3つの撮影タイミングそれぞれについて実行→
  保存までの一連の流れを確認済み。
- v0.45: コードレビューを実施し、【重要度高】②ステップ構成・グループ編集ダイアログの
  「↑上へ/↓下へ/削除/グループ化/グループ解除」操作が、無関係な別のステップ（または
  グループメンバー）の設定を破損させうる不具合を発見・修正した。
  - **原因**: `MainWindow::onMoveStepUp`等は「③の編集中の内容を`m_steps[m_lastEditedStepRow]`
    へ`flushActionParamsEditor()`で書き込む→`m_steps`を並べ替え/削除→
    `refreshStepList()`→`setCurrentRow(newRow)`」という順で処理する。ところが
    `QListWidget::clear()`（`refreshStepList()`内）と`setCurrentRow()`はどちらも
    `currentRowChanged`シグナルを**同期的に**発火し、`MainWindow::onStepSelectionChanged`
    （`flushActionParamsEditor()`→`loadActionParamsEditorForSelection()`）を再入させる。
    この再入時点では`m_steps`は既に並べ替え/削除済みだが`m_lastEditedStepRow`は
    まだ移動前の古いインデックスのままのため、再入したflushが「移動後にそのインデックスへ
    シフトしてきた別のステップ」へ、表示されたままの（別ステップの）編集内容を書き込んで
    しまう（インデックスが無効化されている場合はさらに、`flushActionParamsEditor`の
    `m_lastEditedStepRow < 0`分岐により共有の`m_defaultActionParams`が別ステップの
    カスタム設定で上書きされる、という2次的な不具合も生じうる）。v0.39で見つかった
    参照エイリアシングの不具合（本章参照）と根は同じ「flushの再入」だが、あちらは
    ステップ選択そのものが変わらないケースの話で、今回はステップの**インデックスが
    ミューテーションの前後で変わる**操作（並べ替え/削除/グループ化/グループ解除）に
    特有の別パターン。
  - **修正**: `MainWindow`に`m_suppressStepSelectionHandling`フラグを追加し、
    `onStepSelectionChanged()`の先頭でこれが立っている間は何もしないようにした上で、
    上記5つの操作それぞれで「flush（移動前のインデックスへ）→
    `m_lastEditedStepRow = -1`→抑制フラグON→`m_steps`をミューテーション→
    `refreshStepList()`→`setCurrentRow()`→抑制フラグOFF→
    `loadActionParamsEditorForSelection()`を1回明示的に呼ぶ」という一連の流れに書き換えた。
    `StepGroupEditorDialog::onMoveMemberUp/Down`（グループ編集ダイアログのメンバー並べ替え）
    にも同型の不具合があったが、こちらは`flushMemberEditor()`が無効なインデックスに対して
    単に何もしない（`MainWindow`のような「共有デフォルトへ書き込む」副作用がない）ため、
    既存の`onRemoveSelectedMember`と同じ「ミューテーション前に`m_lastEditedMemberRow = -1`
    にする」という単純な修正で十分だった。
  - **実機確認**（Xvfb + openbox）: 操作回数や使用文字（カスタムActionParams）に
    それぞれ異なる値を設定した3つのステップを作り、末尾のステップを選択した状態で
    「↑上へ」を押したところ、修正前の設計のまま再現させると別のステップの設定が
    上書きされて失われるはずのところ、修正後は移動元/移動先いずれのステップの設定も
    正しく保たれ、かつ共有デフォルト（キー入力の使用文字）も書き換わっていないことを
    「デフォルトの操作設定」ダイアログで確認した。同様に、削除操作でも隣接ステップの
    設定が保たれることを確認。グループ編集ダイアログのメンバー並べ替えについても、
    重みに異なる値（1と9）を設定した2メンバーで並べ替えを行い、双方の重みが正しく
    保たれることを確認した。-Wall -Wextra -Wpedantic付きビルドで警告0件を維持。
- v0.46: 「不具合発生時に、どのような操作で不具合が起きたのかを開発者に伝える」という
  観点で耐久テストツールとしての改良提案4件（ログの欠落防止、異常時自動収集の強化、
  ログの可読性向上、画面録画）を実装した。詳細な実装内容は6.6〜6.8節の「実装状況」を
  参照（各節に分散して記載）。概要は以下の通り。
  - **①ログの欠落防止**（6.8節）: 画面表示用ログ欄（`m_logView`）は
    `setMaximumBlockCount(5000)`で古い行を自動的に捨てるため、長時間実行では不具合
    発生前の履歴が失われていた。`MainWindow::m_fullLogFile`を新設し、実行開始から
    終了までの全ログ行を`~/Documents/EnduranceTestGUI_Logs/run_<タイムスタンプ>.log`
    へ書き込み+都度flushするようにした。
  - **②異常時自動収集の強化**（6.7節）: 異常停止時に、既存の全画面スクリーンショット
    保存に加え、実際に使われたテスト設定を実際の乱数シード値付きでJSONプリセットとして
    自動保存（`MainWindow::buildPresetJson()`を共通処理として切り出して再利用）、
    およびOSのクラッシュダンプ・診断ログのベストエフォート探索
    （`PlatformAutomation::findRecentCrashReport`、Linux/macOSとも実装、オプトアウト
    可能・デフォルトON）を追加した。
  - **③ログの可読性向上**（6.8節）: 座標を伴う操作のログ行に対象ウィンドウ相対座標を
    追記し、さらにOSのアクセシビリティAPI経由でクリック地点のUI部品名をベストエフォート
    取得して追記する（`PlatformAutomation::accessibleNameAtPoint`）ようにした。
    **この実装作業中に、Linux側のAT-SPI呼び出しがアクセシビリティ用D-Busバスに接続
    できない環境ではGLibの`G_LOG_LEVEL_ERROR`経由でプロセスを無条件にabort()させる
    （マスク不可能なfatalエラー）という、AT-SPIライブラリの重大な特性を発見した。**
    これにより新機能を素朴に実装したところクリックのたびにGUI本体がクラッシュする
    不具合が実機テストで発覚したため、`DBUS_SESSION_BUS_ADDRESS`環境変数の有無を
    事前確認する`atspiUsable()`ガードを追加して修正した。同じ脆弱性が既存の
    右クリックメニュー選択機能（`findOpenMenu()`）にも潜在していたため、そちらにも
    同じガードを追加した（副次的な既存バグ修正）。残存リスクは8章参照。
  - **④画面録画**（6.7節）: `TestConfig::enableScreenRecording`
    （①パネルのチェックボックス、デフォルトOFF、追加負荷がある旨を明示した
    オプトイン）を有効にすると、実行中0.5秒間隔・最大20枚のリングバッファで対象
    ウィンドウのスクリーンショットを保持し、異常停止時に連番PNGとして書き出す。
    クラッシュダンプ収集も同様に`TestConfig::enableCrashDumpCollection`
    （デフォルトON）で実行前にオプトアウト可能にした。
  - 4機能とも実機（Xvfb + openbox）で、`kill -9`による対象プロセスの強制終了を使って
    異常停止を確実に誘発しながら検証した（対象アプリ自身の「終了」ボタン経由の停止は、
    既存の「予期しないウィンドウ」自動解消機能が確認ダイアログをEscapeで閉じてしまい
    安定して再現できなかったため、より確実な`kill -9`に切り替えた）。全ログファイルの
    内容、異常時自動保存された3種のファイル（プリセットJSON・操作領域スクショ・
    録画フレーム）の内容、ログ行への相対座標/部品名の付記を直接確認し、AT-SPI修正後は
    実行回数300回を超えてもクラッシュしないことを確認した。-Wall -Wextra -Wpedantic
    付きのクリーンビルドで警告0件を維持。
- v0.47: 3件の改善を実装した（6.12/6.6/6.7/5節に詳細を記載）。
  - **①多言語対応**: UI表示言語を日本語/英語から選べるようにした（6.12節）。
    `tr()`/`.ts`ではなく、既存コード中の日本語`QStringLiteral`をそのまま翻訳テーブルの
    キーとする独自の`I18n`レイヤーを新設し、`MainWindow`ほか計10ファイルの約290件の
    ユーザー向け文字列を`I18n::t()`でラップした。メニューバーに「言語 / Language」を
    新設し、選択内容は`QSettings`に永続化・次回起動時に反映される。実機確認の過程で、
    英訳文中の`&`がQtのラベルウィジェットにキーボードニーモニックとして解釈され
    「Timing & Limits」が画面上崩れて表示される不具合を発見し、`&`を使わない訳文に
    修正した（実装しただけでは気づけず、実際に画面を見て初めて分かった不具合）。
  - **②保存するPNGへの操作領域名の付記**: 操作領域スクリーンショット（手動保存・
    異常停止時自動保存の両方）に、緑枠で示す各操作領域の名前（名前付き操作領域の名前、
    または「対象GUIの全領域」）を画像内に直接描き込むようにし、ファイル名にも含める
    ようにした（6.6節）。従来は「どのステップで撮ったか」しかファイル名から分からず、
    画像内にも領域名の表示がなかったため、複数の名前付き操作領域を使うテスト構成で
    不具合発生時にどの領域が関係していたか画像だけでは判別しづらいという課題があった。
  - **③非管理者（非root）Linuxユーザーでの動作確認**: 実際に`sudo`権限のない
    非特権ユーザーを作成し、Xvfb+openboxのXセッション・TestTarget・EnduranceTestGUIの
    すべてをそのユーザーとして起動して動作確認した（5節）。入力送信・グローバル
    ホットキー・ログ/スクリーンショット/画面録画の保存・クラッシュ検知は問題なく
    動作した。唯一、クラッシュダンプ収集機能が探索する`/var/lib/systemd/coredump/`
    配下の個々のファイルが非rootでは読めない場合があることを実機で確認し、見つかった
    パスが現在のユーザー権限で読み取れるかどうかを`QFileInfo::isReadable()`で確認して
    読めない場合はその旨をログに追記する対策を追加した（6.7節）。
  - いずれも実機（Xvfb + openbox、非rootユーザーでの起動を含む）で検証済み。
    -Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。
- v0.48: 「対象アプリがクラッシュして落ちた場合に検知してダイアログで知らせ、同時に
  テストを終了してどのステップで落ちたか分かるようにしてほしい」という要望を実装した
  （6.7節に詳細を記載）。
  - `RunSummary::targetCrashed`フラグを新設し、`isProcessRunning`ベースのクラッシュ
    検知（対象ウィンドウ消失や安全停止など他の異常停止とは区別）でのみ立てるようにした
    上で、`MainWindow::onRunSummaryReady`がこのフラグを見て`QMessageBox::critical`で
    クラッシュ通知ダイアログを表示するようにした。テストは（元々の設計どおり）検知と
    同時に停止するため、「検知→ダイアログ表示」と「テスト終了」は同時に起こる。
    ログ・ダイアログの本文双方に、検知時点で実行中だったステップ番号（「ステップ 1の
    実行中に...」）・実行回数・乱数シードを含めた。
  - 実装・実機確認の過程で、**`PlatformAutomation::isProcessRunning`がゾンビプロセス
    （終了済みだが親プロセスにまだ回収されていないプロセス）を「まだ生きている」と
    誤判定する既存の潜在バグ**を発見した（`kill(pid, 0)`は生存中のプロセスとゾンビの
    両方に対して0を返すため）。これにより、クラッシュ直後の短い時間はクラッシュ検知が
    働かず、代わりに「対象領域が見つからない」等の別の異常停止経路が先に働いてしまい、
    今回追加したクラッシュ通知ダイアログが出ないことがあった（9章のv0.40の実機確認
    記録でも同じ現象自体は記録されていたが、当時は「どちらでも安全に停止するので
    実害なし」という結論だった）。Linux側は`/proc/<pid>/stat`の状態フィールドで、
    macOS側は`proc_pidinfo(..., PROC_PIDTBSDINFO, ...)`の`pbi_status`で、それぞれ
    ゾンビ状態（`Z`/`SZOMB`）かどうかを追加確認するよう修正した（macOS側は
    **この開発機では対話的な動作確認ができないため未検証**、8章参照）。
  - 実機（Xvfb + openbox）で、修正前は`kill -9`直後に別経路で停止しダイアログが出ない
    ことを確認した上で、修正後は確実に「対象アプリのクラッシュを検知しました」
    ダイアログが表示され、ステップ番号・実行回数・乱数シードが正しく表示されること、
    ダイアログを閉じるとテストが同時に停止済みの状態になっていることを確認した。
    -Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。
- v0.49: 2件の対応を行った。
  - **①リポジトリをpublicにするにあたってのライセンス整備**（6.11節）: 直下に
    `LICENSE`（EnduranceTestGUI自身、MIT License）・`THIRD_PARTY_NOTICES.md`
    （Qt/GLib・GObject/AT-SPI/X11クライアントライブラリ/macOS純正フレームワークの
    ライセンス一覧。いずれも動的リンクのみで無改変であることを明記）・`LICENSES/`
    （LGPLv3・GPLv3・LGPLv2.1・GPLv2の条文全文、Debianパッケージ配布物からの参考同梱）
    を新設した。Qt自体のライセンス条文はアプリ内の「ヘルプ」→「Qtについて...」からも
    確認できる（6.11節冒頭、既存機能）ため、今回追加したファイル群はリポジトリを
    見ただけでもライセンス関係が分かるようにするための補足という位置付け。
  - **②クラッシュ後、対象アプリを再起動すればすぐ同じ条件で再テストできるようにする**
    （6.1節）: ②ステップ構成・①操作領域・タイミング設定はそもそも特定のpidに紐付いて
    おらず、クラッシュ（v0.48）やテスト終了で消えることはなかったが、対象アプリの
    選び直しの手間自体は残っていた。`MainWindow::onRefreshTargets()`が一覧再列挙の
    前に選択中のアプリ名を記憶しておき、再列挙後に同じアプリ名のウィンドウを自動的に
    探して再選択するようにした（`tryReselectLastTarget()`）。`onEngineFinished()`
    （停止・クラッシュ検知どちらでも呼ばれる）から一覧の再列挙を呼ぶようにしたことで、
    クラッシュ検知→対象アプリを再起動→（既存のフォーカス復帰時の自動再列挙などを
    経て）一覧が更新され新しいプロセスが自動的に選び直される→「開始」を押すだけで
    同じ設定のまま再実行できる、という流れになる。プリセット読込
    （`targetAppNameHint`）にも同じ仕組みを適用した。
  - 実機（Xvfb + openbox）で、`kill -9`によるクラッシュ誘発→対象アプリの再起動→
    「更新」でコンボボックスが新しいpidへ自動的に選び直されること、ステップ・操作領域・
    タイミング設定が保持されたまま「開始」だけで再実行できることを確認した。
    -Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。
- v0.50: 先に追加したHTML操作マニュアル（`docs/manual.html`・`docs/manual_en.html`、
  実機スクリーンショット付き）を、将来GitHub Pages（Settings > Pages > Deploy from
  a branch > `/docs`）で公開することを見据えてディレクトリ構成を再編した（3節）。
  - `git mv`で`docs/manual.html`→`docs/ja/index.html`、`docs/images/`→
    `docs/ja/images/`、`docs/manual_en.html`→`docs/en/index.html`、
    `docs/images_en/`→`docs/en/images/`へ移動し、履歴を保持した。両ファイル内の
    相互リンク（言語切り替えバッジ）も新しい相対パス（`../ja/`・`../en/`）に
    合わせて更新した。
  - ルートに`docs/index.html`を新設。ブラウザの`navigator.language`を見て
    `ja/`または`en/`へJavaScriptでリダイレクトするランディングページとし、
    JavaScript無効時のフォールバック用に両言語へのリンクも直接表示する
    `<noscript>`対応も入れた。
  - Jekyll処理を無効化しhtmlをそのまま配信させるため、空の`docs/.nojekyll`を追加した。
  - `file://`での直接閲覧はディレクトリの`index.html`を自動解決しない
    （実サーバ・GitHub Pagesとは挙動が異なる）ため、検証は`docs/`直下で
    `python3 -m http.server`を立てて行った。ルート`/`からのリダイレクトが
    ブラウザ言語設定通りの言語へ飛ぶこと、`/ja/`・`/en/`それぞれがマニュアル本文と
    画像（新しい相対パス）を正しく表示すること、言語切り替えバッジの相互リンクが
    正しく動作することを、ヘッドレスChromiumのスクリーンショットで確認した。
    アプリ本体のソースコードには変更なし（ドキュメント・ディレクトリ構成のみ）。
- v0.51: 「テスト対象ツールによってはある操作の後に確率的にツールが落ちるようなバグを
  解析する。その際には何回テストして何回ツールが落ちたのかをまとめる必要がある」という
  要望を受け、10節で提案した5件（③複数回実行の集計・①連続自動実行・②対象アプリ自動
  起動・④ステップ別クラッシュ率バッジ・⑤直前操作ログ添付）を実装した（詳細・実機確認
  内容は6.1/6.7節を参照）。
  - `TestStatistics`（新設、`src/TestStatistics.h/.cpp`）: 実行結果をテスト設定の
    フィンガープリント単位でJSON-Lines（`crash_stats.jsonl`）に永続化し、試行回数・
    クラッシュ率・ステップ別内訳を集計する。`StatisticsDialog`（新設、
    `src/StatisticsDialog.h/.cpp`）で表示・CSVエクスポート・リセットできる。
  - `RunSummary`に`crashStepIndex`（クラッシュ時のステップ、0始まり）・`recentActions`
    （直近15件の操作説明のリングバッファ）を追加。
  - `MainWindow`に「連続実行回数」スピンボックス（バッチ実行、`beginRun()`/
    `continueBatchIfNeeded()`/`waitForTargetThenContinueBatch()`）と、①対象選択に
    対象アプリの自動起動コマンド欄（`QProcess::splitCommand()`+`startDetached()`）を
    追加。バッチ実行は自動起動コマンドが未設定でも、対象アプリの手動再起動を検知して
    継続する。
  - ②ステップ一覧に、③の集計に基づく「⚠ クラッシュ N/M回」バッジを追加
    （`describeStep()`/`updateStatisticsDisplay()`）。
  - 実機テストで2件の既存不具合を発見し合わせて修正した: ①クラッシュ通知ダイアログ
    （v0.48）が`QMessageBox::critical`によるモーダル表示だったため、バッチ実行中の
    クラッシュのたびにアプリ全体が停止し無人運用を妨げていた問題（`Qt::WA_DeleteOnClose`
    付きの非モーダル`show()`に変更）。②`MainWindow::onEngineFinished()`が
    `m_currentRunningStepIndex`をリセットする前に`describeStep()`を呼んでいたため、
    実行終了後も②ステップ一覧に「▶ 実行中」表示が残り続けていた既存バグ（リセット後に
    呼ぶ順序へ修正）。
  - 実機（Xvfb + openbox）で、バッチ実行＋自動起動コマンドありの構成で`kill -9`と
    `TestTarget`の「クリック厳禁」終了ボタンへの偶発クリックの両方を経由した連続クラッシュ
    →自動再起動→続行が完走すること、統計ダイアログ・CSVエクスポート・リセットが正しい
    ことを確認した。さらに自動起動コマンド欄を空にし、`kill -9`後に手動で対象アプリを
    再起動してから改めて「開始」を押す完全手動運用でも、試行回数・クラッシュ率・ステップ
    別バッジが同じように正しく積算されることを確認した。
    -Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。
- v0.52: 「全体の実行テストを行なって不具合がないか修正してください」という依頼を受け、
  v0.51までの新機能に限らず、既存の①②③パネル・タイミング/制限・プリセット保存/
  読込・ログ/サマリー/操作領域画像の保存・グローバル緊急停止ホットキー・日本語UIを
  含めたアプリ全体の実機回帰テスト（Xvfb + openbox）を行った。
  - **対象選択・操作領域・ステップ**: 名前付き操作領域の追加（矩形描画・除外矩形・
    「対象ウィンドウの移動に追従させる」）・編集・削除、ステップの追加（名前付き
    領域参照／対象GUI全領域）・待機ステップ追加・編集・削除・並べ替え・複数選択での
    グループ化／グループ解除（`StepGroupEditorDialog`でのメンバー設定保持を含む）、
    ③操作パラメータのステップ別カスタム設定切り替えと「デフォルト」ボタン
    （`DefaultActionParamsDialog`）を一通り確認し、いずれも正しく動作することを確認した。
  - **タイミング・制限とプリセット**: ms指定/回数指定（レート）モードの切り替え、
    乱数シード明示指定、①の自動起動コマンド・②の連続実行回数を含む全設定をテスト
    設定として保存し、いったんステップ・操作領域を全削除した状態から読み込み直して
    完全に復元されること（対象アプリの自動再選択を含む）を確認した。
  - **ログ・サマリー・スクリーンショット保存**: 「ログを保存...」「実行結果サマリーを
    保存...」（JSON、⑤の直近操作ログを含む）「操作領域画像を保存...」（保存された
    PNGに操作領域名・除外領域のラベルが正しく描画されていることを画像として確認）、
    グローバル緊急停止ホットキー（Ctrl+Alt+Shift+Esc、対象アプリ側にフォーカスが
    あってもテストが停止すること）が正しく動作することを確認した。なお、メイン
    ウィンドウ側の「‖ 一時停止」ボタンをクリックするためにOS上のフォーカスを対象
    アプリから奪うと、右クリックメニュー処理中の安全チェック（6.7節、fail-closed）が
    「対象アプリがアクティブでなくなった」と正しく判定し安全側に停止する場合がある
    ことを実機で確認した（バグではなく、設計どおりのfail-closed動作）。
  - **【発見・修正】Qt標準ダイアログ部品（OK/Cancel/Close、はい/いいえ、ファイル
    選択ダイアログ等）が日本語モードでも常に英語表示のままになる不具合**
    （詳細は6.12節のv0.52を参照）: このアプリ独自の`I18n::t()`翻訳レイヤーは
    アプリ自身のコード中に書かれた日本語文字列だけを対象としており、
    `QDialogButtonBox`の標準ボタン・`QInputDialog`・`QMessageBox`の標準ボタン・
    `QFileDialog`の内部ラベルなど、Qt自身が内部的に持つ標準UI文字列は対象外だった
    ため、これまで一度も日本語化されたことがなかった（v0.47の多言語対応時にも
    見落とされていた）。Qt自身が保有する日本語訳（`qtbase_ja.qm`）をアプリ起動時に
    `QTranslator`で読み込み登録するよう`main.cpp`を修正し、これらのQt標準部品も
    含めてアプリ全体が正しく日本語化されるようにした。該当ファイルが見つからない
    環境では静かに英語表示へフォールバックするため、後方互換上のリスクはない。
  - 上記の修正を反映した上で-Wall -Wextra -Wpedantic付きのクリーンビルドを
    再度確認し、日本語・英語両モードで主要画面に問題がないことを実機で確認した。
- v0.53: 「テストツールの起動後にある決められた操作を行なってからランダム操作を
  行いたい」という要望を受けて提案した4機能（10章参照）のうち①「起動時セットアップ
  マクロ」・②「実行タイミング設定（連続自動実行の初回のみ／毎回）」・④「失敗時の
  検知・診断強化（安全確認失敗時の再試行と異常停止）」を実装した（詳細は6.13節）。
  ③「記録（レコーディング）モードでの作成支援」は、真のシステム全体パッシブ入力
  記録（X11 XInput2の生イベント購読やmacOS CGEventTapなど、プラットフォームごとの
  低レベルフック実装が必要で、対話的に検証できないmacOS側のリスクが大きい）
  ではなく、`PointPickerOverlay`によるクリックでの座標指定＋直接のテキスト/キー
  入力という、それ自体で「座標を手入力しなくて済む」という③の主目的を満たす形に
  スコープを絞って実装した。
  - `TestConfig::setupActions`（`SetupAction`/`SetupActionType`）、`PointPickerOverlay`、
    `SetupActionEditorDialog`を新設。`RandomActionEngine`に起動時セットアップ
    フェーズの実行ロジック（`performSetupAction`/`trySetupAction`/
    `retrySetupOrFail`）を追加し、①パネルにセットアップ一覧UIと「連続自動実行では
    初回のみ実行する」チェックボックスを追加、プリセット保存/読込にも対応した。
  - 実機（Xvfb + openbox、日英両モード）で、Wait/Click両種別のセットアップ操作が
    ②の乱数操作開始前に正しく1回だけ実行されること（Clickは対象アプリの実際の
    ボタンを正しくクリックできることをTestTarget側のカウンタで確認）、プリセット
    保存/読込の往復、「初回のみ実行」チェックがバッチ2回目以降のセットアップを
    正しくスキップすることを確認した。この過程で1件、英語モードの種類コンボ
    ボックスで「文字入力」（TypeText）が未翻訳のまま表示される不具合を発見・修正
    した（ログ用の「文字入力 '%1'」とは別キーだったため翻訳テーブルへの登録が
    漏れていた）。
  - -Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。
- v0.54: 「コードレビューとテスト実行を行なってバグ修正をしてください」という
  依頼を受け、v0.53で追加した起動時セットアップ機能について差分全体のコード
  レビューを行い、4件の不具合を発見・修正した（詳細は6.13節のv0.54を参照）。
  - 座標を一度も選択しないまま「起動時セットアップ操作の設定」ダイアログの
    「OK」を押せてしまい、既定値`QPoint(0, 0)`のまま保存された結果、実行時に
    安全確認が必ず失敗して対象アプリは正常なのに異常停止扱いになる不具合を修正
    （座標が実際に選択されたかを追跡し、未選択なら入力エラーで弾くように変更）。
  - 文字入力（TypeText）セットアップの内容が、パスワード等の想定ユースケースに
    もかかわらず画面ログ・完全ログファイル・異常停止時の診断一式（`RunSummary::
    recentActions`）に平文のまま記録されていた不具合を修正（文字数のみを記録する
    よう変更し、ダイアログにも案内文を追加）。
  - `PointPickerOverlay`と`RegionSelectorOverlay`に重複していたジオメトリ
    ヘルパー関数を`src/OverlayGeometry.h`に共通化。
  - プリセット読込の上書き確認ダイアログの文言に「起動時セットアップ」が
    含まれていなかった不具合を修正。
  - 実機（Xvfb + openbox、日本語モード）で4件すべての修正を再現・確認し、
    -Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持した。
  - `docs/ja/index.html`・`docs/en/index.html`（操作マニュアル）に、起動時
    セットアップ機能を説明する新しい節（日本語版「5. 起動時セットアップ
    （ログイン等を一度だけ自動実行）」、英語版「5. Startup setup (running
    login etc. automatically, once)」）を追加し、以降の節番号を繰り下げた。
    この過程で、英語版マニュアルの画像パスが`images_en/`という存在しない
    ディレクトリを指したままになっており（`a77f8ec`でのdocs/ディレクトリ
    再編時に更新漏れ）、7枚の図が公開ページ上ですべて表示されていなかった
    不具合を発見・修正した（実際のディレクトリ名`images/`に合わせて修正）。
    ヘッドレスChromiumでの実描画確認により、新設節・既存の図とも日英両方で
    正しく表示されることを確認した。
- v0.55: 「Linux上の実行で操作領域の追加をしようとすると画面が真っ暗になって
  操作できません。画面を透かしにして操作領域の選択をしやすいようにコードを
  変えてください」という報告を受け、8章に既知の制約として記載していた
  コンポジット非対応環境での黒塗り問題（`Qt::WA_TranslucentBackground`が
  効かない）を、実際に操作不能になる実害を踏まえて解消した（詳細は6.13節の
  v0.55を参照）。
  - `RegionSelectorOverlay`・`PointPickerOverlay`・`RegionHighlightOverlay`を
    `Qt::WA_TranslucentBackground`からスクリーンショット背景方式（表示直前に
    画面を撮影し、不透明な背景として自前で描画）に変更（`OverlayGeometry::
    grabVirtualDesktopSnapshot()`）。
  - 実機テストで新たに発見した2件の関連不具合も修正: (1)
    `RegionHighlightOverlay`（操作領域一覧ハイライト）が画面全体を覆って黒塗り
    になり、開いた瞬間の「操作領域の設定」ダイアログごと隠してしまう不具合、
    (2) `openbox`が常時最前面ヒントを優先するため`RegionHighlightOverlay`の
    ウィンドウがダイアログより手前に居座り、ダイアログ自体を操作不能にする
    不具合（ダイアログに同じ`Qt::WindowStaysOnTopHint`を付与して解消）。
  - X11のunmapが非同期であることに起因する3件目の不具合（閉じた直後の
    `RegionSelectorOverlay`のヒントバーがハイライトの背景に焼き付く）も発見・
    修正し、`OverlayGeometry::settleDesktopBeforeSnapshot()`で生の画面グラブ前に
    必ず一呼吸置くようにした。
  - 実機（Xvfb + openbox、コンポジットマネージャなし）で、ダイアログを開いた
    瞬間・矩形描画中・確定直後のすべての段階で背景が正しく透けて見え、焼き付き等の
    見た目の不具合が無いことを確認した。-Wall -Wextra -Wpedantic付きの
    クリーンビルドで警告0件を維持。
- v0.66: 「起動時セットアップの記録機能にホイール操作や特定のメニューの選択が
  記録されるようにしてください」という要望を受けて実装した（詳細は6.13節のv0.66を
  参照）。`InputRecorder`を拡張し、連続するホイール操作を1つの`Scroll`アクションに
  まとめて記録する機能と、右クリック後の猶予時間（600ms）内の左クリックを
  `PlatformAutomation::accessibleNameAtPoint()`でAT-SPI/Accessibility越しに
  識別して1つの`MenuSelect`アクションにまとめる機能を追加した。項目名が
  取得できない場合はフェイルソフトで従来通り`RightClick`として記録し、後続の
  クリックも取りこぼさない。実機（Xvfb + openbox）で、フェイルソフト経路
  （プレーンなボタンへの右クリック→別ボタンへの左クリックが別々の
  `RightClick`/`Click`として正しく記録される）を確認した。AT-SPI経由で項目名が
  実際に取得できる方の経路は、このテスト環境固有の制約（D-Busセッションバス
  不在によるAT-SPI常時無効化、および本物のポップアップメニューが能動グラブを
  持つ間は合成クリックの生イベントがこのXvfbでは配信されないこと）により実機
  確認ができなかった（詳細と根拠は6.13節のv0.66を参照）。-Wall -Wextra
  -Wpedantic付きのクリーンビルドで警告0件を維持。
- v0.67: 「設定した起動時セットアップが実際に問題なく動くかどうかの確認するために、
  起動時セットアップを実行する機能が欲しい」という要望を受けて実装した（詳細は
  6.13節のv0.67を参照）。①に「▶ 起動時セットアップを試す」ボタンを追加し、
  押すと①のセットアップだけを対象ウィンドウに対して実行して自動的に停止する
  （②のステップ構成は未設定のままでよい）。`RandomActionEngine::
  startSetupOnly()`を新設し、既存の`start()`/セットアップフェーズ実行ロジックを
  そのまま共有しつつ、セットアップ完了時に②のランダム操作ループへ進まず
  停止するようにした。②のステップに基づくクラッシュ率統計や異常時プリセット
  自動保存など、この実行には意味を持たない副作用は`MainWindow`側で
  スキップするようにした。実機（Xvfb + openbox）で、未設定時のエラー
  ダイアログ、実際のクリックがTestTargetへ届くこと、完了後のステータス表示、
  ③統計欄が変化しないことを確認した。-Wall -Wextra -Wpedantic付きの
  クリーンビルドで警告0件を維持。
- v0.68: 「連続実行ボタンを押してツールがすぐに立ち上がらないとエラーができます。
  テスト対象のツールは場合によっては起動に時間がかかる可能性もあるので、起動まで
  一定時間待つパラメータを設定できるようにしてください」という要望を受けて実装
  した（詳細は6.1節のv0.68を参照）。①に「対象ツールの起動検知後、さらに待つ時間」
  スピンボックス（0〜300秒、デフォルト0=従来通り）を追加し、連続実行・①バッチの
  自動再起動待ち・▶開始の「起動してから開始する」の3経路すべてで、対象ウィンドウ
  検知後この秒数だけ追加で待ってから実際にテストを開始するようにした
  （`MainWindow::startRunAfterLaunchWait()`）。待ち時間中の「■停止」による中断にも
  対応。実機（Xvfb + openbox）で、3秒設定時に指定通りの遅延で開始すること、
  待ち時間中の停止が正しく機能することを確認した。-Wall -Wextra -Wpedantic付きの
  クリーンビルドで警告0件を維持。
- v0.69: 「設定した起動時セットアップや操作領域、ステップ構成などの情報を保存できる
  ようしてください。その上でGUIを立ち上げなくてもターミナル実行で保存した手順を
  引数として与えることでテストを実行できるようにしてください。その際連続実行の
  回数も引数として設定できるようにしてください」という要望を受けて実装した
  （詳細は新設した6.14節を参照。プリセット保存/読み込み自体は6.1節v0.51で対応済み
  だったため、今回の実装はターミナルからの直接実行経路のみ）。
  `EnduranceTestGUI --run <preset.json> [--repeat N]`というコマンドライン引数を
  新設し、ウィンドウを一切表示せずに「⟳ 連続実行」と全く同じキル→起動→実行の
  サイクルを指定回数繰り返した後、自動的にプロセスを終了するようにした
  （`MainWindow::runHeadless()`/`checkHeadlessCompletion()`）。進捗はすべて標準
  出力へミラーし、終了コードは異常（クラッシュ等）が1回でも発生していれば1、
  無ければ0とした。実機（Xvfb + openbox）で、GUIで作成・保存したプリセットを
  `--repeat 2`で実行し、ウィンドウ非表示のまま2回とも実際にTestTargetへ操作が
  届き、正常終了（終了コード0）することを確認した。-Wall -Wextra -Wpedantic付きの
  クリーンビルドで警告0件を維持。
- v0.70: 「ステップ構成のタスク内では操作途中で出てくるダイアログも操作できるように
  してください。特に出てきたダイアログのボタンを押したり特定の操作をできるように
  したいです」という要望を受けて実装した（詳細は6.2節のv0.70を参照。ダイアログ自体を
  操作領域として解決する土台はv0.62で対応済みだったため、今回の実装はその上に載せる
  具体的な「ボタンを押す」操作）。既存の右クリックメニュー項目選択
  （`enableContextMenuSelection`/`contextMenuItemNames`）と同じ「候補名を複数登録し、
  実在するものをOSのアクセシビリティAPIから探して押す」設計を踏襲し、
  `RegionStep::enableDialogButtonPress`/`dialogButtonPressWeight`、
  `ActionParams::dialogButtonNames`、新しい`ActionKind::DialogButtonPress`、
  `PlatformAutomation::clickButtonByName`（macOS Accessibility API / Linux AT-SPI）を
  追加した。`targetsPopupDialog`なタスクメンバーでのみ選択可能で、候補ボタンが見つから
  ない場合はスキップして次のティックで同じメンバーを再試行する（タスクの固定順序を
  保つ既存挙動）。実機（Xvfb + openbox）では、AT-SPI自体が環境上無効
  （`DBUS_SESSION_BUS_ADDRESS`未設定、v0.59と同じ既知の制約）のため「実際に見つけて
  押す」成功経路そのものは検証できなかったが、それ以外の全経路（ダイアログ出現待ち→
  領域解決→操作発火→候補を順に試す→見つからずスキップして安全に再試行→時間制限での
  正常停止）を手作業で組み立てたプリセットJSONのCLIヘッドレス実行（`--run`）で確認した。
  -Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。
- v0.71: 「テスト対象ツールの操作応答が遅い時に自動でテスト操作を減速するオプションを
  つけてください」という要望を受けて実装した（詳細は6.7節のv0.71を参照）。既存の
  ハング検知（`PlatformAutomation::checkWindowResponsive`、`_NET_WM_PING`を8秒間隔で
  ポーリング）の仕組みをそのまま流用し、`TestConfig::autoSlowdownEnabled`
  （デフォルトOFF、①「対象アプリの応答が遅い時は操作間隔を自動的に延ばす」）を有効にすると、
  WM_PINGへの応答を1回だけ逃した時点（＝ハングと確定する2回連続失敗の前）で操作間隔を
  一時的に5倍へ延ばし、応答が戻れば自動的に元へ戻す。ハング確定時の停止条件自体は
  変更していない。実機（Xvfb + openbox）では、`TestTarget`のイベントループを一時的に
  止めるデバッグ用ボタンで検証用プリセットをCLIヘッドレス実行したが、この環境の
  `TestTarget`/Qt-xcbビルドが`_NET_WM_PING`に対応を表明しながら実際には一度も応答しない
  という8章記載の既知の制約により、既存のハング検知自体の成功経路と同様、本オプションが
  狙う「1回だけ遅れて延ばし、その後回復して戻る」という経路そのものは検証できなかった
  （それ以外の経路——オプション無効時の非干渉、有効時に異常なく通常どおり完走すること——は
  確認した）。デバッグ用ボタンは検証後に削除。-Wall -Wextra -Wpedantic付きのクリーン
  ビルドで警告0件を維持。
- v0.72: 「テスト対象ツールの全体のウィンドウサイズを保存する機能と、保存した
  ウィンドウサイズに合わせてテスト対象ツールのウィンドウサイズを変更する機能を
  つけてください」という要望を受けて実装した（詳細は6.1節のv0.72を参照）。①対象選択
  パネルに保存済みサイズを表示するラベルと「現在のウィンドウサイズを保存」「保存した
  サイズに変更」の2ボタンを追加し、既存の「ウィンドウ操作」リサイズ種別（6.5節）が
  使っているのと同じ`PlatformAutomation::resizeWindow`で復元する。保存は1件のみ、
  プリセットJSON（`hasSavedWindowSize`/`savedWindowWidth`/`savedWindowHeight`）にも
  保存/復元される。実機（Xvfb + openbox）で、`TestTarget`のサイズを保存→変更→復元し、
  対象アプリ自身が自己申告するサイズと保存時の値がピクセル単位で一致することを確認した
  （対象アプリ自身のレイアウト上の最小サイズを下回る値を指定した場合に丸められる挙動は、
  `xdotool`による直接リサイズでも同様に起きることを確認済みの、既存の「ウィンドウ操作」
  リサイズ種別と共通のQtレイアウト制約であり、今回のリサイズ呼び出し自体の精度問題では
  ない）。-Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。
- v0.73: 「テスト対象ツールの現在のウインドウ位置を保存する機能と、保存したウィンドウ
  位置に合わせてテスト対象ツールのウィンドウ位置を変更する機能をつけてください」という
  要望を受けて実装した（詳細は6.1節のv0.73を参照）。v0.72のウィンドウサイズ保存・復元と
  全く同じ設計で、「現在のウィンドウ位置を保存」「保存した位置に変更」の2ボタンを追加し、
  既存の「ウィンドウ操作」移動種別（6.5節）が使っているのと同じ
  `PlatformAutomation::moveWindow`で復元する。保存は1件のみ、プリセットJSON
  （`hasSavedWindowPos`/`savedWindowPosX`/`savedWindowPosY`）にも保存/復元される。
  実機（Xvfb + openbox）で、`TestTarget`を`xdotool windowmove`で移動→保存→別の位置へ
  再度移動→「保存した位置に変更」で、`xdotool getwindowgeometry`で見た座標が保存前の
  位置へぴったり戻ることを確認した（アプリ内部の座標取得とxdotoolの座標取得の間には
  ウィンドウマネージャの装飾枠を含むかどうかによる約20pxの表示差があるが、それぞれの
  座標系内では保存・復元が常に一致することを確認済み）。-Wall -Wextra -Wpedantic付きの
  クリーンビルドで警告0件を維持。
- v0.74: 「連続実行を実行する時に、テスト対象ツールのウィンドウ位置やウィンドウサイズを
  保存したものに合わせて変更するオプションをつけてください」という要望を受けて実装した
  （詳細は6.1節のv0.74を参照）。①対象選択パネルに「連続実行時に保存済みのウィンドウ
  位置・サイズへ変更する」チェックボックス（デフォルトOFF）を追加し、有効時は対象アプリが
  （再）起動して検知されるたびに、v0.72/v0.73で保存済みのウィンドウサイズ・位置が
  あればそれぞれ独立に適用してから実行を始める（`MainWindow::
  applySavedWindowGeometryIfEnabled()`）。適用箇所はv0.68の「起動待ち時間」と同じ
  `startRunAfterLaunchWait()`の先頭にしたため、「⟳ 連続実行」のキル→再起動サイクル・
  ①バッチ実行の自動/手動再起動待ち・「▶ 開始」の「起動してから開始する」オプションの
  3経路すべてに共通で適用される。プリセットJSON（`applySavedGeometryOnRelaunch`）にも
  保存/復元される。実機（Xvfb + openbox）で、サイズ(1250x950)・位置(300,200)を保存済みに
  した状態でCLIヘッドレス実行（`--run`は常に対象を一度キルしてから新規起動するため
  「⟳ 連続実行」相当の経路を踏む）し、新しいインスタンス検知直後に両方の変更ログが
  出力され、その後テストが異常なく正常完走（終了コード0）することを確認した。
  -Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。
- v0.63: 「少なくともLinux環境では、操作領域の追加で矩形を描画を押すと...指定した
  位置と実画面での位置が少しズレています...矩形を描画するモードで選択した位置と
  実際に操作する操作領域がずれないように修正してください」という報告を受け、
  コードレビューでHiDPI環境での実際のバグを発見・修正した（詳細は6.3節のv0.63を
  参照）。
  - `OverlayGeometry::grabVirtualDesktopSnapshot()`が合成する背景スクリーンショットの
    `QPixmap`がdevicePixelRatioを明示的に設定していなかったため、拡大率(dpr)>1の
    HiDPI環境で背景画像がオーバーレイの論理座標系に対して実際の拡大率倍大きく
    描画され、見えている位置と実際に記録される矩形座標が画面端に近いほど大きく
    ズレる不具合があった。合成先ピクスマップにプライマリスクリーンのdprを明示的に
    設定するよう修正し、`RegionSelectorOverlay`/`PointPickerOverlay`の両方に適用した。
  - 実機（Xvfb + openbox、dpr=1固定のためHiDPI起因のズレ自体は再現できなかった）で、
    TestTargetの実ボタンにピクセル単位で位置を合わせて矩形を描画し、記録される座標が
    ドラッグ座標と完全一致すること（修正前後で結果が同一、退行が無いこと）を確認した。
    HiDPI環境での改善効果自体、および「ダイアログが2つ出る」不具合（#51）との関連は
    未確認のまま。-Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。
- v0.64: 「起動時セットアップに全消去のボタンを追加してください」という要望を受けて
  実装した（詳細は6.13節のv0.64を参照）。②ステップ構成の「すべて削除」と同じ方式で
  「全消去」ボタンを追加し、確認ダイアログ無しで即座に起動時セットアップの一覧を
  空にする。実機（Xvfb + openbox）で動作確認済み。-Wall -Wextra -Wpedantic付きの
  クリーンビルドで警告0件を維持。
- v0.65: 「起動時セットアップの操作にホイール操作や特定のメニューの選択を操作として
  追加してください」という要望を受けて実装した（詳細は6.13節のv0.65を参照）。
  `SetupActionType`に`Scroll`（位置・方向・量を指定する1回分のホイール操作）と
  `MenuSelect`（右クリックで開いたメニューから、名前指定または上から何番目かの指定で
  常に同じ1項目を選ぶ）を追加し、`SetupActionEditorDialog`にそれぞれ専用の入力ページを
  追加した。既存の`RightClick`（開いたメニューは常に閉じるだけ）とは別の種別として
  新設し、対象アプリのメニュー内容が実行ごとに変わる場合でも指定項目が見つからなければ
  フェイルソフトでメニューを閉じるだけに留める。実機（Xvfb + openbox）で両種別の
  UI・値の記録を確認。-Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。
- v0.62: 「タスクの実装に伴って、テスト対象のアプリの操作中に出現したダイアログの
  操作をできるようにしてください」という要望を受けて実装した（詳細は6.2節のv0.62を
  参照）。
  - `RegionStep::targetsPopupDialog`をタスクのメンバー専用のオプションとして追加。
    有効なメンバーは自身の領域設定の代わりに「対象アプリのメインウィンドウ以外に
    新しく出現したウィンドウ」を操作領域として実行時に自動解決する
    （`RandomActionEngine::resolveStepRegion`）。`TaskEditorDialog`のメンバー編集
    ダイアログにのみ、この領域源を選ぶ3つ目の選択肢を追加した。
  - 既存の「予期しないウィンドウの自動解消」（`handleUnexpectedWindows`）と競合
    しないよう、`targetsPopupDialog`なメンバーを実行しようとしている間だけそれを
    抑制する（`currentActionTargetsPopupDialog`）。ダイアログがまだ現れていない
    場合は同じメンバーを再試行し（上限あり、既存の「閉じられない場合」と対称的な
    上限超過時は異常停止）、逆にそのメンバーを離れた後も余分なウィンドウが残って
    いれば既存の自動解消がそのまま後始末する。
  - 実機（Xvfb + openbox）で、`TestTarget`の終了確認ダイアログを実際に開いて
    そのダイアログ上で操作できること、既存の自動解消機構との共存が設計どおり
    働くことを確認した。-Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を
    維持。
- v0.61: 「ステップ構成にタスクを登録できるようにしてください。タスクはいくつかの
  操作フローを一つにまとめた操作で、タスク全体を一つの操作としてください（タスク内で
  操作はランダムに前後などはしない）」という要望を受けて実装した（詳細は6.2節の
  v0.61を参照）。
  - `RegionStep`に`isTask`/`taskMembers`を追加。グループ（メンバーから重み付きで
    ランダムに1つだけ選んで実行）とは対照的に、タスクは登録した順番で全メンバーを
    必ず1回ずつ実行し、一巡全体で「実行回数」1回分としてカウントする。
  - ②ステップ構成リストに「タスク化」「タスク解除」ボタンを新設し、複数の通常
    ステップを選択してタスク化する/タスクをメンバーへ展開して解除する操作を、
    既存のグループ化/グループ解除と同じ使用感で行えるようにした。
  - `TaskEditorDialog`（`StepGroupEditorDialog`からグループ固有の合計呼び出し
    回数・重みを除いた専用エディタ）、`RandomActionEngine::performTaskAction`
    （`m_currentTaskMemberIndex`で現在のメンバー位置を管理し固定順序で1メンバーずつ
    進める）を新設し、JSONプリセットの保存/読み込み・名前付き操作領域の参照追従・
    実行前検証もグループと同様にタスクのメンバーまで再帰的に対応させた。
  - 実機（Xvfb + openbox）で、タスク化したステップの実行順序が常に登録順どおりに
    なること、実行回数がメンバー数に関わらず1巡＝1回としてカウントされること、
    タスク解除で元のステップ構成に復元されることを確認した。実装後にI18n.cpp
    未対応の既存文言（v0.59の開始/連続実行オプション、v0.60の記録機能）が
    残っていることも英語UIでの確認テストを通じて発見し、あわせて翻訳を追加した。
    -Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。
- v0.60: 「起動時セットアップに記録ボタンをつけて、記録ボタンを押すと次に
  エスケープボタンが押されるまでのユーザー操作を記録して起動時セットアップに
  記録されるようにしてください」という要望を受けて実装した（詳細は6.13節の
  v0.60を参照）。
  - 新設`InputRecorder`がマウスクリック・ドラッグ・キーボード入力をシステム
    全体で観測し（Linux: XInput2 rawイベント購読、macOS: CGEventTapの
    listen-onlyモード。いずれも入力を横取りせず素通しで観測するだけなので、
    対象アプリが開くダイアログを操作している最中でも記録できる）、①の新設
    「● 記録...」ボタンを押すとEscキーが押されるまでの操作をリアルタイムで
    起動時セットアップ一覧に追加していく。記録中は常に最前面の小さいパネル
    （`RecordingIndicatorPanel`）を表示し、対象アプリ側にフォーカスが移って
    いてもいつでも終了できるようにした。
  - `PlatformAutomation::terminateProcess()`同様、Linux/macOS両対応の新規
    プラットフォームバックエンド追加（`InputRecorder_linux.cpp`/
    `InputRecorder_mac.mm`）。CMakeLists.txtにXInput2拡張（libXi）の依存を
    追加した（既存のXTest同様、見つからなければビルド時にエラーにする）。
  - 実機（Xvfb + openbox）での動作確認中に、XTestで合成したクリックの
    ボタン解放（RawButtonRelease）がこの仮想Xサーバー環境では配送されない
    ことを専用の切り分けテストで発見し、ボタン押下中は`XQueryPointer`で
    ボタン状態をポーリングして自前で解放を検出するフォールバックを追加して
    回避した（詳細は6.13節参照）。
  - 実機（Xvfb + openbox）で、クリック・ドラッグ・右クリック・文字列入力・
    Tab/Returnキー入力が種別・座標とも正しく記録され、Escキーで記録が
    終了することを確認した。-Wall -Wextra -Wpedantic付きのクリーンビルドで
    警告0件を維持。
- v0.59: 「開始ボタンにテスト対象ツールの起動から始めるかどうかのオプションを実装して
  ください」「連続実行は開始ボタンとは別の連続実行用のボタンを作成してください」という
  2件の要望を受けて実装した（詳細は6.7節のv0.59を参照）。
  - ①対象選択に「開始時にこのコマンドで対象ツールを起動してから開始する」チェック
    ボックスを新設。オンで「▶ 開始」を押すと、②の自動起動コマンドで対象アプリを
    まず起動し、出現を確認してから起動時セットアップ→ステップ構成の実行を始める。
  - 「▶ 開始」とは独立した「⟳ 連続実行」ボタンを新設。押すたびに（対象アプリが
    生きているかどうかに関わらず）必ず一度終了させてから新しいインスタンスを起動
    し直し、起動時セットアップ→ステップ構成を実行、これを「連続実行回数」の指定
    回数だけ繰り返す。`PlatformAutomation::terminateProcess(pid)`
    （Linux/macOSともSIGTERM送信、既存の`isProcessRunning()`と同じ`kill()`ベース）を
    新設した。
  - 実装中に、①バッチ実行の継続処理（`beginRun(false)`失敗時にコントロールの
    再有効化を一切行っていなかった）を確認し、新機能の2つの待機パス
    （「起動してから開始する」の初回起動待ち、連続実行のキル→再起動待ち）を
    含めて三箇所とも、`beginRun()`が失敗した場合は`setControlsEnabled(true)`で
    確実にUIを操作可能な状態へ戻すよう統一した（副次的な既存の潜在バグ修正、
    実運用では権限喪失等の稀なケースでのみ影響）。
  - 実機（Xvfb + openbox）で、TestTargetが未起動の状態から「開始時に...起動してから
    開始する」を有効にして「▶ 開始」を押すと自動起動→検知→実行が完走すること、
    自動起動コマンド欄を空のままオンにすると設定エラーが表示されること、を確認した。
    「⟳ 連続実行」を連続実行回数2で押すと、1回目はTestTarget未起動状態から新規
    起動して実行、1回目終了後に実際にプロセスが終了→pidの異なる新しいインスタンス
    が起動して2回目が実行され、「連続実行が完了しました（2/2回）」でログ・
    ①の対象表示（新しいpid）ともに正しく完了することを確認した。-Wall -Wextra
    -Wpedantic付きのクリーンビルドで警告0件を維持。
- v0.58: 「起動時セットアップで追加ボタンでセットアップを追加するとき、選択された
  位置や開始位置、終了位置をダイアログ表示中だけ表示をしてください」という要望を
  受けて実装した（詳細は6.13節のv0.58を参照）。
  - 新設`PointHighlightOverlay`（`RegionHighlightOverlay`と同じ設計）で、
    `SetupActionEditorDialog`が開いている間、選択済みの点（Click系は1点、
    Dragは開始・終了の2点を破線で結んで）を画面上にラベル付きマーカーとして
    表示し続けるようにした。
  - 実装直後の実機テストで、`NamedRegionEditorDialog`がv0.55より前に抱えていた
    のと同じ原因（常に最前面のオーバーレイの下にダイアログ自身が隠れる）で
    `SetupActionEditorDialog`が見えなくなる不具合を発見し、同じ対処
    （`Qt::WindowStaysOnTopHint`の付与＋`raise()`/`activateWindow()`）で修正した。
  - 実機（Xvfb + openbox）で、Click/Drag両方の種別でマーカーが正しい位置に
    表示されること、ダイアログの開閉に連動してマーカーが表示/非表示されること、
    既存エントリの再編集でも保存済みの点が正しく再表示されることを確認した。
    -Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を維持。
- v0.57: 「対象ウィンドウの移動に追従させる」オプションが効かないという報告を受け、
  実機で原因を再現・特定して修正した（詳細は6.3節のv0.57を参照）。
  - `NamedRegionEditorDialog::result()`が、追従オプション有効時に矩形を描き
    直さない編集（名前変更のみ等）でも基準点(`anchorTopLeft`)を無条件に
    現在位置へ上書きしてしまい、矩形自体は古い基準点のままずれが生じる
    不具合を修正（矩形が実際に変更されたかを追跡し、変更が無ければ既存の
    基準点を維持するよう変更）。ずれが大きい場合は「対象アプリ以外の
    ウィンドウを操作しそうになったため停止」の安全装置が働いてテストが
    異常停止することもあった。
  - `NamedRegion::followsTargetWindow`のデフォルトを`true`に変更し、新規
    作成する操作領域では「対象ウィンドウの移動に追従させる」チェックボックスが
    初期状態でオンになるようにした（保存済みプリセットの読込には影響しない）。
  - 実機（Xvfb + openbox）で、ウィンドウ移動中もクリックが正しく追従すること、
    矩形を描き直さない編集後も追従が壊れないことを確認した。-Wall -Wextra
    -Wpedantic付きのクリーンビルドで警告0件を維持。
- v0.56: 「ステップ構成で二つのステップでそれぞれ操作を設定した後に二つを一つの
  グループにまとめると最初の設定が消えてしまいます」という報告を受け、
  `StepGroupEditorDialog`（6.2節）でメンバー間の選択を切り替えた際に、切り替え元の
  メンバーの設定が別のメンバーの内容で上書きされてしまう不具合を修正した。
  - **原因**: `StepGroupEditorDialog::loadMemberEditorForSelection()`が、選択中の
    メンバーを指す`m_lastEditedMemberRow`を`ActionKindEditor::setKinds()`の呼び出し
    より*後*に更新していた。`setKinds()`はチェックボックスを1つずつプログラム的に
    切り替えるたびに`changed`シグナルを発火し、これは`flushMemberEditor()`（現在の
    UI表示内容を`m_members[m_lastEditedMemberRow]`へ書き戻す関数）に直結している。
    そのため、メンバーを切り替えた直後の`setKinds()`実行中は`m_lastEditedMemberRow`
    がまだ*切り替え前の*メンバーの行を指したままで、切り替え先メンバーの
    チェックボックス状態（遷移途中の部分的な状態、最終的には遷移先メンバーと同じ
    状態）がそのまま切り替え前のメンバーに書き戻され、上書きされてしまっていた。
    実機で「ステップ1をドラッグのみ、ステップ2をキー入力のみに設定→グループ化→
    「編集...」でメンバー2をクリックしてからメンバー1に戻る」を再現したところ、
    メンバー2の設定がメンバー1と同じ「ドラッグ」に化けることを確認した。
  - **修正**: `MainWindow::loadActionParamsEditorForSelection()`（同種の再入問題に
    既に対応済み）と同じ順序に合わせ、`m_lastEditedMemberRow`と（`flushMemberEditor()`
    が併せて読む）重みスピンボックス・パラメータモードのラジオボタンを、
    `setKinds()`を呼ぶ*前*に確定させるよう並べ替えた。
  - 実機（Xvfb + openbox）で、ドラッグのみ/キー入力のみに設定した2ステップを
    グループ化し、「編集...」内でメンバー間の選択を複数回往復させても両メンバーの
    設定が正しく保持されること、OKで確定して再度「編集...」を開いても保持されて
    いることを確認した。-Wall -Wextra -Wpedantic付きのクリーンビルドで警告0件を
    維持。

## 10. 追加提案（耐久テストツールとしての機能拡張案）

v0.40での実機確認（9章）を踏まえた追加提案。**以下の6件はv0.41で全て実装済み**
（詳細は9章v0.41を参照）。この節は提案時点の記録として残す。

- **予期しない（右クリックメニュー以外の）モーダルダイアログ・ポップアップの検出**:
  v0.40で確認した通り、対象アプリが右クリックメニュー以外の理由で自前のモーダル
  ダイアログ（確認ダイアログ等）を出した場合、6.5節の「右クリック直後にメニューを
  解決する」ロジックの対象外のため、ダイアログが開いたまま以後のランダム操作の
  大部分が何もヒットせず浪費され続ける（安全チェック自体は通過するため停止はしない）。
  対象プロセスの新しいトップレベルウィンドウ出現をポーリングし、想定外のウィンドウが
  一定回数以上「操作を吸っている」と判断したら、ログに警告を出す、Escapeで閉じてみる、
  あるいはテストを一時停止してユーザーに通知する、といった対処を検討する価値がある。
- **ウィンドウ相対座標での名前付き領域**: 8章に既知の制約として記載の通り、名前付き
  操作領域は画面絶対座標で保存されるため対象ウィンドウの移動に追従しない。対象
  ウィンドウの現在位置からの相対オフセットとして保存し、実行時に絶対座標へ変換する
  方式に変更すれば、「対象GUIの全領域」以外のステップでもウィンドウ移動に追従できる。
- **グローバルホットキーによる緊急停止**: 8章に未実装として記載の通り、現状は
  フローティングパネル（StopPanel）のボタンのみが停止手段。対象アプリがフォーカスを
  奪って離さない・入力を吸い込み続けるといった異常時にはパネルの操作自体が難しくなる
  可能性があるため、OSのグローバルホットキー（例: Ctrl+Alt+Esc等）での緊急停止を
  検討する価値がある。
- **実行後サマリーレポート**: 現状はタイムスタンプ付きログの垂直な羅列のみ。停止時に
  「操作種別ごとの実行回数」「シーケンス何回完走したか」「異常停止の理由」「使用した
  乱数シード」等をまとめたサマリー（画面表示、またはテキスト/JSON/CSVでの書き出し）を
  出せると、長時間実行後の結果確認や複数回実行の比較がしやすくなる。
- **テスト設定のプリセット保存・読み込み**: `TestConfig`一式（対象アプリ名、操作領域、
  ステップ構成、操作パラメータ、タイミング・制限）をJSON等のファイルに保存/読み込み
  できるようにすると、同じテスト構成を繰り返し使う場合や、複数マシン・複数バージョンの
  対象アプリに対して同一の耐久テストを再実行する場合に、毎回GUIで組み立て直す手間が
  省ける。
- **対象アプリの無応答（ハング）検知**: 現状のクラッシュ検知はプロセスの生死
  （`isProcessRunning`）のみを見ており、プロセスは生きているがウィンドウが完全に
  無応答になっている（イベントループが固まっている）ケースは検知できない。macOSなら
  `AXUIElement`経由での応答性チェック、LinuxならX11の`_NET_WM_PING`プロトコルへの
  応答有無などで「ハング」を検知し、クラッシュと同様に異常停止・スクリーンショット
  保存の対象に含められると、耐久テストとしての検知範囲が広がる。

「テスト対象ツールによってはある操作の後に確率的にツールが落ちるようなバグを解析する
（何回テストして何回落ちたかをまとめる必要がある）」というユーザーからの要望を踏まえた
追加提案（第2弾）。**以下の5件はv0.51で全て実装済み**（詳細は9章v0.51を参照）。この節は
提案時点の記録として残す。

- **③複数回実行の集計レポート（クラッシュ率の可視化）**: 現状は1回ごとにサマリーを
  保存するだけで、複数回分の比較ができない。セッションをまたいで（アプリを再起動しても）
  実行結果を蓄積し、「総試行回数」「クラッシュ回数／率」「クラッシュしたステップの内訳」
  「クラッシュまでの平均実行回数・経過時間」をCSV/JSONで書き出せるようにすると、まさに
  「何回テストして何回落ちたか」を数値でまとめられる。同じテスト構成（ステップ・操作
  領域・パラメータ・タイミング）を対象アプリの手動再起動を挟んで繰り返す場合にも同じ
  集計に含まれる必要がある。
- **①連続自動実行モード（バッチ実行）**: 1回のテストが正常終了/クラッシュいずれで
  止まっても、対象アプリの再起動さえ検知できれば自動的に次の回を開始し、指定回数に
  達するまで繰り返す。夜間放置での再現試行を可能にする。
- **②対象アプリの自動再起動コマンド設定**: ①を完全無人化するための前提として、対象
  アプリの起動コマンド/パスを登録しておき、クラッシュ検知時に自動再起動できるように
  する。手動再起動でも①は機能するべきで、自動起動はあくまで任意の強化。
- **④ステップ別クラッシュ率のハイライト**: ③の内訳をさらに一歩進め、②ステップ一覧の
  UI上に「このステップの直後のクラッシュ N/M回」のようなバッジを表示し、どの操作が
  怪しいか一目で分かるようにする。
- **⑤直前操作ログの自動添付強化**: クラッシュ通知には現在ステップ番号・実行回数・
  シードを記録済みだが、「クラッシュ直前の実際の操作（クリック座標等）N件」をログから
  自動抜粋してサマリーに含めると、シードだけでは再現しない非決定的なクラッシュでも
  直前の操作パターンから当たりをつけやすくなる。


##　追加実装及び修正依頼
  - 操作領域で追加ボタンを押したあとに矩形領域を選択ボタンをおして、左クリックのドラッグで矩形領域を選択後右クリックを押すとダイアログが２つ出てくる。これを正常に１つ出てくるように修正してください。
    - 【調査中】Linux/Xvfb環境（単一右クリック・素早い連続右クリック・「矩形を描画...」ボタンの座標での右クリック等）では再現できませんでした。`RegionSelectorOverlay`/`PointPickerOverlay`に再入防止ガード（`m_finished`未使用だった箇所）は防御的に追加済みですが、根本原因は未確定です。再現する環境（OS・Qtバージョン）、再現手順（クリックの間隔・回数）、毎回再現するか時々かを教えていただけると助かります。
  - macOSの場合に、操作領域を追加しようとすると、他のアプリが見えなくなります。原因を調査して修正してください
  - 検証用のtesttargetを本体ツールの機能に合わせて機能拡張してください
