# EnduranceTestGUI 動作テスト

## 1. 目的

- EnduranceTestGUI の動作確認を行うためのテストケースを定義する。
- もしSPEC.mdの仕様と不整合なテストケースがある場合には、聞いてください。

## 2. テスト対象・環境

- 対象コード
  ./src/ 以下の C++/Qt GUI アプリケーションコード

- 環境
  macOSおよびLinux

## 3. テストケース一覧

### TC-001 操作画面の設定機能テスト
- **操作手順**
  1. EnduranceTestGUI を起動する。
  2. メインウィンドウが表示されることを確認する。
  3. テスト対象アプリケーションを選択する。
  4. 操作領域の追加を押す。
  5. ダイアログが出て矩形領域の選択を押す。
- **期待される結果**
  - 画面が真っ暗にならずに、画面表示を見ながら矩形領域を選択できることを確認する。

### TC-002 ステップ構成のグループ機能テスト
- **操作手順**
  1. EnduranceTestGUI を起動する。
  2. メインウィンドウが表示されることを確認する。
  3. テスト対象アプリケーションを選択する。
  4. 操作領域の追加を押す。
  5. 矩形領域の選択で操作領域を選択
  6. ステップ構成で２つのステップを作成。それぞれのステップの操作を適当に設定
  7. ２つのステップを同時に選択してグループ化
  8. グループを選んで編集を押す
- **期待される結果**
  -　出てきたダイアログの操作に6で設定した操作が反映されていることを確認する。

### TC-003 操作画面追従テスト
- **操作手順**
  1. EnduranceTestGUI を起動する。
  2. メインウィンドウが表示されることを確認する。
  3. テスト対象アプリケーションを選択する。
  4. 操作領域の追加を押す。
  5. 矩形を描画でテスト対象ツールの領域の一部を操作領域として選択
  6. OKボタンを押す
  7. テスト対象ツールの画面上の位置を動かす
  8. ステップ構成で4,5,6で追加した操作領域を選択して編集を押す
- **期待される結果**
  - 操作領域の枠がテスト対象ツールの移動を追従して移動していることを確認

### TC-004 操作画面位置テスト
- **操作手順**
  1. EnduranceTestGUI を起動する。
  2. メインウィンドウが表示されることを確認する。
  3. テスト対象アプリケーションを選択する。
  4. 操作領域の追加を押す。
  5. 矩形を描画でテスト対象ツールの小さなボタンを操作領域として選択
  6. OKボタンを押す
  7. ステップ構成で4,5,6で追加した操作領域を選択して編集を押す
- **期待される結果**
  - 操作領域の枠が5で選択したテスト対象ツールのボタンを意図通りに覆えているかを確認する

### TC-005 待機ステップの追加と実行テスト
- **操作手順**
  1. EnduranceTestGUI を起動する。
  2. TestTarget を起動し、テスト対象として選択する。
  3. ②ステップ構成パネルで「追加」ボタンを押し、「対象GUIの全領域を使用」でステップを1つ追加する。
  4. ③操作パラメータパネルで操作種別を「ランダムクリック（左）」のみ有効にし、回数を10に設定する。
  5. ②ステップ構成パネルで「待機を追加...」ボタンを押し、待機時間を 2000 ms に設定して追加する。
  6. 再度「追加」ボタンでステップをもう1つ追加し、同様に「ランダムクリック（左）」を有効にして回数を10に設定する。
  7. タイミング設定で最大実行回数を 30、最大実行時間を 60 秒、繰り返し回数を 1 に設定する。
  8. 「▶ 開始」ボタンを押してテストを実行する。
- **期待される結果**
  - ログにステップ1のクリック操作が10回出力された後、「待機: 2000 ms」または同等のログが出力され、その後ステップ3のクリック操作が出力される。
  - 待機ステップ中に TestTarget への操作が止まり、約2秒後に次のステップへ進むことを確認する。
  - 待機ステップは実行回数のカウントに含まれず、合計実行回数が 20 回でシーケンス完了となることを確認する。

### TC-006 ステップグループ化と重み付きランダム実行テスト
- **操作手順**
  1. EnduranceTestGUI を起動する。
  2. TestTarget を起動し、テスト対象として選択する。
  3. ②ステップ構成パネルで「追加」ボタンを2回押し、それぞれ「対象GUIの全領域を使用」でステップを2つ追加する。
  4. ステップ1を選択し、③で「ランダムクリック（左）」のみ有効・重みを 1 に設定する。
  5. ステップ2を選択し、③で「ランダムキー入力」のみ有効・重みを 3 に設定する。
  6. ②のリストでステップ1とステップ2を同時に選択（Shift または Ctrl クリック）して「グループ化」ボタンを押す。
  7. 作成されたグループステップを選択し「編集...」ボタンを押してグループ編集ダイアログを開く。
  8. グループ全体の合計呼び出し回数を 20 に設定して OK を押す。
  9. タイミング設定で最大実行回数を 20、最大実行時間を 60 秒、繰り返し回数を 1 に設定する。
  10. 「▶ 開始」ボタンを押してテストを実行する。
- **期待される結果**
  - ②のリストに「ステップ1: グループ（2個のステップ、合計呼び出し20回）」のような表示が出ることを確認する。
  - テスト実行後のログで、クリック操作とキー入力がランダムに混在し、合計20回の操作後に停止することを確認する。
  - 重み比（1:3）により、キー入力の発生回数がクリックより概ね多いことをログで確認する。

### TC-007 操作間隔の指定方法切り替えテスト
- **操作手順**
  1. EnduranceTestGUI を起動する。
  2. TestTarget を起動し、テスト対象として選択する。
  3. ①対象選択パネルの「操作間隔:」欄が初期状態で「ms指定」になっていることを確認し、最小間隔を 50 ms、最大間隔を 200 ms に設定する。
  4. ラジオボタンを「回数/秒指定」に切り替え、表示が切り替わって回数/秒の入力欄が現れることを確認する。
  5. 最小回数/秒を 5.00、最大回数/秒を 10.00 に設定する。
  6. 再び「ms指定」に切り替え、3 で設定した 50 ms / 200 ms の値が保持されていることを確認する。
  7. もう一度「回数/秒指定」に戻し、5 で設定した 5.00 / 10.00 の値が保持されていることを確認する。
  8. 「回数/秒指定」のまま、ステップを1つ追加し（「対象GUIの全領域」・「ランダムクリック（左）」・回数10）、最大実行回数を10、最大実行時間を30秒、繰り返し回数を1に設定してテストを開始する。
- **期待される結果**
  - モードを切り替えても、それぞれのモードの入力値が失われないことを確認する。
  - 「回数/秒指定」モードで実行したとき、操作間隔が概ね 100 ms〜200 ms（5〜10 回/秒の逆数）の範囲になっていることをログのタイムスタンプ差分で確認する。

### TC-008 最大実行回数・最大実行時間・繰り返し回数による自動停止テスト
- **操作手順**
  1. EnduranceTestGUI を起動する。
  2. TestTarget を起動し、テスト対象として選択する。
  3. 「対象GUIの全領域を使用」でステップを1つ追加し、「ランダムクリック（左）」を有効・回数を 5 に設定する。
  4. タイミング設定で最大実行回数を 10、最大実行時間を 120 秒、繰り返し回数を 2 に設定してテストを開始する。
  5. テストが自動停止したらログを確認する。（a）
  6. 次に、最大実行回数を 10000、最大実行時間を 5 秒、繰り返し回数を 10000 に変更して再度テストを開始する。
  7. テストが自動停止したらログを確認する。（b）
  8. 次に、最大実行回数を 10000、最大実行時間を 120 秒、繰り返し回数を 1 に変更して再度テストを開始する。
  9. テストが自動停止したらログを確認する。（c）
- **期待される結果**
  - （a）繰り返し回数 2 を先に満たして停止し、ログに「シーケンス繰り返し回数上限に達しました」または「停止しました」等の自動停止メッセージが出力されることを確認する。
  - （b）実行時間 5 秒の上限で停止し、ログに対応するメッセージが出ることを確認する。
  - （c）合計実行回数が 5 回（1ステップ×回数5×繰り返し1）に達して停止し、ログに対応するメッセージが出ることを確認する。

### TC-009 フローティング緊急停止パネルによるテスト停止テスト
- **操作手順**
  1. EnduranceTestGUI を起動する。
  2. TestTarget を起動し、テスト対象として選択する。
  3. 「対象GUIの全領域を使用」でステップを1つ追加し、「ランダムクリック（左）」を有効・回数を 1000 に設定する。
  4. タイミング設定で最大実行回数を 10000、最大実行時間を 600 秒、繰り返し回数を 1000 に設定する。
  5. 「▶ 開始」ボタンを押してテストを実行する。
  6. フローティングの StopPanel（常時最前面パネル）が表示されることを確認する。
  7. StopPanel の「■ 停止」ボタンをクリックする。
- **期待される結果**
  - テストが即座に停止し、ログに停止を示すメッセージが出力されることを確認する。
  - StopPanel が非表示になり、メインウィンドウの「▶ 開始」ボタンが再び有効になることを確認する。

### TC-010 グローバルホットキーによる緊急停止テスト
- **操作手順**
  1. EnduranceTestGUI を起動する。
  2. TestTarget を起動し、テスト対象として選択する。
  3. 「対象GUIの全領域を使用」でステップを1つ追加し、「ランダムクリック（左）」を有効・回数を 1000 に設定する。
  4. タイミング設定で最大実行回数を 10000、最大実行時間を 600 秒、繰り返し回数を 1000 に設定する。
  5. 「▶ 開始」ボタンを押してテストを実行する。
  6. TestTarget を最前面にしてフォーカスを移す（EnduranceTestGUI がアクティブでない状態にする）。
  7. キーボードで Ctrl+Alt+Shift+Escape を押す。
- **期待される結果**
  - EnduranceTestGUI がフォーカスを持っていなくてもテストが即座に停止することを確認する。
  - ログに停止を示すメッセージが出力され、StopPanel が非表示になることを確認する。
  - グローバルホットキー登録に成功した場合は起動時ログに「グローバルホットキー登録成功」等のメッセージが出ていることを併せて確認する。

### TC-011 クラッシュ検知による自動停止テスト
- **操作手順**
  1. EnduranceTestGUI を起動する。
  2. TestTarget を起動し、テスト対象として選択する。
  3. 「対象GUIの全領域を使用」でステップを1つ追加し、「ランダムクリック（左）」を有効・回数を 1000 に設定する。
  4. タイミング設定で最大実行回数を 10000、最大実行時間を 600 秒、繰り返し回数を 1000 に設定する。
  5. 「▶ 開始」ボタンを押してテストを実行する。
  6. テスト実行中に TestTarget を強制終了する（macOS: `kill -9 <PID>`、Linux: 同様、またはタスクマネージャから強制終了）。
- **期待される結果**
  - EnduranceTestGUI がクラッシュを検知して自動的にテストを停止することを確認する。
  - ログに「対象アプリケーションの異常終了（クラッシュ）を検知したため停止しました」等のメッセージが出力されることを確認する。
  - クラッシュ通知ダイアログが表示されることを確認する。
  - ①対象選択コンボボックスに再起動した TestTarget が自動的に再選択される（v0.49 の動作）ことを、TestTarget を再起動してフォーカスを戻すことで確認する。

### TC-012 操作領域スクリーンショット撮影・保存テスト
- **操作手順**
  1. EnduranceTestGUI を起動する。
  2. TestTarget を起動し、テスト対象として選択する。
  3. ①操作領域の追加で TestTarget の一部（例: ボタン群の領域）を矩形選択し、名前付き操作領域「ボタン領域」として登録する。
  4. 「対象GUIの全領域を使用」のステップと「ボタン領域」を使うステップの計2ステップを追加する。
  5. タイミング設定の「操作領域スクリーンショットの撮影タイミング」を「ステップが変わるたび」に設定する。
  6. 最大実行回数を 20、最大実行時間を 60 秒、繰り返し回数を 1 に設定してテストを開始する。
  7. テストが完了または停止したら、ログ欄の隣にある「操作領域画像を保存...」ボタンが有効になっていることを確認する。
  8. 「操作領域画像を保存...」を押し、任意のフォルダを選択して保存する。
- **期待される結果**
  - 保存されたフォルダに PNG ファイルが作成されることを確認する。
  - PNG ファイルを開き、緑の実線で操作領域が描かれ、領域名（「対象GUIの全領域」または「ボタン領域」）がラベルとして画像内に表示されていることを確認する。
  - ファイル名にステップ表示名と操作領域名が含まれることを確認する。

### TC-013 ログのファイル自動保存テスト
- **操作手順**
  1. EnduranceTestGUI を起動する。
  2. TestTarget を起動し、テスト対象として選択する。
  3. 「対象GUIの全領域を使用」でステップを1つ追加し、「ランダムクリック（左）」を有効・回数を 10 に設定する。
  4. 最大実行回数を 10、最大実行時間を 60 秒、繰り返し回数を 1 に設定してテストを開始する。
  5. テストが完了したら `~/Documents/EnduranceTestGUI_Logs/` ディレクトリを開く。
  6. 直近のタイムスタンプを持つ `run_<タイムスタンプ>.log` ファイルをテキストエディタで開く。
  7. ログ欄の「ログを保存...」ボタンを押し、任意の場所にログを手動保存する。
- **期待される結果**
  - `~/Documents/EnduranceTestGUI_Logs/` に `run_<タイムスタンプ>.log` が自動生成されていることを確認する。
  - ファイルにテスト実行開始から終了までの全操作ログが記録されており、画面上のログと同じ内容が含まれることを確認する。
  - 「ログを保存...」で手動保存したファイルと自動保存ファイルの内容が（表示上限の差を除いて）一致することを確認する。

### TC-014 起動時セットアップマクロ（デターミニスティックシーケンス）テスト
- **操作手順**
  1. EnduranceTestGUI を起動する。
  2. TestTarget を起動し、テスト対象として選択する。
  3. ①対象選択パネルの「起動時セットアップ」欄（または専用のセットアップマクロ編集UI）を開く。
  4. 以下の固定シーケンスをセットアップアクションとして追加する:
     - TestTarget のテキスト入力欄の座標をクリック
     - テキスト「hello」をタイプ入力
     - 待機 500 ms
  5. ②ステップ構成で「対象GUIの全領域を使用」のステップを1つ追加し、「ランダムクリック（左）」を有効・回数 10 に設定する。
  6. 最大実行回数を 10、最大実行時間を 60 秒、繰り返し回数を 1 に設定してテストを開始する。
- **期待される結果**
  - ランダム操作ループが始まる前に、セットアップシーケンスが1回だけ実行されることを確認する。
  - ログにセットアップアクション（クリック・テキスト入力・待機）の実行が記録され、その後ランダムクリックのログが続くことを確認する。
  - TestTarget のテキスト入力欄に「hello」が入力された状態でランダムループが開始されることを目視で確認する。


### TC-015 連続実行テスト
- **操作手順**
  1. EnduranceTestGUI を起動する。
  2. TestTarget を起動し、テスト対象として選択する。
  3. 対象アプリの自動起動コマンドを設定する。
  4. 起動時セットアップを設定する。
  5. 操作領域としてテスト対象の一部の領域を選択する。
  6. 「連続実行回数」を 5 に設定する。
  7. 「連続実行開始」ボタンを押す。
- **期待される結果**
  - 連続実行が開始され、5回のテストが順次実行されることを確認する。
  - 各テストの開始前に、対象アプリが自動的に起動され、設定された起動時セットアップが実行されることを確認する。


